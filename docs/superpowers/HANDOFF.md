# Loomn — Handoff per il prossimo agente

> **Data:** 2026-06-23 · **Branch:** `main` · **HEAD:** `d92b480` ([AGGIORNATO sessione 3, 2026-06-23: **D-01b (generazione AI-da-brief del seed) FATTO e mergiato** `76850e8` (805→817 test) **+ flag gate-harness CHIUSO** `d92b480` (`pnpm gate:selftest` via bin electron diretto, ri-verificato fase1+fase2 VERDICT: PASS); tutto su origin/main fino a `4c442e8`; **AZIONE CORRENTE = D-01c (onboarding: IPC generate-seed + UI brief→genera→review/edit→conferma→apertura) oppure D-03 (registro multi-campagna additivo)** — leggi il PRIMO bullet del §0] · storico: (**🎉 REMEDIATION COMPLETA 28/28; D‑01a (Campaign Seed: motore+contesto) FATTO e mergiato [805 test, `44085b1..14e7d21`], prossimo = decidere con l utente D‑01b (AI‑da‑brief) o D‑03 (registro multi‑campagna additivo); ⚠️ flag F7: `pnpm gate:selftest` rotto in questo ambiente [`pnpm exec electron` not-found post-rebuild:electron] → gate verificato col binario electron diretto, fix raccomandato nel gate script** — vedi il PRIMO bullet del §0; ultimo CODICE `4885487` [F7], gate PASS, 777 test; *storico:* **FASE 5 remediation [renderer-logica] FATTA e mergiata**, **769 test**, ABI jsdom [NESSUN gate Electron], azione corrente = **FASE 6 [renderer-UI]**; prima: **FASE 4 [IPC/main] FATTA** a `cce7432`, 757 test, gate Electron 2 fasi PASS; **FASE 3 [AI] FATTA** a `a413ab8`, 751 test; **FASE 2 [memoria/host] FATTA** a `fe14646`, 742 test; **FASE 1 [motore] FATTA** a `582fb7a`, 719 test; prima: report audit + campagna remediation 7 fasi + piano dettagliato F1 [collocazione test corretta]; **ultimo codice = Piano 10e a `0e91cc2`**, §0-tervicies — Diario + Compagnia: memoria narrativa L2 + canon + roster display-only, **691 test**, gate Electron 2 fasi PASS, pushato; piano 10e a `bbce6c5`. Prima: Piano 10d a `83cafea` §0-duovicies [Scheda + inventario display-only, 664 test]; Piano 10c a `f0e931c` §0-unvicies [cockpit scontro + zone display-only, 639 test]; Piano 10b a `1138a97` + seam dispatch→dadi `6423011` + fix post-10b `1fadda2` — §0-vicies; 10f a `bf532f4`; 10g a `fa6bc17`; 10a a `071e71c`; Piano 0 a `614a6bb`; fai `git log`) · **Stato:** Piani 1-6 + **7a/7b/7c + 8a/8b/8c + 9a + 9b + 9c-i + 9c-ii** completi e mergiati = **PIANO 9 COMPLETO** (engine + persistenza + Provider Layer AI + StructuredOutputPort + AI Master pipeline + Canon Ledger L1.5 + Reflection/L2/Salienza + Context Assembler + **Shell Electron `app/desktop`** + **Wiring memoria+AI `@loomn/host`** + **Application layer `createCampaignService`/`createLanguageProvider` + `commandSchema`** + **Integrazione Electron: IPC reale + DB su `userData` + `safeStorage`**), **266 test verdi a fine Piano 9** (conteggio corrente **664**, vedi sotto), typecheck pulito (incluso `app/desktop` via `vue-tsc` e **`@loomn/host`**), tree pulito. **Gate "esegui l'app" del 9c-ii SUPERATO** (self-test 2 fasi `VERDICT: PASS`; vedi §7-quinquies). **Post-Piano 9:** eseguita la **Traccia B (slice di validazione memoria+AI con LLM reale)** — vedi **§0-bis** e `docs/superpowers/findings-slice-llm.md`; **prossima mossa decisa = traccia engine/AI** guidata dai findings. **G1 (schemi tool coercivi-ma-strict) ✅ FATTO** (`d1ec4a5`) e **F4 (la storia entra in canone) ✅ FATTO e mergiato** (`9e143aa`, **280 test**) — vedi §0-ter/§0-quater. **MANDATO DECISO con l'utente: completare TUTTO il backlog pre-Piano 10** prima di toccare la UI — sequenza in **§0-quinquies** (~~G6~~ ✅ → ~~F2~~ ✅ → **traccia engine decomposta in SP1-4: ~~SP1~~ ✅ → ~~SP2 `apply_effect`~~ ✅ → ~~SP3 quest~~ ✅ → ~~SP4 FSM §5.5~~ ✅** → ~~G3/G4~~ ✅ → ~~F3/G5~~ ✅ → ~~segmentazione reflect (item 6)~~ ✅ = **BACKLOG CHIUSO**). **G6** (`b9e3614`, §0-sexies); **F2** (`2d517fe`, §0-septies); **SP1 request_check** (`c04d9a3`, §0-octies); **SP2 `apply_effect`** (`dbee1a8`, §0-novies); **SP3 quest in L1 + `advance_quest`** (`8d3f67b`, **360 test**, §0-decies); **SP4 FSM di fase §5.5 FATTO** (`50c6060`, 392 test, §0-undecies) → **traccia engine CHIUSA**. **G3/G4 (vocabolario di gioco iniettato via `Ruleset` §5.3 + `spawn_npc` combat-ready) FATTO** (`06b398b`, **422 test**, §0-duodecies). **F3/G5 (coercizione strutturale write-path Reflection) FATTO** (`0acbff0`, **427 test**, §0-terdecies). **Item 6 (segmentazione `reflect` per scena) FATTO** (`2607342`, **444 test**, §0-quaterdecies) → **BACKLOG PRE-PIANO 10 CHIUSO** (G6/F2/SP1-4/G3-G4/F3-G5/item 6 tutti fatti e mergiati). **STUDIO PIANO 10 ✅ CHIUSO** (spec autorità `specs/2026-06-17-piano10-ui-design.md`, `5530aaf`; vedi **§0-quindecies**). **Piano 0 — IPC/CQRS completeness ✅ FATTO e mergiato** (`614a6bb`, **476 test**, vedi **§0-sexdecies**). **Piano 10a — Fondamenta UI ✅ FATTO e mergiato** (`071e71c`, **515 test** = 476 packages + 39 renderer, vedi **§0-septdecies**). **Piano 10g — Vocabolario di gioco su IPC (`get-ruleset`) ✅ FATTO e mergiato** (`fa6bc17`, 533 test, vedi **§0-octodecies**). **Piano 10f — Impostazioni/provider + first-run + creazione PG + controlli GM ✅ FATTO e mergiato** (`bf532f4`, **570 test** = 497 packages + 73 renderer; gate Electron 2 fasi `VERDICT: PASS`; vedi **§0-novodecies**). **Piano 10b — Gioco (chat/narrazione + dadi 3D) ✅ FATTO e mergiato** (`1138a97`, 609 test; + seam dispatch→dadi `6423011` [612]; fix post-10b `1fadda2` [616]; **dadi 3D `@3d-dice/dice-box-threejs` a facce forzate — NON `@3d-dice/dice-box`: la scelta dello spec e stata invalidata dallo spike**; vedi **§0-vicies**). **Piano 10c — Combattimento (cockpit + zone display-only) ✅ FATTO e mergiato** (`f0e931c`, 639 test, vedi **§0-unvicies**). **Piano 10d — Scheda + inventario display-only ✅ FATTO e mergiato** (`83cafea`, **664 test** = 500 packages + 164 renderer; gate Electron 2 fasi `VERDICT: PASS`; vedi **§0-duovicies**). **Piano 10e — Diario + Compagnia ✅ FATTO e mergiato** (`0e91cc2`, §0-tervicies — narrativa L2 + canon via i canali read del Piano 0 `getSummaries`/`getCanon` + trigger `reflect`; roster PG/PNG con relazioni canon DISPLAY-ONLY; **ULTIMO sotto-piano del Piano 10 → PIANO 10 (UI) COMPLETO**). Ordine sotto-piani: `10a✅ → 10g✅ → 10f✅ → 10b✅ → 10c✅ → 10d✅ → 10e✅`. **AZIONE CORRENTE = ESEGUIRE LA CAMPAGNA DI REMEDIATION DEI BUG D'AUDIT (28 finding a causa radice, in 7 fasi). AUDIT FATTO** (report `audits/2026-06-19-loomn-audit-findings.md`: 0 Critical / 13 Important / 15 Minor; 35 findings verificati avversarialmente, 2 refutati, 143 probed); **piano-campagna a 7 fasi** `plans/2026-06-19-loomn-remediation-campaign.md`; **piano dettagliato Fase 1** `plans/2026-06-19-loomn-fix-remediation-f1-motore.md` → **FASE 1 (motore) FATTA** (`582fb7a`, 719 test) e **FASE 2 (memoria/host) FATTA e mergiata** (`fe14646`, **742 test**; branch eliminati, pushati a origin/main; **FASE 3 [AI] FATTA a `a413ab8`, 751 test; **FASE 4 [IPC/main] FATTA** a `cce7432`, 757 test, gate Electron 2 fasi PASS → prossimo = **FASE 5 [renderer-logica]**). Vedi il primo bullet del TL;DR §0 e la checklist §9. DOPO la remediation (F1→F7) → tracce design-first separate (D-01 incipit/campagna progettata [il 'problema enorme'], D-02 redesign UI/UX, D-03 multi-campagna) o feature deferite (Inventario, movimento/zona).**
>
> Questo documento ti permette di riprendere **esattamente** da dove siamo. Leggilo tutto prima di agire. La memoria di progetto è in `.claude/.../memory/loomn-project.md` e `loomn-working-style.md` (caricata a inizio sessione).

---

## 0. TL;DR — cosa fare adesso

> ⏩ **AZIONE CORRENTE (2026-06-23, fine sessione 3) — D‑01b (generazione AI‑da‑brief del Campaign Seed) FATTO e mergiato → riportare all utente per la decisione sulla prossima slice (D‑01c onboarding o D‑03 registro multi‑campagna).** D‑01b eseguito DESIGN-FIRST (brainstorming → spec `4bdc975` → piano `cedb5e7`) poi subagent-driven (4 task TDD: implementer→spec-review→code-quality; code-quality **opus** su T2 transform e T4 host; **final review opus "Ready to merge: Yes"**, 0 Critical/0 Important), **`76850e8`**, **817 test** (805→817, +12), pushato `f0a0256..76850e8`, branch `feat/d01b-campaign-generation` eliminato. **Consegnato:** `@loomn/ai/campaign-generation.ts` — `CampaignBrief` ibrido (`{text, name?, overrides?{genres,tone,npcCount,contentGuidance}}`); `rawSeedSchema` **transform-free** (gate di GENERAZIONE, NON read-path; `tier` enum `comune|esperto|eccezionale`); `rawToCampaignSeed` **PURO** (slugify ids + dedup `-2`; `tier`→valore `1/2/3` su TUTTI gli attributi/abilità lette da `ruleset.vocabulary`; fatti verbatim; `contentGuidance` override>raw; `worldRules` condizionale exactOptional); `generateCampaignSeed` single-shot via `StructuredOutputPort.generate` (`schemaName:'campaign_seed'`, temperature 0.9, `SYSTEM_PROMPT` italiano); `slugify` puro (NFD + strip U+0300–U+036F). `@loomn/host`: `CampaignService.generateSeed(brief)` **NON-enqueued** (inietta `deps.structured`+`deps.ruleset`, ritorna una BOZZA `CampaignSeed`, **NON semina**; la conferma resta `seedCampaign` di D‑01a) + re-export `CampaignBrief`. **Decisioni brainstorming (con l utente):** brief ibrido (Q1); confine = gen `@loomn/ai` + metodo host, **niente IPC/UI/gate** → D‑01c (Q2); stat PNG riempite **DAL CODICE** (AI vocab-agnostica, confirm mai fallibile) — l utente voleva PNG riempiti evitando sia l iniezione del vocabolario nel prompt sia il rischio di fallire al confirm → `tier`→valore dal Ruleset (Q3/Q3b); **genera-e-valida, bozza sempre confermabile** (soli errori veri = provider assente / `StructuredOutputError`; nessuna incoerenza rompe il confirm — i fatti sono triple libere) (Q4). **Debt-free:** `@loomn/shared`/`@loomn/engine`/`app/desktop` **NON toccati** (verificato `git diff --stat`); `rawSeedSchema` è solo gate di generazione (bound ammessi, mai un read-path). **Garanzia "bozza sempre confermabile" provata da test host END-TO-END** (`generateSeed`→`seedCampaign` riesce: `campaignFrame` + attore seminati, non mock). Spec `docs/superpowers/specs/2026-06-23-d01b-campaign-generation-design.md` (`4bdc975`), piano `docs/superpowers/plans/2026-06-23-d01b-campaign-generation-plan.md` (`cedb5e7`). **⚠️ FLAG GATE-HARNESS CHIUSO** (`d92b480`, fix ops post-D‑01b): `pnpm gate:selftest` ora lancia il **bin electron diretto** (`require('electron')` dal workspace `app/desktop` → `spawnSync` senza shell) invece di `pnpm --filter @loomn/desktop exec electron`, che si rompeva subito dopo `rebuild:electron` (ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL "electron not found"). **Gate ri-verificato canonico: fase1+fase2 `VERDICT: PASS`, `rebuild:node` ripristina l ABI Node** → di nuovo utilizzabile per D‑01c (main/renderer). `rebuild:electron` resta via `pnpm exec` (electron-rebuild funziona; era solo il successivo lancio `electron` a rompersi). **➡️ PROSSIMO PASSO CONCRETO: riportare all utente e attendere la decisione sulla prossima slice — D‑01c (IPC `generate-seed` + UI onboarding brief→genera→review/edit→conferma→apertura) oppure D‑03 (registro multi‑campagna additivo).** HEAD codice = `d92b480` (D‑01b `76850e8` + fix gate-harness `d92b480`); baseline test **817**; tree pulito (solo `.claude/`, MAI committarlo). **`pnpm gate:selftest` ora funziona** (flag F7 chiuso).
>
> ⏩ **AZIONE CORRENTE (2026-06-23, fine sessione 2) — D‑01a (Campaign Seed: motore+contesto) FATTO e mergiato → decidere con l utente la prossima slice (D‑01b o D‑03).** D‑01a eseguito subagent-driven (5 task TDD: implementer→spec-review→code-quality, + final review opus), **`14e7d21`**, **805 test** (777→805), pushato `44085b1..14e7d21`, branch `feat/d01a-campaign-seed` eliminato. **Consegnato:** Command `SeedCampaign` (`decide`→`[CampaignFramed, ...ActorAdded]`, riusa `AddActor` via helper `buildActorAddedEvent`, once-guard `/gia seminata/`, batch-dup `/duplicato/`, phase-agnostic) + evento `CampaignFramed` + `GameState.campaignFrame?` event-sourced + tipi engine in `packages/engine/src/campaign.ts`; schemi shared **permissivi** (`campaignFrameSchema`/`campaignFramedEventSchema` nell outer-`z.union` per via del `.transform()`, `gameStateSchema.campaignFrame`), **bound SOLO su `seedCampaignCommandSchema`** (convenzione dieGroupSchema/Command); blocco "campaign frame" **never-cut** anteposto a L1 nel Context Assembler (escluso dal budget L2); host `seedCampaign(seed)` **atomico** (1 `runInTransaction`: eventi+canon `seed-<i>` con `eventSeq=versionBefore+1`=seq di CampaignFramed; **`state=s` avanzato SOLO dopo tutte le scritture DB** — fix code-quality anti-divergenza in-memory/DB su rollback) + **narrazione d apertura best-effort** fuori dalla tx (`_runTurn('(apertura)')` estratto, swallow se no provider, `narration` omessa se vuota); `devCampaignSeed` ("La Cripta di Vetro"); **fondamenta multi‑campagna: un DB per campagna** `userData/campaigns/<id>/loomn.db` via `campaignDbPath`+`DEFAULT_CAMPAIGN_ID`, `main` apre la campagna attiva (id `default`). **2 BUG a causa radice colti durante/dopo l implementazione, fixati debt-free:** (1) **dir genitore del DB inesistente** — better-sqlite3 NON crea le dir intermedie → boot crash "Cannot open database because the directory does not exist" (**visto dal vivo dall utente al gate**); fix al **seam** `createMemorySystem` = `mkdirSync(dirname(dbPath),{recursive:true})` guard `:memory:` (D‑03 ne beneficia); (2) **brick path Infinity** — `seedNpcCommandSchema.attributes`/`skills` usavano `z.number()` nudo (accetta Infinity) → `JSON.stringify`→`null`→ read-path `actorSchema` con `finiteNumber` rifiuta `null` al replay = DB inservibile; fix = `finiteNumber` sul command schema (**colto dalla final-review-opus**, primed per D‑01b/AI). **GATE Electron 2 fasi PASS** (fase1 versione 0 DB fresco al nuovo path; fase2 **versione 8 PERSISTITA** dopo riavvio+reload; **self-test v8 INVARIATO**, non semina, `renderer.ts` intatto). **⚠️ FLAG GATE-HARNESS (NON D‑01a, F7 ops, DA FIXARE): `pnpm gate:selftest` è ROTTO in questo ambiente** — `pnpm --filter @loomn/desktop exec electron .` dà `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "electron" not found` **subito dopo `rebuild:electron`** (lo stesso comando funziona standalone; `build`/`rebuild` sono *script* pnpm, `electron` è un *bin* → la risoluzione del bin si rompe post-rebuild). Gate verificato lanciando il **binario electron diretto** `app/desktop/node_modules/.bin/electron app/desktop` (con `LOOMN_SELFTEST`/`LOOMN_USERDATA`; entrambe le fasi VERDICT: PASS). **Fix raccomandato (follow-up, `scripts/gate-selftest.mjs`): invocare electron via il path del bin diretto invece di `pnpm --filter exec electron`.** Minori declinati/annotati: `framedSeq` (versionBefore+1 è già la forma corretta/chiara — `append` ritorna l ULTIMO seq); re-export `SeedOutcome` (rinviato a D‑01c che cabla l IPC). **➡️ PROSSIMO PASSO CONCRETO: riportare all utente per la decisione sulla prossima slice — D‑01b (generazione AI‑da‑brief del seed via `campaignSeedSchema`/StructuredOutput) o D‑03 (registro multi‑campagna lista/crea/seleziona/switch/elimina + UX, ora *additivo*: le fondamenta isolamento-per-DB+identità sono in D‑01a).** HEAD = `14e7d21`; baseline test **805**; tree pulito (solo `.claude/`, MAI committarlo). Comandi ops invariati (`pnpm verify`, `pnpm gate:selftest` [⚠️rotto, vedi flag], `pnpm gate:kill-ghost`).
>
> ⏩ **CONTESTO (2026-06-23, fine sessione 1) — REMEDIATION COMPLETA (28/28) → traccia D‑01 (incipit/campagna) APERTA: spec + piano di D‑01a PRONTI → [ORA ESEGUITO, vedi il bullet sopra].** Dopo la remediation (F1‑F7 tutte mergiate, vedi il bullet sotto), l utente ha scelto via AskUserQuestion la traccia design-first **D‑01** (incipit/campagna — "il problema enorme", la più a leva). **Brainstorming fatto, decisioni bloccate:** (1) **seed source-agnostic ibrido** (prima sorgente = AI‑da‑brief, in D‑01b); (2) **seed strutturato che semina lo stato** (PNG→attori, luoghi/fatti→canon — "il codice è l arbitro" anche nel setup); (3) **decomposizione** D‑01a (motore+contesto, questo) → D‑01b (AI‑da‑brief) → D‑01c (UX onboarding) → D‑01d/Piano 11 (moduli); (4) **Approccio A** (Command `SeedCampaign` che l engine espande in `[CampaignFramed, ...ActorAdded]`; `GameState.campaignFrame` event-sourced, legittimato dal precedente `Quest`); (5) **fondamenta multi‑campagna decise ORA** (isolamento = **un DB per campagna** `userData/campaigns/<id>/loomn.db`, identità `id`+`name` nel frame; la **gestione** — registro/UX lista/crea/seleziona/switch/elimina — resta **D‑03, additiva**, zero rework). **Spec autorità: `specs/2026-06-23-d01a-campaign-seed-design.md` (`4b61499`)** — verificato file:line contro l architettura (4 esplorazioni: `decide`→`DomainEvent[]`, auto-fill PNG nel motore `commands.ts:109`, debt-free `gameStateSchema` non-strict, drift-guard engine/shared/host, canon `seed-<i>`, opening narration LLM-backed come `reflect` → fuori dal gate). **Piano dettagliato: `plans/2026-06-23-d01a-campaign-seed-plan.md` (`fd5437a`)** — 5 task TDD bite-sized: T1 frame/evento/stato (engine+shared, drift-guard); T2 Command `SeedCampaign`+`decide` (riusa `AddActor` via helper `buildActorAddedEvent`, once-guard); T3 blocco campaign-frame never-cut nel Context Assembler; T4 `seedCampaign` host atomico (`runInTransaction` + canon + narrazione d apertura best-effort, estrae `_runTurn`); T5 `devCampaignSeed` + `campaignDbPath` + wiring `main` + **gate Electron**. Conteggi attesi 777→~811, **ABI Node salvo T5** (l unico gate Electron, **versione self-test 8 INVARIATA** — il self-test NON semina, decisione presa). **➡️ PROSSIMO PASSO CONCRETO: eseguire il piano D‑01a subagent-driven** (flusso §4: `git checkout -b feat/d01a-campaign-seed` da `main`; per task implementer→spec-review→code-quality-review; final review opus; gate `pnpm gate:selftest` al T5; `finishing-a-development-branch` merge ff→`pnpm test`→`git push origin main`→cancella branch; aggiorna HANDOFF+memoria). **DOPO D‑01a:** D‑01b (AI‑da‑brief) o D‑03 (registro multi‑campagna, ora additivo), da decidere con l utente. **HEAD = `fd5437a`** (doc); ultimo CODICE = `4885487` (F7); baseline test **777**; tree pulito (solo `.claude/`, MAI committarlo). **Comandi ops (F7): `pnpm verify`, `pnpm gate:selftest`, `pnpm gate:kill-ghost`** (README; il gate flippa l ABI a Electron e la ripristina a Node).
>
> ⏩ **CONTESTO (2026-06-23) — FASE 7 (ops/gate & CI) ✅ FATTA E MERGIATA → 🎉 REMEDIATION COMPLETA (28/28 finding a causa radice, debt-free).** F7 (`4885487`, **777 test** invariati [ops puro: nessun nuovo test vitest — `renderer.ts` e il bootstrap, non e importato da alcun test], branch `fix/remediation-f7-ops-gate` eliminato, pushato `e6ce91b..4885487`; piano `plans/2026-06-23-loomn-fix-remediation-f7-ops-gate.md`; **gate Electron 2 fasi `VERDICT: PASS` su ENTRAMBE via il NUOVO `pnpm gate:selftest`, incluso il nuovo check reload, versione 8 invariata**) ha chiuso a causa radice **M‑11** (gate senza script/CI) + **D‑06** (hazard ABI) + la **lacuna di copertura del reload** che fece sfuggire I‑02 al gate. Scope = SOLO ops/config (6 file): root `package.json` (3 script nuovi), `scripts/kill-ghost.mjs` + `scripts/gate-selftest.mjs` (nuovi), `app/desktop/src/renderer/src/renderer.ts` (SOLO il self-test), `.github/workflows/verify.yml` (nuovo), `README.md` (nuovo). **Consegna:** (1) **`pnpm verify`** = `pnpm -r typecheck && pnpm test` (rete anti-regressione aggregata); (2) **`pnpm gate:kill-ghost`** (`scripts/kill-ghost.mjs`): termina SOLO gli `electron.exe` di Loomn (Name~`electron` + CommandLine~`tabl|loomn` + esclude il PID corrente → NON si auto-uccide [il gate e node], NON tocca VS Code/Claude/Slack [binari diversi]; **deviazione consapevole** dal pattern documentato `electron|node` per evitare l auto-kill + il fatto che il lock-holder dell ABI Electron e sempre electron.exe; + guardia `process.argv[1]` nel direct-run); (3) **`pnpm gate:selftest`** (`scripts/gate-selftest.mjs`): orchestratore Node **single-source** [riusa `pnpm rebuild:electron`/`rebuild:node`/`build` + `killGhosts()`] = kill-ghost → build → **[try:** rebuild:electron → 2 lanci self-test (`LOOMN_SELFTEST` 1/2 su userData temp `mkdtemp`)**] → finally:** `rebuild:node` (ABI Node ripristinata **SEMPRE**, anche su flip parziale — il flip e DENTRO il try, hardening colto dalla code-quality opus) + cleanup temp; esce 0 solo se entrambe le fasi `VERDICT: PASS`, niente false-PASS (phase1/phase2 default 1=FAIL); (4) **estensione self-test path RELOAD** [`renderer.ts` fase 2: `location.reload()` **one-shot** via `sessionStorage` [in-memory per sessione renderer → non leakka tra i 2 lanci-processo, persiste tra il reload] → ogni asserzione di persistenza vale post-reload + check esplicito `dopo il reload lo store si ri-popola SENZA dispatch (I‑02 pull-on-mount + .on did-finish-load)`; **read-only → versione resta 8**; chiude la lacuna: il gate copriva il riavvio-processo, MAI il reload-in-finestra]; (5) **README** con l hazard ABI documentato (Node↔Electron, sintomo `NODE_MODULE_VERSION 146/137`→`rebuild:node`); (6) **CI** `.github/workflows/verify.yml` = `pnpm verify` (ABI Node, portabile) su push/PR — il gate Electron resta **local-only** (flip ABI + Electron headless non portabili in CI), documentato. Subagent-driven 5 task (implementer + spec-review per ognuno; code-quality opus su kill-ghost/gate/reload, saltata-e-dichiarata sui 2 task di sola config README+CI) + **final review opus** = "Ready to merge: Yes" (0 Critical/0 Important; verificato: scope ops-only [**nessun file di prodotto di F1‑F6 toccato**], debt-free [nessuno schema toccato], versione‑8 invariata, fase‑1 self-test intatta, kill-ghost safety, gate ABI-restore airtight, reload one-shot leak-proof). **Code-quality opus ha colto 2 hardening accolti:** kill-ghost guardia `process.argv[1]` (`b46f939`); gate flip ABI dentro il try così `rebuild:node` ripristina SEMPRE (`168dd75`). **➡️ PROSSIMO: la remediation e CHIUSA — NON proseguire in autonomia su iniziative grandi. Si RIPORTA all utente e si attende una decisione su quale traccia DESIGN-FIRST aprire** (con `superpowers:brainstorming` + `frontend-design` dove UI): **D‑01** (incipit/campagna progettata, il "problema enorme"), **D‑02** (redesign UI/UX), **D‑03** (multi-campagna), le **feature deferite** (Inventario & Equipaggiamento, movimento/topologia di zona — §8), oppure **Piano 11** (moduli a tema). **Flag residui annotati (NON nei 28, NON in F7):** `rulesetResultSchema.defaultResources` `z.number()` nudo (shared); journal store sovrascrive il 1o errore se entrambi i canali read falliscono (store); `provider-status.error` non surfacciato; factory `nextRound` usata solo dal suo test (lib); osservazione minore final-review: il README dice "matcha `electron.exe`" mentre il matcher e `-match 'electron'` (substring) — comportamento corretto/safe, lasciato. **(`.claude/` = untracked, MAI committato.)**
>
> ⏩ **CONTESTO (2026-06-22e) — FASE 6 (renderer-UI) ✅ FATTA E MERGIATA (la FASE 7 e ORA FATTA E MERGIATA, vedi il bullet sopra).** F6 (`d6ba863`, **774 test** = 565 packages + 209 renderer, branch `fix/remediation-f6-renderer-ui` eliminato, pushato a `origin/main`; piano `plans/2026-06-22-loomn-fix-remediation-f6-renderer-ui.md`; **ABI jsdom + verifica visiva preview tools + gate Electron 2 fasi `VERDICT: PASS` su ENTRAMBE, versione 8 invariata**) ha chiuso a causa radice **debt-free** i 5 finding renderer-UI + il flag cross-fase, scope `app/desktop/src/renderer/src/{views,components,styles}` + `App.vue` + (prereq tipi) `env.d.ts`: **M‑09** (3 file `#d98b6b`→`var(--bad)` + guard test `no-color-drift.test.ts` che scandisce i `.vue` via `node:fs`), **I‑07‑UI** (`GmConsole`: barriera input dadi ApplyEffect — `min`/`step` su count/sides + helper `intAtLeast(value,min)` [trunc+isFinite+max, NaN/Infinity-safe] che clampa count≥1/sides≥2 in `submitApplyEffect`; arbitro autorevole resta nel motore F1/AI F3; bonus intatto), **bottone "Round successivo" RIMOSSO** da `EncounterPanel` (+ import `nextRound` + il suo test; il factory `nextRound()` resta in `lib/combat-commands.ts`, Command valido ancora testato — fuori scope F6), **M‑15** (`App.vue`: `isDev = import.meta.env.DEV` + `<GmConsole v-if="isDev"/>` → Regia nascosta in produzione; test `vi.stubEnv('DEV',false)`), **M‑10** (`LoomnDialog` adottato per Regia [drawer] e creator CompanyView [center], rimosso lo scrim/creator hand-rolled; **reso INLINE — niente DialogPortal/Teleport — per rispettare la decisione 10f "GmConsole no-Teleport per testabilita"**: Reka `DialogContent` richiede solo un `DialogRoot` antenato e da Escape+focus-trap a prescindere dal portal, content `position:fixed` non clippato; controllato `v-model:open` via `rootProps` computed che omette `open` se undefined [exactOptionalPropertyTypes-safe] preservando controllato/non-controllato; i test esistenti GmConsole/CompanyView restano verdi senza modifiche), **I‑03** (`App.vue`: `grid-template-rows: minmax(0,1fr)` su `.app-shell` + `min-height:0` su `.stage` + wrapper `.stage__view {flex:1; min-height:0; display:flex; overflow:auto}` attorno a `<RouterView/>`; `base.css body{overflow:hidden}` INTENZIONALE, lasciato — il fallback di scroll va a livello route non documento). **+ fix tipi import.meta** (`env.d.ts` dichiara `ImportMetaEnv`/`ImportMeta`: **vite NON e dep diretta di app/desktop sotto pnpm → `/// <reference types="vite/client" />` non risolve mai → `import.meta.env`/`glob` erano UNTYPED sotto vue-tsc** [gap latente, mai esercitato]; toccare package.json/tsconfig era vietato dallo scope → dichiarazione type-only in-renderer = path debt-free; il guard M‑09 usa `node:fs` non `import.meta.glob`). Subagent-driven 6 task TDD (implementer + spec/code-quality review; code-quality opus dedicata sul task crux M‑10) + **final review opus** = "Ready to merge: Yes" (0 Critical/0 Important; ri-verificato scope, ogni finding, la deviazione env.d.ts, 774 test + 6 typecheck). **VERIFICA VISIVA I‑03 (preview tools, NON sessione utente):** build renderer + server statico `node` in `.claude/preview/` servito in browser headless (config `.claude/launch.json` `loomn-renderer-preview`:5199); **A/B in-browser decisivo** su `/impostazioni` (3000px iniettati): pre-fix simulato → 2658px clippati silenziosamente da `body{overflow:hidden}`; post-fix → scroller interno bounded (clientH 653 / scrollH 3325) che scrolla (scrollTop=1000 ok); **tutte e 5 le route + il Gioco** scrollano (docClip 0); **grid-layout-plus misura corretto AL MOUNT** (vgl-layout 542px, colonne 758/372 non collassate) → **re-measure condizionale NON necessario (YAGNI, skippato)**; bonus: topbar della build di produzione SENZA bottone Regia → M‑15 confermato in prod. **PROSSIMO: scrivi il piano della FASE 7 (ops/gate & CI: M‑11 script `gate:selftest`+`verify`+kill-ghost-process, D‑06 hazard ABI documentato, + estensione del self-test sul path RELOAD di I‑02 — `renderer.ts`; ⚠️ F7 TOCCA la config [`package.json` scripts, eventuale `.github/workflows`] — l unica fase in cui e consentito) just-in-time con `superpowers:writing-plans`, poi subagent-driven (flusso §4).** **DOPO F7 la REMEDIATION e COMPLETA (28/28 finding a causa radice)** → restano le tracce design-first D‑01 (incipit) / D‑02 (redesign UI) / D‑03 (multi-campagna) e le feature deferite (Inventario & Equipaggiamento, movimento/zona) — tutte da aprire con `superpowers:brainstorming`. **Flag residui annotati (NON in F6):** `rulesetResultSchema.vocabulary.defaultResources` `z.number()` nudo (shared, fuori scope); `journal` store sovrascrive il 1o errore se entrambi i canali falliscono (store, fuori scope); `provider-status.error` non surfacciato; factory `nextRound` ora usato solo dal suo test (lib, fuori scope). **(Scaffolding preview in `.claude/` = untracked, NON committato.)**
>
> ⏩ **FIX FUORI-CAMPAGNA (2026-06-22f) — bug dadi 3D: il canvas non si ridimensionava col pannello.** Mergiato (`75169bb`, **777 test** = 565 packages + 212 renderer; branch `fix/dice-canvas-resize` eliminato, pushato). **NON e uno dei 28 finding** (segnalato dal vivo dall utente): `@3d-dice/dice-box-threejs` dimensiona il renderer al container all init e ri-misura **SOLO su `window 'resize'`** → un resize del pannello (grid-layout-plus) non tocca la finestra → il canvas restava alla dimensione iniziale e veniva tagliato. Fix in `DiceCanvas.vue`: `ResizeObserver` sul container → `box.setDimensions({x,y})` (la **stessa** op dell handler `window.resize` interno della libreria; dimensioni PIENE, `setDimensions` legge solo `.x/.y`), coalizzato in `requestAnimationFrame`, guardia 0×0 + cleanup `disconnect`/`cancelAnimationFrame`, degrada se `ResizeObserver` assente (jsdom). `+setDimensions` allo shim `dice-box-threejs.d.ts`; nuovo `DiceCanvas.test.ts` (3 test, mock DiceBox+ResizeObserver+rAF). TDD (red→green) + root-cause sul sorgente libreria; la verifica visiva 3D NON e fattibile headless (WebGL instabile + il box e lazy su un roll che richiede `window.loomn` → serve l app reale). **La remediation (F1-F6 fatte, F7 pendente) e INVARIATA: questo fix e indipendente.** ⚠️ **La baseline test per il piano F7 e ora 777 (non 774); ultimo commit di CODICE = `75169bb`.** **Lezione ABI (D-06/M-11, vissuta qui):** dopo il gate F6 l ABI nativa era su Electron (146) → `pnpm test` falliva con `NODE_MODULE_VERSION 146 requires 137` su 110 test SQLite → `pnpm rebuild:node` (un primo tentativo e fallito "install: Failed" per un lock transitorio, ripetuto = "Done") → 777 verdi. Proprio cio che F7/M-11 deve automatizzare.
>
> ⏩ **CONTESTO (2026-06-22d) — FASE 5 (renderer-logica) ✅ FATTA E MERGIATA (ora il prossimo e la FASE 7, vedi il bullet sopra).** F5 (`79ffcff`, **769 test** = 565 packages + 204 renderer, branch `fix/remediation-f5-renderer-logica` eliminato, pushato a `origin/main`; piano `plans/2026-06-22-loomn-fix-remediation-f5-renderer-logica.md`; **ABI jsdom, NESSUN gate Electron**) ha chiuso a causa radice **debt-free** i 4 finding renderer-logica + il flag F4, scope SOLO `app/desktop/src/renderer/src/{composables,stores,components,views}` + il nuovo `PanelError.vue`: **I‑09** (`composables/use-dispatch.ts`: la `dispatch` avvolge `window.loomn.dispatch` in try/catch → ritorna `{ok:false,error}` sul reject IPC [handler che lancia / structured-clone fallita "An object could not be cloned"] invece di un unhandled rejection; i caller `GmConsole.send`/`EncounterPanel.send` leggono gia `res.ok`/`res.error` e restano **INTATTI** → garanzia "mai fallire in silenzio" single-source nel composable, come il gemello `CompanyView.submit`), **M‑07** (stesso file: `dice.clear()` **prima** dell `enqueue`, allineato a `useRunTurn` → un comando GM senza tiri non lascia un readout dadi stantio), **M‑08** (`components/NarrativePanel.vue`: `onSend` ripristina il `draft` se il turno fallisce — `if (narration.error !== null) draft.value = action` dopo l await, svuota solo su esito ok; robusto perche `useRunTurn.submit` fa `setError(null)` a inizio → `error!=null` dopo l await = fallimento di QUESTO turno), **I‑08** (Decisione 5: nuovo `components/PanelError.vue` `<p role="alert">` presentazionale [`color:var(--bad)`, allineato a `NarrativePanel.narr__error`, `v-if=error`] riusato per surfacciare `journal.error` in `JournalView`/`CompanyView` e `ruleset.error` in `GmConsole`/`EncounterPanel`/`SheetPanel`/`CompanyView` — errori dei canali read prima raccolti negli store ma **mai mostrati** → pannelli vuoti indistinguibili da "ok ma vuoto"/Regia silenziosamente inoperabile), e il **flag F4 CHIUSO** (guardia di monotonia su `applyPush` in `stores/read-model.ts`: `if (state.value !== null && push.version < version.value) return;` → un pull stantio dal pull-on-mount di I‑02 non sovrascrive un push piu fresco; strict `<` = equal-version idempotente, prima push sempre applicata). Subagent-driven 5 task TDD (implementer + spec-review + code-quality-review per task; code-quality saltata sui 2 task banali [PanelError scaffold, guardia 1-riga] e dichiarata) + **final review opus** = "Ready to merge: Yes" (0 Critical/0 Important; verifica avversariale: guardia `applyPush` sana su ogni edge case [first push, equal, stale pull], M‑08 senza restore spurio, **self-test NON perturbato** [usa il suo listener `onReadModelPush` indipendente → la guardia non tocca le sue asserzioni di versione; versione attesa **8 invariata**, F5 non tocca engine/IPC]). `#d98b6b` (M‑09) e tutti gli item UI **INTATTI** (sono F6). **PROSSIMO (SUPERATO — la FASE 6 e ORA FATTA E MERGIATA `d6ba863`, vedi il bullet 2026-06-22e in cima; all epoca era): scrivi il piano della FASE 6 (renderer-UI: I‑03 scroll/overflow [⚠️ riprodurre in-browser con i preview tools, NON sulla sessione live dell utente; sospetto grid-layout-plus measure-timing], I‑07‑UI input dadi `min/step`+clamp, M‑09 `#d98b6b`→`var(--bad)`, M‑10 adottare `LoomnDialog` per Regia/creator, M‑15 dev-gate Regia `import.meta.env.DEV`, + bottone 'Round successivo' ridondante in `EncounterPanel`) just-in-time con `superpowers:writing-plans`, poi subagent-driven (flusso §4); **⚠️ F6 richiede il gate Electron 2 fasi + verifica visiva**; poi F7.** **Flag residui (da valutare in F6/follow-up, NON in F5):** `rulesetResultSchema.vocabulary.defaultResources` `z.number()` nudo (consistenza minore); il `journal` store sovrascrive il 1o errore se **entrambi** i canali (`getSummaries`+`getCanon`) falliscono insieme (pre-esistente, fuori dal finding I‑08 → eventuale follow-up store, es. array di errori); `provider-status.error` non surfacciato (non nel finding I‑08, che nomina `journal.ts`/`ruleset.ts`).
>
> ⏩ **CONTESTO (2026-06-22c) — FASE 4 (IPC/main) ✅ FATTA E MERGIATA (ora il prossimo e la FASE 7, vedi il bullet sopra).** F4 (`cce7432`, **757 test**, branch `fix/remediation-f4-ipc-main` eliminato, pushato a `origin/main`; piano `plans/2026-06-22-loomn-fix-remediation-f4-ipc-main.md`; **gate Electron 2 fasi `VERDICT: PASS` su ENTRAMBE le fasi**) ha chiuso a causa radice **debt-free** i 3 finding IPC/main + i flag F4: **I‑02** (read-side self-healing — canale pull `get-read-model` [riusa `readModelPushSchema`, handler **sincrono fuori dalla coda FIFO** come `getCanon`/`getSummaries`; helper `buildReadModelPush` estratto e condiviso con `pushReadModel`, DRY] + `.once`→`.on` su `did-finish-load` + **pull-on-mount** nel bootstrap renderer → Ctrl+R non perde piu i dati a schermo), **I‑11** (single-instance lock per-userData [false→`app.quit()`, `second-instance`→restore+focus] + `app.setPath('userData')` **PRIMA** del lock e di `whenReady` + try/catch attorno a `createMemorySystem`/`createCampaignService`→`dialog.showErrorBox`+`app.exit(1)` + `process.on('unhandledRejection')` **solo-log** [non killa il gate]), **M‑06** (`StatusResult`→`z.union([{ok:true,…},{ok:false,error}])` come gli altri canali + `getStatus` try/catch + store `provider-status` getter guardati su `.ok` con **superficie pubblica preservata** [le viste `.vue` NON cambiano: leggono i getter dello store] + `ProviderMeta = NonNullable<Extract<StatusResult,{ok:true}>['provider']>` [read-drift guard preservato] + 5 stub di test renderer adeguati), e il **flag `.finite()`** sui 3 read-DTO (`canonFactSchema.salience`, `summarySchema.importance`+`salience` — type-contract, debt-free). Subagent-driven 4 task TDD (implementer + spec-review + code-quality-review per task) + **final review opus che ha COLTO UN CRITICAL eseguendo il GATE VERO** (la suite unit `pnpm test` NON esercita il self-test, solo il gate Electron): **il flag self-test "7→8" era REALE, NON "resta 7" — fu un ERRORE del piano F4.** I‑01/F1 fa auto-emettere `RoundAdvanced` su `EndTurn` con **1 solo partecipante** (il round si chiude → coppia atomica `[TurnEnded, RoundAdvanced]`, [commands.ts:143-146](packages/engine/src/commands.ts:143)) → **+1 evento** nel ladder di fase 1; F1/F2/F3 sono ABI-Node-only e NON eseguono il gate → la versione attesa `7` e rimasta **stantia da F1** finche F4 (primo gate) non l ha colta → corretta a **8** (3 asserzioni + label/commenti in `renderer.ts`). **(STORICO — FASE 5 ORA FATTA, vedi il PRIMO bullet del §0; all epoca il prossimo era:) scrivi il piano della FASE 5 (renderer-logica: I‑09 `useDispatch` ingoia i reject IPC [→ ritorna `{ok:false,error}`], I‑08 errori dei canali read mai mostrati [`PanelError` condiviso], M‑07 `useDispatch` non azzera i dadi prima dell enqueue, M‑08 azione persa su errore in `NarrativePanel`) just-in-time con `superpowers:writing-plans`, poi subagent-driven (flusso §4); ABI jsdom, **NESSUN gate Electron** (renderer-logica jsdom); poi F7.** **Flag cross-fase residui:** bottone 'Round successivo' ridondante renderer → F6 [CHIUSO in F6, `041669c`]. **Nuovi flag annotati da F4 (NON implementati):** guardia di monotonia su `applyPush` (`stores/read-model.ts`, anti-clobber pull/push) → **CHIUSA in F5**; `rulesetResultSchema.vocabulary.defaultResources` `z.number()` nudo (consistenza minore; `.finite()` sicuro ma fuori dal set nominato del flag). **⚠️ Caveat ABI (visto al gate F4):** un `electron-vite dev` FANTASMA di una sessione precedente teneva il lock su `better_sqlite3.node` (EPERM su `rebuild:node`) **e** aveva lasciato l ABI su Electron → killare SOLO i processi Loomn (`Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'tabl|loomn' -and $_.Name -match 'electron|node' }` + `Stop-Process -Force`; NON tutti gli electron), poi `pnpm rebuild:node`.
>
> ⏩ **CONTESTO (2026-06-22b) — FASE 3 (AI), gia fatta (ora il prossimo e la FASE 7, vedi il bullet sopra).** F3 (`a413ab8`, **751 test**, branch `fix/remediation-f3-ai` eliminato, pushato a origin/main; piano `plans/2026-06-22-loomn-fix-remediation-f3-ai.md`) ha chiuso a causa radice **debt-free**, scope `packages/ai`, i 3 finding AI + 1 flag cross-fase: **I‑07‑tool** (barriera dadi lato AI — `llmInt(min, max?)` con `.max()` opzionale; `dieGroupArgSchema` usa `MAX_DICE_COUNT`/`MAX_DICE_SIDES` **importati dal motore** [single-source con `assertDieGroup`] → un `count`/`sides` allucinato e ARGOMENTI NON VALIDI reiniettati, non un freeze; l arbitro autorevole resta `assertDieGroup` in F1; il bound vive SOLO sullo schema d argomento tool, mai su un read-path), **M‑04** (registro tool memoizzato per identita del `Vocabulary` via `WeakMap`, behaviour-preserving: stesso vocab→stesso registro, osservato via la referenza di `jsonSchema`; nessun export nuovo dal modulo), **I‑04** (turno muto: dopo il loop, se `narration.trim()` e vuota → `TraceEvent` `kind:'error'` diagnostico [riusa l arm esistente, `tracing.ts` INTATTO] + narrazione di **fallback DETERMINISTICA non-vuota**; il giocatore non resta nel vuoto, la diagnosi non sparisce), e **flag F1 `next_round` RIMOSSO** (tool AI ridondante post-I‑01: il motore auto-avanza il round, `decide(NextRound)` esige `roundComplete` che la FSM normale non raggiunge mai → tool che puo solo fallire; `NextRound` Command/`commandSchema`/drift-guard/bottone GM **INTATTI**). Subagent-driven 4 task TDD (implementer + spec-review + code-quality-review per task) + final review opus = "Ready to merge: Yes" (0 Critical/Important; 7 vincoli forti verificati: scope, debt-free/no-read-path, next_round AI-only, I-04 additivo, M-04 mutation-safe della cache, nessun export nuovo, house rules). **Conseguenza cross-package gestita (utente APPROVA via AskUserQuestion):** I‑04 rende `narration` sempre non-vuota → `campaign-service.runTurn` ora persiste un `NarrationRecorded` di fallback (la guardia `narration.length>0` ora passa) → **2 test host riconciliati** (`packages/host/src/campaign-service.test.ts`; **produzione `campaign-service.ts` INTATTA**, era gia corretta; F3 tocca `packages/ai` + 1 file di test host). **Nessun nuovo flag cross-fase da F3.** **PROSSIMO: scrivi il piano dettagliato della FASE 4 (IPC/main — I‑02 Ctrl+R perde i dati a schermo [`main/index.ts:236` `.once`+nessun pull, read-side self-healing], I‑11 startup robusto + single-instance lock + dialog su errore, M‑06 error-handling coerente al confine IPC; + i flag F4: self-test versione 7→8, `ipc.ts` `canonFactSchema.salience`/`summarySchema.importance`+`salience` senza `.finite()` sui read-DTO) just-in-time con `superpowers:writing-plans`, poi subagent-driven (flusso §4); **⚠️ da F4 entra il gate Electron 2 fasi** [build → `pnpm rebuild:electron` → 2 self-test → `pnpm rebuild:node`]; poi F5→F7.** **⚠️ Caveat ABI ricorrente (visto in F3 al merge):** `pnpm test` full puo fallire con `NODE_MODULE_VERSION 146 ... requires 137` se la nativa e rimasta su ABI Electron → `pnpm rebuild:node` (in F3 ha risolto da solo, prebuild-install; se EBUSY chiudi i processi Loomn).
>
> ⏩ **CONTESTO (FASE 2 — MEMORIA/HOST, gia fatta).** F2 (`fe14646`, **742 test**, branch `fix/remediation-f2-memoria-host` eliminato, pushato a origin/main; piano `plans/2026-06-19-loomn-fix-remediation-f2-memoria-host.md`) ha chiuso a causa radice **debt-free** gli 8 finding del layer persistenza/memoria: **I‑05** (`loadSince`/`loadNarration` finestrati DB-side sull adapter SQLite → niente full-scan+Zod per pagina; `getNarrationHistory`/`reflect` li usano), **M‑05** (clamp limit≥1 con `Math.max(1, Math.trunc(...) || 1)`, +NaN), **M‑03** (snapshot CABLATO: avvio da `latestSnapshot()`+`loadSince(snap.version)` tail invece del full-replay, save a soglia `snapshotEvery` default 100 in `dispatch`/`runTurn`; firma retro-compatibile, app/desktop intatto), **M‑13** (confine transazionale per-scena nella Reflection: `runReflection` split in `computeScene` async [LLM, FUORI tx] / `writeScene` sync, `MemorySystem.runInTransaction`=`db.transaction` su connessione condivisa, {fatti+riassunto+cursor} atomici → crash rolla-back tutto [niente UNIQUE-deadlock], **determinismo-su-retry preservato** [la ricorrenza si ricalcola pulita]; `supersede`=savepoint annidato testato), **I‑10** (drift guard `Command`↔`commandSchema` bidirezionale esaustivo compile-time + probe `@ts-expect-error` in `command-schema.test`), **M‑01** (`.strict()` sui 13 arm di `domainEventSchema` → drift evento RUMOROSO; **debt-free-safe**: il motore e l unico writer e non emette mai chiavi extra → `.strict()` non rifiuta MAI un dato storico; il `roll` annidato resta non-strict di proposito; l alternativa Equals strutturale e stata SCARTATA empiricamente = falso positivo gia oggi), **M‑12** (tie-break canon `.orderBy(eventSeq, id)`), **M‑14** (`getRuleset` verifica la legalita su TUTTE le `SOFT_PHASES` con throw su divergenza, non il proxy hardcoded `'exploration'`). Subagent-driven 7 task TDD (implementer + spec-review + code-quality-review per task) + final review opus che ha applicato lo scrutinio F1-level su M‑01 e **REFUTATO il rischio brick read-path** tracciando ogni sito di costruzione evento. Scope = `memory`+`host` + 1 hardening additivo a `shared`; NESSUN gate Electron (entra da F4). **Flag cross-fase: NESSUNO nuovo da F2; restano aperti quelli di F1** — self-test versione 7→8 (F4), tool `next_round` ridondante (F3), bottone 'Round successivo' ridondante (F6), `ipc.ts` `salience`/`importance` senza `.finite()` sui read-DTO host→renderer (F4). **[⚠️ SUPERATO — la FASE 3 e ORA FATTA E MERGIATA (`a413ab8`, 751 test); vedi il bullet "AZIONE CORRENTE (2026-06-22b)" in cima a §0: la FASE 4 (IPC/main) e ORA FATTA, il prossimo e la FASE 7 (renderer-logica).]** Vincolo debt-free riconfermato da F2: per le query paginate cambia il PIANO di lettura, non lo SCHEMA; `.strict()` e ok sul read-path SOLO perche nessun dato storico ha chiavi extra.
>
> ⏩ **CONTESTO (FASE 1 — MOTORE, gia fatta).** F1 (`582fb7a`, **719 test**, branch `fix/remediation-f1-motore` eliminato, pushato a origin/main) ha chiuso a causa radice **I‑01** (motore possiede la FSM round/turno: `decide(EndTurn)` auto-avanza il round + rifiuta EndTurn/NextRound illegali, riusa `endTurn`/`roundComplete`), **I‑06** (`clampPool`: invariante current∈[0,max] anche alla CREAZIONE in `decide(AddActor)` + guard NaN/Infinity), **I‑07** (`assertDieGroup` arbitro nel motore: interi, count 1..100, sides 2..1000 — blocca il freeze del main da `count=1e8`), **I‑12** (single-source `DOWNED_CONDITION_KEY`/`dyingCondition`, rimosso il blocco morente morto in `combat.ts`), **I‑13** (`finiteNumber` rifiuta Infinity al confine), **M‑02** (`StartEncounter` rifiuta participants vuoto). Subagent-driven 6 task TDD (implementer + spec-review + code-quality-review per task) + final review opus, che ha **colto un BUG LATENTE del piano**: i bound dadi `.int().min().max()` su `dieGroupSchema` finivano sul READ-path (`itemEffectSchema`→`actorSchema`→`domainEventSchema`/`gameStateSchema`, ri-parsati a OGNI replay/load in `sqlite-event-store`) → avrebbe brickato campagne con item dai dadi fuori-bound → fix debt-free = split `dieGroupSchema` (lettura permissiva, solo `finiteNumber`) vs `dieGroupCommandSchema` (bound solo su `ApplyEffect`); l'arbitro resta `assertDieGroup` nel motore al tiro. *(All'epoca di F1 il prossimo passo era la FASE 2; F2 e F3 sono ORA FATTE E MERGIATE — vedi il bullet "AZIONE CORRENTE (2026-06-22b)" in cima a §0: la FASE 4 e ORA FATTA, il prossimo e la FASE 7.)* **Flag cross-fase aperti da F1:** self-test versione 7→8 (F4), tool `next_round` ridondante per l'auto-advance (F3), bottone 'Round successivo' ridondante (F6), **+ NUOVO: `ipc.ts` `canonFactSchema.salience`/`summarySchema.importance`+`salience` senza `.finite()` (read-DTO host→renderer, basso rischio, F4)**. **Vincolo debt-free riconfermato:** `.finite()` ok ovunque, ma i bound (`.int()/.min()/.max()`) vanno SOLO sullo schema di comando, mai dove un read-path li porterebbe. *(Quanto segue è il contesto della FASE 1 — ora fatta — e dell'audit.)* L audit bug+design su TUTTO il codebase e CONCLUSO: report `docs/superpowers/audits/2026-06-19-loomn-audit-findings.md` (28 finding-class deduplicate: **0 Critical / 13 Important / 15 Minor**; 35 findings sopravvissuti alla verifica avversariale, 2 refutati [incl. il falso-negativo sullo scroll I-03, sovrascritto dall evidenza dal vivo dell utente], 143 probed-and-rejected; Workflow multi-agente 90 agenti). I **3 bug osservati dal vivo** dall utente sono confermati: Ctrl+R perde i dati a schermo = **I-02** (`main/index.ts:236` `.once` + nessun pull); nessuno scroll/overflow in tutta l app = **I-03**; round di combattimento = **I-01**. **MANDATO DECISO con l utente: fixare TUTTI i 28 finding a CAUSA RADICE, senza debiti, in un piano a FASI.** Piano-campagna (7 fasi per layer): `docs/superpowers/plans/2026-06-19-loomn-remediation-campaign.md` (F1 motore · F2 memoria/host · F3 AI · F4 IPC/main · F5 renderer-logica · F6 renderer-UI · F7 ops/gate). **DECISIONI BLOCCATE (via AskUserQuestion):** I-01 = il motore possiede la FSM round/turno (auto-avanza + rifiuta illegali); Regia M-15 = **dev-gate** (nascosta in prod); snapshot M-03 = **cablare** lo snapshotting; ambito = **solo i 28 bug ora**, le tracce grandi (D-01 incipit, D-02 redesign UI, D-03 multi-campagna) restano DOPO come iniziative design-first. **PIANO DETTAGLIATO DELLA FASE 1 (motore: I-01/I-06/I-07/I-12/I-13/M-02) ESEGUITO e mergiato** (`582fb7a`, `docs/superpowers/plans/2026-06-19-loomn-fix-remediation-f1-motore.md`); le fasi **F2→F7** seguono con lo STESSO flusso §4 (writing-plans del piano di fase → branch → implementer+2review per task → final review → finishing/merge ff → push → aggiorna HANDOFF/memoria), una fase alla volta. **FLAG CROSS-FASE da F1** (annotati nel piano F1, NON in F1): self-test versione attesa **7→8** in **F4**; tool `next_round` ridondante per l auto-advance → **F3**; bottone 'Round successivo' ridondante → **F6**. **Vincolo debt-free importante:** le restrizioni schema NON vanno sul percorso di LETTURA (rifiuterebbero dati storici); clamp solo nel motore, `.min(1)`/bound-dadi solo sullo schema di comando, `.finite()` ovunque (sicuro). Vedi §9 punto 3. *Quanto segue (audit playbook, Piano 10) è ora CONTESTO STORICO.*

> ⏩ **CONTESTO STORICO — la sessione di AUDIT (decisa 2026-06-19), ora FATTA (vedi il bullet sopra).** Il Piano 10 (UI) e completo; prima di aprire le feature deferite l utente vuole una sessione dedicata di audit/correzione. **Il playbook e gia scritto: `docs/superpowers/plans/2026-06-19-loomn-audit-bug-design-review.md`** (deciso via AskUserQuestion: ambito = tutto il codebase engine+shared+memory+ai+host+app/desktop; focus = bug di correttezza + design/architettura; profondita = MISTA [scout inline → fan-out multi-agente mirato con verifica avversariale → sintesi]; output = **report-first**, l utente tria, poi fix TDD nella sessione ANCORA successiva — **NESSUNA modifica al codice di prodotto nella sessione di audit, solo il file di report**). **L utente ha approvato l orchestrazione multi-agente** (opt-in valido per il tool Workflow). Il prossimo agente esegue il playbook: Fase A scout → Fase B Workflow → Fase C report in `docs/superpowers/audits/`. Le feature deferite (Inventario, movimento/zona) o il Piano 11 restano DOPO l audit+fix. *Stato del Piano 10 sotto.*

> ⏩ **AGGIORNAMENTO 2026-06-19 — PIANO 10e (DIARIO + COMPAGNIA: MEMORIA NARRATIVA L2 + CANON + ROSTER DISPLAY-ONLY) FATTO E MERGIATO → PIANO 10 (UI) COMPLETO** (`0e91cc2`, **691 test** = 500 packages + 191 renderer; typecheck pulito; gate Electron 2 fasi `VERDICT: PASS` [fase 2 = versione 7 persistita, Diario/Compagnia sono read-only; `reflect` NON invocato nel self-test perche LLM-backed]; pushato a `origin/main` `893f53b..0e91cc2`; vedi **§0-tervicies**). Renderer-only: `lib/journal-view.ts` (canon ordinato per salienza, riassunti L2 raggruppati per livello, `reflectMessage`) + `stores/journal.ts` (read-side L2+canon+trigger `reflect`, riusato da Diario e Compagnia) + `views/JournalView.vue` (compone cronologia [RIUSA lo store `narration` di 10b] + riassunti + canon + affordance "Rifletti") + `lib/company-view.ts` (`toCompanyCard` + `canonForActor`) + `views/CompanyView.vue` arricchita (roster PG/PNG in carte + relazioni canon display-only, **creator PG di 10f INTATTO**). Relazioni STRUTTURATE deferite (spec §11) → solo fatti canon. App.test/CompanyView.test stubbano `getSummaries`/`getCanon` (lezione stub-mancante: le route reali chiamano `journal.load()` onMounted). Self-test esteso (naviga `/diario`+`/compagnia`). NESSUN nuovo Command/Event/IPC/dipendenza/CSP/config; `tokens.css`/`GameView`/`router` intatti. Final review opus "Ready to merge: Yes" (0 Critical/0 Important). **Prossimo = feature DEFERITE post-Piano-10 (Inventario & Equipaggiamento, movimento/zona) DESIGN-FIRST, oppure Piano 11.** *Contesto storico sotto.*

> ⏩ **AGGIORNAMENTO 2026-06-18 — PIANO 10d (SCHEDA + INVENTARIO DISPLAY-ONLY, DATA-DRIVEN DAL RULESET) FATTO E MERGIATO** (`83cafea`, **664 test** = 500 packages + 164 renderer; typecheck pulito; gate Electron 2 fasi `VERDICT: PASS` [fase 2 = versione 7 persistita, la Scheda e read-only e non aggiunge eventi]; pushato a `origin/main` `1522ec1..83cafea`; vedi **§0-duovicies**). Renderer-only: `lib/sheet-view.ts` mappa PURA attore+vocabolario → vista scheda (attributi/abilita ordinati dal vocabolario, barre risorse con %, condizioni/effetti-oggetto formattati in stringhe) + `resolveSelectedActor` (default primo PG); `components/SheetPanel.vue` (selettore attore coi NOMI + identita/attributi/abilita/risorse/condizioni/inventario DISPLAY-ONLY) RIUSATO sia nel pannello `sheet` del Gioco sia nella route `/scheda` (`SheetView.vue` lo avvolge); wiring `GameView` (monta SheetPanel nel `sheet`, stub in `GameView.test` per evitare unhandled-rejection da `getRuleset`) + self-test esteso (naviga a `/scheda`, monta SheetPanel via route reale). Inventario sul modello piatto `Item{id,name,equipped,effects}` — slot profondi/contenitori/equip-come-azione = motore Inventario DEFERITO post-10. NESSUN nuovo Command/Event/IPC/dipendenza/CSP/config; `tokens.css` intatto. Final review opus "Ready to merge: Yes" (0 Critical/0 Important). **Prossimo = Piano 10e (Diario + Compagnia).** *Contesto storico sotto.*

> ⏩ **AGGIORNAMENTO 2026-06-18 — PIANO 10c (COMBATTIMENTO: COCKPIT SCONTRO + ZONE DISPLAY-ONLY) FATTO E MERGIATO** (`f0e931c`, **639 test** = 500 packages + 139 renderer; gate Electron 2 fasi `VERDICT: PASS`; pushato a `origin/main`; vedi **§0-unvicies**). Cockpit nel pannello scontro del preset combat (iniziativa/round/turni dal read-model PRE-ORDINATO, zone DISPLAY-ONLY da `participants[].zone`), controlli combat via il seam `use-dispatch` (i `RollResult` animano i dadi 3D di 10b); Regia: swap EndEncounter→StartEncounter. Movimento/topologia di zona DEFERITI post-Piano-10 (§8/§11). **Prossimo = Piano 10d (Scheda + inventario).** *Contesto storico sotto.* **Piano 0 ✅** (`614a6bb`). **10a ✅** (`071e71c`). **10g ✅** (`fa6bc17`). **10f ✅** (`bf532f4`). **Piano 10b ✅ FATTO e mergiato** (`1138a97`, **609 test**; + follow-up seam dispatch→dadi `6423011` → **612 test** = 497 packages + 115 renderer; gate Electron 2 fasi `VERDICT: PASS`; vedi **§0-vicies**). **DECISIONE TECNICA dallo spike (con l utente): libreria dadi = `@3d-dice/dice-box-threejs` NON `@3d-dice/dice-box`** — lo spike ha invalidato la scelta dello spec (Babylon non si inizializza sotto la CSP attuale [Worker `blob:` bloccato] e non sa forzare le facce); il fork threejs forza le facce con notazione `NdS@v1,v2,...` sotto la CSP di **produzione esatta, ZERO rilassamenti** (no worker/wasm/eval), texture vendorate offline; sides non-standard → token numerico (spec §6 fallback). 10b: mappa pura `RollResult→notazione`, store storia (cursor-by-seq) + coda dadi, orchestratore turno (`run-turn`, gate `canRunTurn`), NarrativePanel (log + input gated, "il Master sta scrivendo" — streaming deferito), DicePanel/DiceCanvas (3D lazy a facce forzate + readout autorevole del motore). Remote `origin` = `https://github.com/devfrx/loomn.git` (pushato a `1fadda2`). **FIX POST-10b (`1fadda2`, 616 test = 500 packages + 116 renderer):** (1) `normalizeBaseUrl` in `createLanguageProvider` (`934e20f`) — un base-URL senza `/v1` (es. `http://localhost:1234`) colpiva `/chat/completions` → LM Studio "Unexpected endpoint"; ora normalizzato a `/v1` (la config gia salvata funziona al riavvio, niente re-save); (2) **creazione PG falliva in SILENZIO** (`b35dce3`, vera causa) — `buildActor` passava i proxy `reactive` di Vue (`form.attributes/skills/resources`) al dispatch IPC → Electron non puo clonare un Proxy ("An object could not be cloned"), il dispatch lanciava e `submit` (senza catch) ingoiava l errore; ora `buildActor` copia in oggetti PLAIN e `submit` mostra l errore invece di ingoiarlo. **LEZIONE per 10c/10d: ogni payload verso `dispatch`/IPC deve essere PLAIN, mai un proxy `reactive` annidato** (spread/copia, non passare `reactive(...)` direttamente). Il **single-instance lock** (`812bfe2`) e stato RIMOSSO (revert `1fadda2`): era una pezza su un ipotesi sbagliata del crash PG (la causa vera era la clone IPC) e aggiungeva attrito in dev — eventualmente da ri-aggiungere di proposito in un passo di packaging, non come effetto di un debug. **Prossima azione: scrivere il Piano 10c — Combattimento** (cockpit scontro iniziativa/round/turni; **zone display-only** — niente mappa/movimento, deferiti post-10 §8; feedback attacco/check/effetto dai `RollResult` negli events di dispatch — il seam dispatch→dadi e gia cablato (use-dispatch); re-theme combat). Ordine sotto-piani `10a✅ → 10g✅ → 10f✅ → 10b✅ → 10c → 10d → 10e`. Decisioni/decomposizione in **§0-vicies** (10b) e spec autorita `specs/2026-06-17-piano10-ui-design.md` (§10 riga 10c, §11 zone deferite).

L'engine (Piani 1-5), la **Persistenza (6)**, il **Provider Layer AI (7a)**, lo **StructuredOutputPort (7b)**, l'**AI Master pipeline (7c)**, il **Canon Ledger L1.5 (8a)**, **Reflection + L2 + Salienza (8b)**, il **Context Assembler (8c)**, la **Shell Electron (9a)** e il **Wiring memoria+AI (9b)** sono **finiti e mergiati** in `main`, e cosi tutto il **Piano 9** (9a + 9b + 9c-i + 9c-ii). I Piani 7, **8** e **9** (tutti i sotto-piani) sono completi. **Piano 9 splittato in 9a/9b/9c** (vedi §7-bis/§7-ter e i piani `...piano9a-...` / `...piano9b-...`): **9a fatto** = pacchetto `app/desktop` (electron-vite + Vue), sicurezza (contextIsolation/sandbox/nodeIntegration:false), contratto IPC tipizzato in `@loomn/shared`, giro IPC main↔renderer + push read-side, **senza DB/AI**. **9b fatto** = pacchetto **`@loomn/host`** (`packages/host`, deps engine/memory/ai + zod) con `createSqliteEventStoreOn(db)` (event store su handle Drizzle condiviso, aggiunto a `@loomn/memory`), `createMemorySystem(dbPath)` (UNA connessione: EventStore+CanonLedger+SummaryStore+ContextAssembler), `systemClock` (porta **`Clock`** reale), impl **LLM-backed** di `FactExtractor`/`Summarizer` su `StructuredOutputPort` di 7b (`ai`+`memory` si compongono **solo qui**), assembler reale **iniettabile** in `runMasterTurn` (dettagli in §7-ter). Tutto unit-testato a porte iniettate su **ABI Node** (niente DB nativo/IPC/rete). **Piano 9 splittato in 9a/9b/9c, e il 9c a sua volta in 9c-i/9c-ii** (decisione presa con l'utente: i due hanno modi di verifica diversi — TDD su ABI Node vs esecuzione dell'app — e lo split confina il pericoloso flip ABI al solo 9c-ii). **9c-i fatto** (vedi §7-quater e il piano `...piano9ci-...`) = **application layer** in `@loomn/host` (`createCampaignService` write side+proiezione+turno+reflection serializzati FIFO; `createLanguageProvider` model+structured con transport iniettabile) + `commandSchema` cast-free in `@loomn/shared`; tutto ABI Node, 258 test. **9c-ii fatto** (vedi §7-quinquies e il piano `...piano9cii-...`) = **Integrazione Electron**: `app/desktop` apre il DB reale via `createMemorySystem(userData/loomn.db)` **dentro Electron** (better-sqlite3 ricompilato per ABI Electron + plugin Vite `closeBundle`→`out/migrations`), monta `createCampaignService`+`createLanguageProvider` dietro handler IPC sottili (`dispatch`/`run-turn`/`set-provider`/`reflect`/`get-status` Zod-validati, push read-side `{version,state}`), `safeStorage` per le chiavi (settings in `userData`), provider swappabile a runtime (`provider-holder`), `ipc.ts` riscritto (`ping` rimosso). **Gate "esegui l'app" SUPERATO** (self-test `LOOMN_SELFTEST` 2 fasi). **→ PIANO 9 COMPLETO; 266 test a fine Piano 9** (poi G1/F4/G6/F2/SP1-4/G3-G4/F3-G5; **il conteggio corrente è 427**, vedi §0-terdecies). **Il prossimo passo NON è un altro sotto-piano del 9, e NON è ancora il Piano 10.** Dopo il Piano 9 è partita la **traccia engine/AI** guidata dai findings dello spike: **G1 e F4 fatti** (§0-ter/§0-quater). **MANDATO ATTUALE (deciso con l'utente): completare l'intero backlog pre-Piano 10 — §0-quinquies** (~~G6~~ → ~~F2~~ → ~~traccia engine (SP1-4) + FSM §5.5~~ → ~~G3/G4~~ → ~~F3/G5~~ → ~~segmentazione reflect (item 6)~~ ✅ (`2607342`, §0-quaterdecies); + pianificata la feature Inventario §8) = **BACKLOG PRE-PIANO 10 CHIUSO**. **Ora:** (b) **Piano 10 — UI Vue** (NON da `writing-plans`: prima studio/design, brainstorming + `frontend-design`, approccio deciso con l'utente); (c) **Piano 11 — moduli a tema**. Tutti i piani sono in `docs/superpowers/plans/`.

### 0-bis. Aggiornamento 2026-06-16 (sera): Traccia B fatta → prossima mossa DECISA = traccia engine/AI

**Cos'è successo dopo il Piano 9.** Concordata con l'utente la direzione di partenza tra le candidate di §8/§9, è stata scelta ed eseguita la **Traccia B — slice giocabile** (validare memoria+AI con un LLM vero), perché de-rischia l'assunzione più costosa-se-sbagliata (il nucleo AI+memoria, mai esercitato con un modello reale) **prima** di costruirci sopra UI/altro engine. **Fatta.** Findings completi in **`docs/superpowers/findings-slice-llm.md`** (su main). Lo spike è **throwaway** e vive sul branch **`spike/slice-llm`** (harness + driver `packages/host/src/slice-harness.ts` + `slice-llm.spike.test.ts`, guardati da `LOOMN_SPIKE` → inerti in `pnpm test`); **su main è andato solo il findings doc**. Il branch è **tenuto vivo** per ri-validare dopo i fix.

**Cosa ha detto lo spike (sintesi; dettagli nel findings doc):**
- ✅ **Validato:** il tool-calling end-to-end (7c) gira con un modello dal template tool sano (Llama-3.1-8B), e **il "codice è l'arbitro" REGGE sotto stress reale** (spawn duplicato rifiutato dall'engine, argomenti invalidi rifiutati da Zod, zero mutazioni dirette). Memoria write→read ok. Collisione id su seconda `reflect` riprodotta.
- 🔴 **F4 (il finding centrale):** la Reflection consuma **solo gli `Event`, mai la narrazione** → la memoria **ricorda le statistiche e dimentica la storia** (relazioni/segreti narrati non entrano in L1.5/L2). Per un simulatore narrativo va risolto **prima** del Piano 10.
- 🟠 **G1 (quick win):** il modello manda i numeri come **stringhe** (`defenseBase:"10"`) → `z.number()` li rifiuta → ha bloccato tutto il combattimento. Fix: schemi tool **coercivi** (`z.coerce.number()`) in `master-tools.ts`.
- 🟠 **G3/G4:** il modello inventa risorse/skill inesistenti (manca il **vocabolario di gioco** nel contesto); `spawn_npc` crea PNG senza `hp` (non combattibili). **F2:** l'adapter inghiotte gli errori SSE di LM Studio (confermato dal progetto sibling `alice`). **F3/G5:** L1.5 inquinato da statistiche già in L1; estrazione malformata su modello piccolo.

**Prossima mossa (decisa con l'utente): traccia engine/AI guidata dai findings.** Sequenza raccomandata: ~~**(1) G1** — schemi tool coercivi~~ **✅ FATTO** (vedi §0-ter); **(2) F4** — decisione di design su *come la storia entra in canone* (narration→Reflection e/o nuovo `Command`/tool `record_fact`), che si salda naturalmente con la **traccia engine già prevista** (nuovi `Command`/`Event` per `request_check`/`apply_effect`/`advance_quest` + contesto quest + **FSM di fase** §5.5). **F4 è il prossimo passo: NON partire da `writing-plans` — prima brainstorming (skill `superpowers:brainstorming`)** per soppesare le opzioni (a)/(b)/(c) coi trade-off, decidendo CON l'utente; poi `writing-plans`. Candidati successivi dai findings: **F2** (adapter rileva SSE error), **G3/G4** (vocabolario di gioco nel contesto + combat-readiness di `spawn_npc`; lega a Piano 11), **F3/G5** (qualità estrazione L1.5). **NON** partire dal Piano 10 (UI) finché F4 non è indirizzato (memoria che dimentica la storia).

**Per ri-eseguire l'harness dello spike (dopo i fix):** serve LM Studio con un modello **tool-capable** (`meta-llama-3.1-8b-instruct` va; i GGUF `gemma-4-*-qat` hanno il **template tool rotto** → errore Jinja su qualunque richiesta con `tools`). Porta i file dal branch (`git checkout spike/slice-llm -- packages/host/src/slice-harness.ts packages/host/src/slice-llm.spike.test.ts`) e lancia: `LOOMN_SPIKE=1 LOOMN_MODEL=<id> pnpm exec vitest run packages/host/src/slice-llm.spike.test.ts` (osservazioni in `<tmp>/loomn-spike-observations.md`).

Il flusso da seguire è sempre lo stesso (vedi §4): `writing-plans` → commit doc su main → branch → `subagent-driven-development` (implementer + 2 review per task) → `finishing-a-development-branch` (merge locale) → aggiorna memoria.

L'utente scrive tipicamente **"scrivi il Piano N"** e poi sceglie **"subagent-driven"** per l'esecuzione. Procedi in autonomia fino al merge, senza fermarti tra i task.

### 0-ter. Aggiornamento 2026-06-16 (notte): G1 fatto → prossima = F4 (brainstorming)

**G1 — schemi tool coercivi-ma-strict ✅ FATTO e mergiato in main** (`d1ec4a5`; commit `9343d48` core + `d1ec4a5` hardening da review). **273 test verdi** (266 + 7 nuovi in `master-tools.test.ts`), typecheck pulito. Processo: l'utente ha scelto **TDD diretto + final review** (non la flotta subagent completa — fix a un solo file, proporzionato; tocca Traccia C "rito di review proporzionato").

**Cosa è cambiato (solo `packages/ai/src/master-tools.ts` + il suo test):** introdotto l'helper `llmNumber` e applicato a TUTTI i campi numerici degli argomenti tool (`attackSchema.defenseBase`, `startEncounterSchema.participants[].initiative`, `resourcePoolSchema.current/max`, valori di `spawnNpcSchema.attributes/skills`). **Decisione di design (più del finding):** NON `z.coerce.number()` nudo. La coercizione nuda accetta `""`→`0` e `null`→`0` (zero silenzioso) e, dopo coercizione, `"Infinity"`→`Infinity` — contrario a "il codice è l'arbitro". `llmNumber = z.preprocess(coerci-solo-stringhe-numeriche-trimmate, z.number().finite())`: una stringa numerica (`"10"`, `" 10 "`, `"10.5"`, `"1e3"`, `"-3"`) → numero; **stringa vuota/whitespace/non-numerica, `null`, mancante, e non-finito (`"Infinity"`) → RIFIUTATI**. (Verificato empiricamente prima e durante.) Nota: in questa versione di zod (3.25) `z.number()` **già** rifiuta `NaN` e i campi richiesti mancanti — il valore aggiunto di `llmNumber` sul nudo coerce è chiudere `""`/`null`/`Infinity`→silenzioso.
- **Lo schema JSON mostrato al modello resta `{"type":"number"}`** (preprocess è trasparente a `zodToJsonSchema`): NON advertizziamo che le stringhe vanno bene — coerciamo solo come rete di sicurezza.
- **`makeEntry` cambiato da `makeEntry<A>(schema: z.ZodType<A>, …)` a `makeEntry<S extends z.ZodTypeAny>(schema: S, toCommand: (args: z.infer<S>) => Command)`**: il vecchio capture a un parametro forzava input=output=A e degradava a `unknown` l'output degli schemi con `preprocess` (input≠output). Cambio puramente type-level, idiomatico, cast-free, nessun `any` al confine (verificato dalla review con probe `@ts-expect-error`).

**Final review (subagent, opus):** verdetto **Ready to merge: Yes**, zero Critical/Important. Verificato empiricamente: coercizione funziona, strictness reale e test-guarded (un `z.coerce.number()` nudo farebbe fallire il test della stringa vuota), JSON schema invariato, `z.infer<S>` non degrada ad `any`. Finding minore "Infinity" **accolto** (chiuso con `.finite()` + 2 test, via TDD); restrizione hex/notazione-scientifica **declinata** (numeri finiti legittimi, YAGNI).

**Prossimo = F4** (la decisione di design più importante: come la STORIA entra in canone). ⚠️ **NON partire da `writing-plans`**: prima **brainstorming** (skill `superpowers:brainstorming`) per soppesare (a) narrazione→Reflection, (b) nuovo `Command`/tool `record_fact`, (c) entrambi — coi trade-off, decidendo CON l'utente. Poi `writing-plans` → flusso §4. **Opzionale: ri-validare con lo spike** (LM Studio + Llama-3.1-8B) che il combattimento ora si sblocchi (G1) — vedi §0-bis per come riportare i file dal branch `spike/slice-llm`.

### 0-quater. Aggiornamento 2026-06-16 (notte): F4 fatto → la memoria ricorda la storia

**Ri-validazione post-G1 (prima di F4).** Ri-eseguito l'harness della slice su main (con G1) contro Llama-3.1-8B: **G1 validato sul campo** (`"defenseBase":"10"` coerciva → attacco risolto dal motore, prima la stringa bloccava il combattimento); **F4 ri-confermato forte** (la memoria registrava solo fatti meccanici, l'affare losco/Barone Vhalmar/supplica sparivano); **nuovo finding G6** (il modello stringifica anche gli argomenti **array** — `participants`/`facts` → `Expected array, received string` → versione array di G1, candidato a una coercizione gemella). Dettagli ri-validazione nel findings doc (`docs/superpowers/findings-slice-llm.md`, §Ri-validazione post-G1).

**F4 — la storia entra in canone ✅ FATTO e mergiato in main** (`9e143aa`; spec `docs/superpowers/specs/2026-06-16-f4-storia-in-canone-design.md`, piano `docs/superpowers/plans/2026-06-16-loomn-f4-storia-in-canone.md`). **280 test verdi**, typecheck pulito (6 progetti, incl. `app/desktop`). Processo: brainstorming → spec approvato → writing-plans → branch `feat/f4-storia-in-canone` → subagent-driven (implementer + spec review + code-quality review per task; 2 fix di review accolti con verifica empirica; final review opus = "Ready to merge: Yes", zero Critical/Important) → merge ff.

**Cosa è stato fatto (Approccio A′ — vedi spec):** nuovo evento `NarrationRecorded { playerAction, narration }` come variante di `DomainEvent` nel motore (`applyEvent` no-op di stato, solo `version++`, come `AttackResolved`) — **l'unico evento non prodotto da `decide`**, appeso da `runTurn` quando il Master narra. Cosi la prosa entra nello **stesso** event stream (vincolo spec §6: tutto deriva da uno stream, rebuild-safe; niente sistema parallelo). `reflect` la riceve via `eventStore.load()`; `renderEventsForReflection` la rende come **prosa** (non JSON) e l'estrattore vede la storia; `EXTRACT_SYSTEM` raffinato (fatti **narrativi**, esclude le statistiche già in L1 → **chiude anche F3**). `TurnOutcome.events` restituita resta **meccanica** (il NarrationRecorded è persistenza di stream, documentato con JSDoc); `domainEventSchema` (shared) esteso (drift-guard tengono engine↔schema allineati). **Nessuna modifica** a `commandSchema`/`app/desktop`/UI/migrazioni (l'handler IPC `run-turn` non serializza gli events → NarrationRecorded non attraversa l'IPC, verificato dalla final review). File toccati: `events.ts`(+test), `domain-schema.ts`(+test), `campaign-service.ts`(+test), `reflection-ports.ts`(+test).

**Decisioni di review accolte (verifica empirica):** JSDoc dell'invariante `TurnOutcome.events` + test del ramo "Event meccanici ma narrazione vuota" (Task 2); label del prompt Reflection coerente con la prosa + asserzione sequence-anchor (Task 3). Declinati come gold-plating: micro-opt allocazione `toStore`, test mixed-mode (rami isolati già coperti).

**Prossimo:** vedi **§0-quinquies** — l'utente ha deciso di **completare l'intero backlog pre-Piano 10** prima della UI. Opzionale lungo il percorso: ri-validare con lo spike (LM Studio) per vedere F4/G6 sul campo.

### 0-quinquies. Mandato del prossimo agente: completare il backlog pre-Piano 10

**Decisione dell'utente (2026-06-16):** prima del **Piano 10 (UI)** vanno chiusi **tutti** gli item rimasti della traccia engine/AI (i findings dello spike + la traccia engine già prevista). Non è più una scelta aperta: il mandato è completarli, nell'ordine sotto (resequenziabile dal prossimo agente con motivazione). Ogni item segue il **flusso §4** (per i piani: `writing-plans` → commit doc su main → branch → `subagent-driven-development` → `finishing-a-development-branch` → aggiorna memoria/HANDOFF). Procedi in autonomia fino al merge di ciascuno; **chiedi all'utente solo per le decisioni di design** (gli item "design-first" sotto: prima `superpowers:brainstorming`).

Ordine raccomandato (dal cheap/meccanico allo strutturale):

1. ~~**G6 — coercizione argomenti ARRAY (la "G1b")**~~ **✅ FATTO e mergiato** (`b9e3614`, **287 test**) — vedi **§0-sexies**. Helper `llmArray` in `packages/ai/src/master-tools.ts` su `start_encounter.participants` (scope: write-path Reflection `extract_facts` deliberatamente NON toccato → tracciato per F3/G5 item 5).
2. ~~**F2 — l'adapter rileva i frame SSE di errore**~~ **✅ FATTO e mergiato** (`2d517fe`, **292 test**) — vedi **§0-septies**. `streamChatCompletion` rileva un `error` top-level non-null e solleva `LanguageModelError` (trace `error`); `error:null` su un chunk di successo NON è un errore.
3. ~~**Traccia engine — nuovi `Command`/`Event` + FSM di fase §5.5 · DESIGN-FIRST · la grande, decomponibile.**~~ **✅ FATTO e mergiato — decomposta in SP1-4, tutti fatti** (SP1 `request_check` §0-octies; SP2 `apply_effect` §0-novies; SP3 quest+`advance_quest` §0-decies; SP4 FSM di fase §0-undecies; HEAD `50c6060`, **392 test**). Gli strumenti rimandati di 7c (`request_check`/`apply_effect`/`advance_quest`) ora esistono come Command/Event reali e validati; le fasi sono una FSM esplicita e testabile (`engine/phase.ts` + gate `isCommandLegalInPhase`). I `PhaseChanged` danno i confini naturali di scena per il punto 6. *Acceptance raggiunta.*
4. ~~**G3/G4 — vocabolario di gioco nel contesto + combat-readiness di `spawn_npc`**~~ **✅ FATTO e mergiato** (`06b398b`, **422 test**) — vedi **§0-duodecies**. Vocabolario dati-only iniettato come `Ruleset {vocabulary, dcForDifficulty}` (§5.3, NON event-sourced; `dcForDifficulty` migrato da SP1); `decide` rifiuta gli id fuori-vocabolario PRIMA di tirare (`requireMember`); `spawn_npc` auto-fill da `defaultResources` (combat-ready); tool con `z.enum` dal vocabolario; `renderL1` espone attributi/abilità per-attore; seme dev `devRuleset` in `@loomn/host`. `@loomn/shared` intatto. *Acceptance raggiunta.*
5. ~~**F3/G5 — qualità estrazione L1.5 oltre il prompt**~~ **✅ FATTO e mergiato** (`0acbff0`, **427 test**) — vedi **§0-terdecies**. Coercizione strutturale (G6 portato sul write-path): `llmArray` su `factsResultSchema.facts` + coercizione numerica su `importance`; helper coercivi estratti in un `coercion.ts` condiviso di `@loomn/ai`. Scope (A) solo strutturale; G5b semantico era già smussato da F4 → follow-up dichiarato. *Acceptance raggiunta.*
6. ~~**Segmentazione `reflect` per scena · engine/memory · medio.**~~ **✅ FATTO e mergiato** (`2607342`, **444 test**) — vedi **§0-quaterdecies**. `reflect` ora è incrementale + segmentato per scena (confini `PhaseChanged` di SP4) con un **cursor esplicito** persistito (tabella `reflection_cursor`, riga singleton): riflette solo `seq > watermark`, segmenta in scene (range per-scena → id unici), avanza il watermark per scena (crash-safe). Scena aperta = flush. Decomposto in 3 unità (`segmentScenes` pura · `ReflectionCursor` store · `runScenesReflection` orchestratore) + reorder behaviour-preserving di `runReflection`. Blast radius memory+host. *Acceptance raggiunta* (guard di non-collisione verificato RED sul vecchio codice). **Era l'ULTIMO item del backlog → backlog pre-Piano 10 CHIUSO.**

**Follow-up minori (non bloccanti, da fare opportunisticamente o annotare):** seed RNG per-campagna **persistito** (replay già deterministico, serve per robustezza); **delta read-model** (spec §13, oggi snapshot completo `{version,state}` — più un concern di Piano 10 quando lo stato cresce). Vedi §7-quinquies.

Quando il backlog è chiuso → **Piano 10 — UI** (NON da `writing-plans`: prima studio/design, `superpowers:brainstorming` + `frontend-design`; aggancio `ReadModel={version,state}` + canali IPC del 9c-ii, Pinia sul read side).

### 0-sexies. Aggiornamento 2026-06-16: G6 fatto → prossima = F2 (adapter SSE error)

**G6 — coercizione argomenti ARRAY ("G1b") ✅ FATTO e mergiato in main** (`b9e3614`; HEAD ora `b9e3614`). **287 test verdi** (280 + 7: 6 G6 + 1 guardia schema JSON in `master-tools.test.ts`), typecheck pulito (6 progetti). Processo: **TDD diretto + final review** (concordato con l'utente per i fix meccanici, come G1) — final review (general-purpose) verdetto **Ready to merge: Yes**, zero finding, verificato empiricamente con 14 input di rottura via probe.

**Cosa è cambiato (solo `packages/ai/src/master-tools.ts` + il suo test — scope mono-package disciplinato):** nuovo helper `llmArray` (gemello di `llmNumber`) = `z.preprocess(v => typeof v === 'string' ? tenta JSON.parse(trim) : v, schemaArrayReale)`, applicato a `start_encounter.participants`. **Strict come G1:** stringa JSON-array → coerciuta e poi validata dallo schema array reale; stringa non-JSON / JSON che non è un array (oggetto/numero/`null`) / vuota / whitespace / mancante → **RIFIUTATA** da `z.array` (niente array silenzioso). Proprietà verificate: **`.min(1)` sopravvive** alla coercizione (`"[]"` rifiutato); **compone con `llmNumber`** (un `initiative` stringa dentro un `participants` stringificato coercia a numero); **lo schema JSON mostrato al modello resta `{type:array, minItems:1}`** (il preprocess è trasparente a `zodToJsonSchema`, che usa lo schema di OUTPUT — guardia di test dedicata, non advertizziamo che le stringhe vanno bene).

**Scelta di scope (concordata con l'utente):** G6 resta su `master-tools.ts` (il path tool/engine dove il combattimento si bloccava — blocco DURO). Il write-path Reflection `extract_facts` soffre dello stesso vizio (`facts` stringificato → la function-call ripiega su repair, **degrado morbido** non blocco) ma è **deliberatamente fuori scope di G6** → tracciato per **F3/G5 (item 5)**, dove si valuterà in modo olistico (coercizione array e/o constrained decoding `json_schema` e/o prompt).

**Prossimo = F2** (item 2 di §0-quinquies): l'adapter (`openai-adapter.ts`) deve rilevare i frame SSE di errore (oggi controlla solo `res.ok`; LM Studio segnala l'errore Jinja con HTTP 200 + SSE `event: error` → stream vuoto silenzioso) e sollevarli come `LanguageModelError`. Meccanico, piccolo, `@loomn/ai`; stesso stile TDD diretto + final review. Corroborato dal sibling `alice` (`if "error" in chunk`). *Acceptance:* un frame SSE di errore fa lanciare un `LanguageModelError`, non uno stream vuoto.

### 0-septies. Aggiornamento 2026-06-17: F2 fatto → prossima = traccia engine (DESIGN-FIRST, brainstorming)

**F2 — l'adapter rileva i frame SSE di errore in-band ✅ FATTO e mergiato in main** (`2d517fe`; HEAD ora `2d517fe`). **292 test verdi** (287 + 5 in `openai-adapter.test.ts`), typecheck pulito. Processo: TDD diretto + final review (general-purpose) verdetto **Ready to merge: Yes**, zero finding, verificato empiricamente (anche probe sui casi limite: `error:null`→non lancia; `{"error":{}}` senza `message`→lancia col JSON del body).

**Cosa è cambiato (solo `packages/ai/src/openai-adapter.ts` + il suo test — scope mono-package):** nuovo helper `streamErrorMessage(json)` che rileva un campo `error` **top-level non-null** in un frame SSE (stringa→la stringa; oggetto con `message:string`→il message; altro oggetto→`JSON.stringify`; `undefined`/`null`→non è un errore). `streamChatCompletion` ora riceve `status`/`statusText` e, **prima** di `chunkSchema.safeParse`, se il frame è un errore solleva `LanguageModelError(res.status=200, 'OK', body=messaggio)` invece di saltarlo silenziosamente. `stream()` avvolge il loop in try/catch: su un errore in-fase-di-stream traccia `kind:'error'` e **rilancia** (il chiamante riceve il throw, non più uno stream vuoto). Detection/estrazione gemella del sibling `alice` (`if "error" in chunk`; `err.message` se dict), **migliorata**: `error:null` non è trattato come errore (alice usava la sola presenza della chiave). Nessuna regressione (chunk con `choices`, riga `data:` malformata, errore `!res.ok` pre-stream, trace happy-path `['request','response']` — tutti invariati e verificati).

**Decisione di rappresentazione (difesa in review):** si riusa `LanguageModelError` con `status:200` (l'HTTP **fu** davvero 200, l'errore vive nel body) — onesto e minimale come da mandato; NON si è esteso il tipo né toccata l'API nativa LM Studio (fuori scope).

**Prossimo = item 3 di §0-quinquies — la TRACCIA ENGINE (DESIGN-FIRST, la grande/decomponibile).** Nuovi `Command`/`Event` per gli strumenti rimandati di 7c (`request_check`/`apply_effect`/`advance_quest`) + **contesto quest** (L1) + **FSM di fase** (esplorazione/dialogo/combattimento/downtime, spec §5.5). ⚠️ **NON partire da `writing-plans`: prima `superpowers:brainstorming`** per soppesare le opzioni di design CON l'utente (cosa coprono i tre tool, come si modella la FSM, come il contesto quest entra in L1, confini di scena per la segmentazione reflect dell'item 6), poi decomporre in sotto-piani col flusso §4. *Acceptance:* i tre tool rimandati esistono come Command/Event reali e validati; le fasi sono una FSM esplicita e testabile. **Gli item meccanici (G6, F2) sono chiusi: da qui in poi gli item del backlog sono design-first e vanno aperti col brainstorming.**

### 0-octies. Aggiornamento 2026-06-17: traccia engine decomposta in SP1-4; SP1 (request_check) fatto

**Brainstorming dell'item 3 (traccia engine) → decomposta in 4 sotto-progetti**, ognuno col proprio ciclo spec→piano→merge, ordine piccolo→grande: **SP1** `request_check` · **SP2** `apply_effect` · **SP3** quest in L1 + `advance_quest` (cross-package) · **SP4** FSM di fase §5.5 (per ultima: vincola un vocabolario di Command completo e fissa i **confini di scena** per l'item 6 = segmentazione `reflect`).

**Sfida dell'utente sul design ("è pigra? la voglio senza debiti, professionale"):** la critica ha portato alla decisione architetturale chiave di tutta la traccia — **dove vivono i numeri**. NON l'AI propone i numeri grezzi (debito "il modello inventa", famiglia G3/G4): l'AI propone **giudizi qualitativi**, il **codice** possiede i numeri. Per i check: l'AI propone una **band di difficoltà** (`trivial..legendary`), una funzione pura possiede la tabella band→CD. Valutata e **scartata** (con l'utente) la `Ruleset` injection (§5.3) ORA: chiude lo stesso debito ma aggiunge un'astrazione a una-sola-implementazione modellata su un solo caso (= debito travestito da rigore); `dcForDifficulty` è isolato e migrabile in un `Ruleset` deliberato quando SP2/SP3/Piano 11 ne riveleranno la forma reale.

**SP1 — `request_check` ✅ FATTO e mergiato in main** (`c04d9a3`; HEAD ora `c04d9a3`; spec `specs/2026-06-17-sp1-request-check-design.md`, piano `plans/2026-06-17-loomn-sp1-request-check.md`). **305 test verdi** (292 + 13), typecheck pulito (6 progetti). Processo: brainstorming → spec (con self-review che ha ristretto SP1 a engine+shared+ai) → writing-plans → branch → subagent-driven (4 task TDD: difficulty.ts → CheckResolved engine+shared → RequestCheck command → request_check tool; final review opus **Ready to merge: Yes**, zero finding) → merge ff. **Cosa è stato fatto:** nuovo `packages/engine/src/difficulty.ts` (`DIFFICULTIES`, `Difficulty`, `dcForDifficulty` 5/10/15/20/25/30 — funzione pura, migrabile in un Ruleset); Command `RequestCheck { actorId, attribute?, skill?, difficulty }` + caso `decide` (cerca attore, `actorCheck` con `includeEquipped:true`, RNG seedato); evento `CheckResolved { actorId, attribute?, skill?, difficulty, result: CheckResult }` **no-op di stato** in `applyEvent` (come `AttackResolved`); tool `request_check` con `z.enum(DIFFICULTIES)` **auto-validante** (l'AI non può inventare una band; rovescio strict di G1/G6); `domainEventSchema` (shared) esteso. **Decisione tecnica notevole:** `CheckResolved` è il **primo evento con campi opzionali top-level** → `domainEventSchema` convertito da `z.discriminatedUnion` a `z.union([z.discriminatedUnion([8 eventi invariati]), checkResolvedEventSchema])`, dove `checkResolvedEventSchema` usa un `.transform()` per omettere `attribute`/`skill` assenti cast-free (sotto `exactOptionalPropertyTypes`); `discriminatedUnion` non accetta i `ZodEffects` del transform (stesso motivo di `commandSchema`). Il drift guard bidirezionale di `memory` (`sqlite-event-store.ts:85-90`) resta verde. **Non toccati** (confermato dal self-review + final review): `master-turn.ts` e `host/reflection-ports.ts` (gestiscono `CheckResolved` per via generica — `JSON.stringify` nella reiniezione, ramo generico in `renderEventsForReflection` + `EXTRACT_SYSTEM` ignora i tiri meccanici), `commandSchema` (guard wire→motore unidirezionale, `RequestCheck` è interno all'AI non un dispatch IPC), UI/migrazioni.

**Deferral dichiarati (nessun debito silenzioso):** validazione vocabolario `attribute`/`skill` (oggi `getAttribute`/`getSkill` fanno `?? 0` → stat inesistente = 0 silenzioso) → **item 4** (G3/G4, cross-cutting su tutti i tool); `Ruleset` injection §5.3 → item deliberato dopo SP1-4; `defenseBase` grezzo di `Attack` (l'AI lo inventa ancora) → riconciliazione al band model, follow-up; check contrapposti e modificatori situazionali → YAGNI/capacità engine preservata.

**SP2 — `apply_effect` · DESIGN GIÀ DECISO, spec scritto e committato** (`6c9041c`; `specs/2026-06-17-sp2-apply-effect-design.md`). Il brainstorming è **fatto** (decisioni prese con l'utente). Sintesi del design (leggi lo spec per i dettagli): l'AI propone *target / risorsa / direzione (`restore`|`drain`) / espressione di dadi*; l'**engine TIRA** (RNG seedato), calcola il `delta` netto (magnitudine clampata `≥0`), e arbitra l'applicazione (clamp `[0,max]`, rifiuto attore/risorsa ignoti, event-sourced). Nuovo Command `ApplyEffect { targetId, resource, direction, dice: DieGroup[], bonus? }` + evento `ResourceEffectApplied { targetId, resource, delta, roll: RollResult }` (registra il `roll` — forward-looking per il pannello dadi 3D; nessun campo opzionale top-level → entra nella `discriminatedUnion` interna, niente complicazioni `z.union`). Tool `apply_effect` con coercizioni G1/G6 (`direction` enum auto-validante; serve un helper coercivo-**intero** gemello di `llmNumber` per `count`/`sides`). Tocca **solo engine+shared+ai** (come SP1; `master-turn`/`reflection`/`commandSchema` non toccati). **Rimandato (dichiarato):** condizioni/status (entangled col vocabolario, item 4/Piano 11), auto-downing, effetti flat, Ruleset.

**→ [⚠️ SUPERATO — SP2 FATTO, vedi §0-novies]** Il prossimo passo *era* `superpowers:writing-plans` per SP2; ora SP2 è implementato e mergiato (`dbee1a8`). Poi flusso §4: commit piano su main → branch `feat/sp2-apply-effect` → subagent-driven (3 task TDD: ResourceEffectApplied event engine+shared / ApplyEffect command+decide / helper llmInt+tool) → final review → `finishing-a-development-branch` (merge ff) → aggiorna HANDOFF/memoria. **Dopo SP2:** SP3 (quest in L1 + `advance_quest`, DESIGN-FIRST→brainstorming), SP4 (FSM di fase §5.5, DESIGN-FIRST→brainstorming), poi gli ultimi item del backlog (G3/G4 vocabolario, F3/G5 estrazione, segmentazione `reflect`), infine **Piano 10 (UI)**.

### 0-novies. Aggiornamento 2026-06-17: SP2 (`apply_effect`) fatto → prossima = SP3 (quest, DESIGN-FIRST)

**SP2 — `apply_effect` ✅ FATTO e mergiato in main** (ff a `dbee1a8`; HEAD ora `dbee1a8`; spec `specs/2026-06-17-sp2-apply-effect-design.md`, piano `plans/2026-06-17-loomn-sp2-apply-effect.md`). **329 test verdi** (305 + 24: engine `events` 4, `shared` 2, engine `commands` 7, ai `master-tools` 11), typecheck pulito (6 progetti). Processo: writing-plans → commit piano su main → branch `feat/sp2-apply-effect` → subagent-driven (3 task TDD: evento → command → tool+helper; un implementer + spec-review + code-quality-review per task; **final review opus = Ready to merge: Yes**, zero Critical/Important) → merge ff → branch cancellato.

**Cosa è stato fatto (design nello spec):** l'AI propone *target / risorsa / direzione (`restore`|`drain`) / espressione di dadi*; l'engine **TIRA** (RNG seedato via `rollExpression`, `mode:'effect'`), calcola il `delta` netto (magnitudine clampata `≥0` → restore non drena mai, drain non ripristina mai) e **clampa** la risorsa in `[0,max]` via `adjustResource`. Nuovo Command `ApplyEffect { targetId, resource, direction, dice: DieGroup[], bonus? }` (engine `commands.ts`, caso `decide`: rifiuta attore/risorsa ignoti **PRIMA** di tirare → throw, 0 eventi); evento `ResourceEffectApplied { targetId, resource, delta, roll: RollResult }` (engine `events.ts`, `applyEvent` muta via `adjustResource` come `DamageApplied` ma con `delta` già firmato, **registra il `roll`** = provenienza forward-looking per il pannello dadi 3D, primo evento a farlo). **Nessun campo opzionale top-level → entra direttamente nella `z.discriminatedUnion` interna** di `domainEventSchema` (NON serve l'arm `z.union` con `.transform()` di SP1; `roll` riusa `rollResultFields`), drift guard bidirezionale di `memory` verde. Tool `apply_effect` (ai `master-tools.ts`, **7° strumento**) con `direction` enum auto-validante + coercizioni G1/G6 + **nuovo helper `llmInt(min)`** (gemello coercivo-**intero** di `llmNumber` per `count≥1`/`sides≥2`: coerce solo stringhe numeriche, poi `z.number().int().min(min)` → rifiuta decimali/sotto-minimo/vuoto/non-numerico/null/Infinity, niente intero silenzioso). Refactor di review: estratto `coerceNumericString` condiviso da `llmNumber`/`llmInt` (DRY, 2 consumatori reali). **Solo engine+shared+ai** (come SP1): `master-turn.ts`/`reflection-ports.ts`/`commandSchema`/UI/migrazioni **non toccati** — la final review ha verificato empiricamente che il turno reinietta `delta`+`roll` con `JSON.stringify` generico (il modello li narra) e la reflection li ignora via ramo generico (`EXTRACT_SYSTEM` salta le statistiche meccaniche).

**Decisioni di review accolte (verifica empirica del feedback, working-style §5.2):** Task 1 — asserzione `version` simmetrica + 2 test di rifiuto attore/risorsa a livello `applyEvent`; Task 2 — test del clamp di magnitudine anche in direzione `drain`; Task 3 — test di rifiuto `sides:1`, transparency test esteso al livello-item (`count`/`sides` advertizzati come `integer` nello schema JSON mostrato al modello), estrazione `coerceNumericString`; final review — asserzione drain-clamp pinnata a `ev.delta + 0 === 0` (normalizza `-0`→`+0`). **Declinati** (over-engineering/ridondanza, dichiarati): normalizzare `-0` nel codice di produzione (la final review ha verificato che è un artefatto JS innocuo — identità in `adjustResource`, normalizzato da `JSON.stringify`, lo stato non diverge mai tra replay-da-memoria e replay-da-disco), commenti inline duplicati, `rollResultSchema` nominato (YAGNI), uniformare lo stile di narrowing dei test.

**Deferral dichiarati (nessun debito silenzioso):** condizioni/status (l'altra famiglia di "effetto", entangled col vocabolario di status → item 4/Piano 11), auto-downing quando un drain esaurisce la risorsa, effetti flat senza dadi (YAGNI), `Ruleset` injection (§5.3), `defenseBase` grezzo di `Attack`.

**Prossimo = SP3 — quest in L1 + `advance_quest` · DESIGN-FIRST · cross-package.** **[⚠️ SUPERATO — SP3 FATTO, vedi §0-decies]** ⚠️ NON partire da `writing-plans`: prima **`superpowers:brainstorming`** per soppesare il design CON l'utente (nuova entità `Quest` nel `GameState`, eventi del ciclo di vita quest, e il **contesto quest che entra in L1** = schema in `@loomn/shared` + reso in prosa nel Context Assembler di `@loomn/memory`). Poi flusso §4. Dopo SP3: **SP4** FSM di fase §5.5 (design-first, per ultima: vincola un vocabolario di Command completo e fissa i confini di scena per la segmentazione `reflect`). Poi gli ultimi item del backlog (G3/G4 vocabolario, F3/G5 estrazione, segmentazione `reflect`), infine **Piano 10 (UI)**.

### 0-decies. Aggiornamento 2026-06-17: SP3 (quest in L1 + `advance_quest`) fatto → prossima = SP4 (FSM di fase §5.5, DESIGN-FIRST)

**SP3 — quest in L1 + `advance_quest` ✅ FATTO e mergiato in main** (ff a `8d3f67b`; HEAD ora `8d3f67b`; spec `specs/2026-06-17-sp3-quest-design.md`, piano `plans/2026-06-17-loomn-sp3-quest.md`). **360 test verdi** (329 + 31: engine `quest` 2, engine `events` 6, engine `commands` 9, shared `domain-schema` 5, ai `master-tools` 6, memory `context-assembler` 5 — più i ripple-literal `quests: {}` su 4 file di test, no-conteggio), typecheck pulito (6 progetti). Processo: brainstorming → spec (commit su main) → writing-plans (commit su main) → branch `feat/sp3-quest` → subagent-driven (5 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus = Ready to merge: Yes**, zero Critical/Important) → merge ff → branch cancellato.

**Decisione di design (con l'utente, "cosa consigli per zero debiti?"): `Quest` SOLO STATO.** `Quest { id, title, description?, status: 'active'|'completed'|'failed' }`; nuovo campo richiesto `quests: Record<string, Quest>` nel `GameState`. L'argomento a zero-debiti è la **separazione di strato**: i fatti meccanici autorevoli di una quest sono esistenza+identità+stato; la **storia** della quest (svolte, tradimenti) è narrazione → vive in L1.5/L2 dove **F4 la instrada già** via `NarrationRecorded`. Mettere un `progress[]`/obiettivi semi-narrativi nella quest rimetterebbe narrazione in L1 = duplicare L2 = **il debito di sync vietato dallo spec §6**. "Il codice è l'arbitro" per le quest = l'engine possiede la **FSM del ciclo di vita** e le transizioni legali (no id duplicato, no avanzare una quest ignota o già terminata). Scartati con motivazione: obiettivi/checklist (apre l'hazard di indirizzamento indice/id, G3/G4), log di progresso libero (duplica L2). Obiettivi/ricompense/`abandoned`/riapertura sono deferral additivi dichiarati (spec §7).

**Cosa è stato fatto (cross-package: engine+shared+ai+memory):** nuovo modulo engine `quest.ts` (`Quest`, `QuestStatus`, `QuestOutcome`, costanti `QUEST_STATUSES`/`QUEST_OUTCOMES`, isolato come `difficulty.ts` di SP1). **`GameState` acquisisce `quests` (primo SP che cambia la forma del `GameState`)** → `applyEvent` **muta stato reale** (`QuestStarted` aggiunge, `QuestAdvanced` aggiorna lo status — non no-op come `CheckResolved`/`AttackResolved`); ripple del campo richiesto su 4 file di test (`quests: {}`) enumerato per la disciplina di scope. Eventi `QuestStarted { quest }`/`QuestAdvanced { questId, status }`: **nessun campo opzionale top-level → entrano direttamente nella `z.discriminatedUnion` interna** di `domainEventSchema` (NON serve l'arm `z.union` di SP1; l'opzionale `description` è nidificato in `quest` e gestito da un `.transform()` nidificato di `questSchema` sotto `exactOptionalPropertyTypes`, come `dieGroupSchema`). Command `StartQuest { id, title, description? }`/`AdvanceQuest { questId, status: QuestOutcome }` + `decide` (rifiuta id duplicato → `Quest già presente`; quest ignota → `Quest sconosciuta`; quest già terminata → `Quest già terminata (<status>)`; throw + 0 eventi; nessun RNG). Tool `start_quest`/`advance_quest` (ai, **8° e 9° strumento**; `status` con `z.enum(QUEST_OUTCOMES)` **auto-validante** — l'AI non inventa l'esito, rovescio strict di G1/G6 come `difficulty`/`direction`; `start_quest` non passa lo status, l'engine impone `'active'`). `renderL1` (`@loomn/memory`) aggiunge un blocco **"Quest attive (L1)"** reso **solo se c'è almeno una quest attiva** (stato senza quest → output identico a prima, test L1 invariati; le terminate escono → la conclusione è narrata in L1.5/L2); il blocco è parte di L1 (priorità 2, mai tagliato dal budget). **Non toccati** (verificato dalla final review): `master-turn.ts` (reinietta gli eventi con `JSON.stringify` generico → il Master narra), `reflection-ports.ts`/host produzione, `commandSchema` (`StartQuest`/`AdvanceQuest` sono comandi interni dell'AI, come `RequestCheck`/`ApplyEffect` — NON un dispatch IPC del renderer), `app/desktop`/UI, migrazioni SQLite (la tabella `snapshots` è una stringa JSON dello stato; lo stato si ricostruisce dallo stream, spec §6.3).

**Verifica empirica del feedback di review (working-style §5.2):** accolti (hardening su rami reali) — rifiuto di `QuestAdvanced` con `status` non terminale al confine `domainEventSchema` + preservazione `title` (Task 2); pin del messaggio completo `Quest già terminata (completed): q1` (Task 3); test del caso **misto attive+terminate** (l'invariante centrale "solo attive in L1") + dedup dell'IIFE del test budget-0 (Task 5). **Declinati** con motivazione (no gold-plating §5): riformulare la prosa della description del tool (contraddice lo spec — `description` è lo *statement* dell'obiettivo); test di rifiuto `id:""` su `start_quest` (il `.min(1)` non è testato per-campo sui campi stringa altrove — solo i rami di coercizione G1 lo sono); test passthrough `failed` su `advance_quest` (lo stesso ramo passthrough è già coperto da `completed`; `failed` è coperto al livello command).

**[⚠️ SUPERATO — SP4 FATTO, vedi §0-undecies]** Prossimo *era* SP4 — FSM di fase (§5.5) · DESIGN-FIRST. ⚠️ NON partire da `writing-plans`: prima **`superpowers:brainstorming`**. Macchina a stati dichiarata (esplorazione/dialogo/combattimento/downtime) che vincola quali Command/tool sono abilitati per fase; definisce anche i **confini di scena** → si salda con l'item 6 (segmentazione `reflect`). È l'ultimo SP della traccia engine (vincola un vocabolario di Command ormai completo: `request_check`/`apply_effect`/`advance_quest` esistono). Dopo SP4: G3/G4 (vocabolario di gioco + `spawn_npc` combat-ready, design-first), F3/G5 (qualità estrazione), segmentazione `reflect` per scena, infine **Piano 10 (UI)**.

### 0-undecies. Aggiornamento 2026-06-17: SP4 (FSM di fase §5.5) fatto → traccia engine CHIUSA → prossima = G3/G4

**SP4 — FSM di fase (§5.5) ✅ FATTO e mergiato in main** (ff a `50c6060`; HEAD ora `50c6060`; spec `specs/2026-06-17-sp4-phase-fsm-design.md`, piano `plans/2026-06-17-loomn-sp4-phase-fsm.md`). **392 test verdi** (360 → 392: engine `phase`/`events`/`commands`, shared `domain-schema`, ai `master-tools`/`master-turn`), typecheck pulito (6 progetti). **Conclude la traccia engine (item 3 del backlog): SP1-SP4 tutti fatti.** Processo: brainstorming (4 assi + 3 quesiti decisi con l'utente) → spec (commit su main `141ffd7`/`d2d71e5`) → writing-plans (commit su main `ed1c92c`) → branch `feat/sp4-phase-fsm` → subagent-driven (6 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus = Ready to merge: Yes**, zero Critical/Important) → merge ff → branch cancellato.

**Decisioni di design (brainstorming, con l'utente — 4 assi):** (1) **`phase` primario** nel `GameState`, event-sourced (encounter **subordinato**); invariante **`phase==='combat' ⟺ encounter≠null`** imposta da `decide` (coppie atomiche) e verificata da un test di proprietà — risolve la dualità combat/encounter senza debito di sync. (2) **Transizioni ibride**: combat engine-grounded (`start_encounter` entra atomicamente, nuovo `end_encounter` esce → exploration); fasi soft (exploration/dialogue/downtime) via tool `enter_phase` (**intento AI** validato) — rispecchia SP1/2/3 (l'engine possiede il meccanico, l'AI propone il giudizio narrativo). (3) **Tabella permissiva tra soft + combat modale** (`canTransition` puro dichiarato; vincoli duri solo dall'invariante combat). (4) **Gate uniforme engine autoritativo + filtro tool ai**, single source of truth (`isCommandLegalInPhase` in engine, consumato da `masterToolDefs(phase)` in ai). (4a) **Frammento di prompt per fase** (`phaseGuidance`). (4b) i `PhaseChanged` sono **confini di scena** (substrato per l'item 6) ma la **segmentazione reflect NON è implementata** (resta item 6). **3 quesiti risolti:** `EncounterEnded { encounterId }` tiene l'id (idioma ES auto-descrittivo, no debito forward-compat); prompt per-fase a inizio turno (il cambio-fase mid-turn è già reiniettato via `PhaseChanged`, re-nudge di tono = deferral additivo); `attack` combat-only (l'attacco fuori combat è **incanalato** via `start_encounter`, non bloccato).

**Cosa è stato fatto (engine+shared+ai):** nuovo `engine/phase.ts` (`PHASES`/`SOFT_PHASES`/`INITIAL_PHASE`/`canTransition`, puro come `difficulty.ts`/`quest.ts`); **`GameState.phase`** (primo campo dopo `quests`; `initialState.phase = INITIAL_PHASE`); eventi `PhaseChanged {from,to}`/`EncounterEnded {encounterId}` (nessun opzionale top-level → `discriminatedUnion` interna, niente arm `z.union`); Command `EnterPhase {to: SoftPhase}`/`EndEncounter` + `isCommandLegalInPhase` (insiemi `COMBAT_ONLY`/`NON_COMBAT_ONLY`, open-set documentato) + **gate uniforme** in cima a `decide` (rimuove i guard `encounter===null` sparsi di EndTurn/NextRound; **chiude il bug del doppio `start_encounter`**); `StartEncounter`/`EndEncounter` emettono la **coppia atomica** EncounterStarted/EncounterEnded + PhaseChanged; tool `enter_phase`/`end_encounter` (**10°/11° strumento**; `to` con `z.enum(SOFT_PHASES)` auto-validante) + **`commandType` su `ToolEntry` legato al tipo prodotto da `toCommand` via `Extract<Command,{type:T}>`** (no divergenza silenziosa); `masterToolDefs(phase)` filtra per fase consumando il predicato engine, **ricomputato per-iterazione** nel loop di `runMasterTurn` (flusso start_encounter→attack); `phaseGuidance(phase)` per fase composto in `buildMasterMessages`. **Non toccati** (verificato dalla final review): `commandSchema` (EnterPhase/EndEncounter sono interni all'AI, come RequestCheck/ApplyEffect/StartQuest), `master-turn.ts` reflection/host produzione, `app/desktop`/UI, migrazioni SQLite, drift guard `_EventInfer`/`_StateInfer` (intatto, verde).

**Nota di processo (lacuna di scope del piano, risolta):** il piano aveva assegnato il **ripple di `master-turn.test.ts`** (i test attack che diventano illegali con `attack` combat-only) al Task 6 invece che al Task 4 che lo *causa* → l'implementer del Task 4 ha correttamente riportato **BLOCKED** (la suite restava rossa). Risolto **accorpando il ripple al Task 4** (i test attack partono da `combatState`), così ogni task lascia la suite verde. **Verifica empirica del feedback di review (working-style §5.2):** accolti come hardening reale — `initialState` usa `INITIAL_PHASE` (SoT, T2); `Extract<Command,{type:T}>` lega `commandType`↔`toCommand` (T5); gate testato su tutte le fasi non-combat + commento sull'open-set (T4); rifiuto `PhaseChanged` fuori enum (T3); guard difensivo `EndEncounter` pinnato (final review). **Declinati** con motivazione (no gold-plating §5): accenti nei prompt (convenzione ASCII del progetto), rinomina `NON_COMBAT_ONLY` (simmetrica con `COMBAT_ONLY`, descrive la regola), id-encounter di test estratto a costante (`'e'` è già la convenzione del file), test su uno stato già coperto dal `toEqual` esatto.

**Deferral dichiarati (nessun debito silenzioso):** segmentazione `reflect` per scena (**item 6**, ora ha il substrato dei confini di scena `PhaseChanged`); re-nudge del prompt per-iterazione (seam additivo pronto: `phaseGuidance` nel messaggio di reiniezione quando il batch contiene un `PhaseChanged`); attacco fuori-combat / helper "ambush" (start+attack); sotto-fasi di combat granulari; uscita combat parametrica (oggi `end_encounter` torna sempre a exploration); `Ruleset` injection (§5.3).

**[⚠️ SUPERATO — G3/G4 FATTO, vedi §0-duodecies]** Prossimo *era* G3/G4; ora è fatto e mergiato (`06b398b`).

### 0-duodecies. Aggiornamento 2026-06-17: G3/G4 (vocabolario di gioco + spawn_npc combat-ready) fatto → prossima = F3/G5

**G3/G4 — vocabolario di gioco iniettato + `spawn_npc` combat-ready ✅ FATTO e mergiato in main** (ff a `06b398b`; HEAD ora `06b398b`; spec `specs/2026-06-17-g3g4-vocabolario-design.md`, piano `plans/2026-06-17-loomn-g3g4-vocabolario.md`). **422 test verdi** (392 → 422), typecheck pulito (6 progetti, incl. `app/desktop` via vue-tsc). Processo: brainstorming (4 decisioni con l'utente) → spec (commit su main `e67e98f`/`07d13b0`) → writing-plans (commit su main `b72bebf`) → branch `feat/g3g4-vocabolario` → subagent-driven (8 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus = Ready to merge: Yes**, zero Critical/Important) → merge ff → branch cancellato.

**Decisione di design CHIAVE (brainstorming, sfidata dall'utente "perché non il Piano 11 prima?"):** il vocabolario di gioco (attributi/abilità/risorse/difese) è **territorio-modulo** (spec §11.4-11.7/§5.3), ma i moduli sono il Piano 11 (DOPO). Risolto con un **front-load minimo dati-only** iniettato come `Ruleset` (§5.3): G3/G4 costruisce il *consumatore* (il contratto + la validazione nel motore), il Piano 11 fornirà solo la *sorgente* (modulo caricato) mantenendo la forma — **rilavoro zero** grazie all'iniezione (il seme dev e il modulo atterrano nello stesso buco `Ruleset`). NON è la trappola SP1 (quella era una *strategia comportamentale* a una-impl; questo è *dato* spec-mandato). Carrier = **iniettato, NON event-sourced** (config statica di modulo, non play-state → lo stream resta fatti-agnostici, il replay avviene sotto un ruleset re-iniettato → `@loomn/shared`/`GameState` INTATTI).

**Cosa è stato fatto (engine+ai+memory+host+app/desktop; `shared` INTATTO):** nuovo `engine/ruleset.ts` (`Vocabulary` dati-only {attributes/skills/resources/defenses: ReadonlySet + defaultResources: Record} + `Ruleset {vocabulary, dcForDifficulty}` + factory `createVocabulary`/`createRuleset`, puro come `difficulty.ts`; `createVocabulary` impone l'invariante `defaultResources.keys ⊆ resources`). **`decide(state, cmd, rng, ruleset)`** (4° parametro §5.3, firma finale → il Piano 11/Inventario aggiungono solo campi al `Ruleset`, churn-zero); **`dcForDifficulty` di SP1 migrato** in `ruleset.dcForDifficulty` (chiuso il loose-end SP1). **Il motore è l'arbitro:** `requireMember` (privato, errore che elenca il set legale per l'auto-correzione del loop) rifiuta gli id fuori-vocabolario **PRIMA di tirare** in `AddActor` (chiavi attributi/abilità/risorse), `Attack` (attribute/skill/defense/damageResource + `target.resources` a due livelli), `RequestCheck` (attribute/skill), `ApplyEffect` (resource) → **chiude il buco G3 del silent `?? 0` e del late-throw**. **`spawn_npc` combat-ready (G4):** `decide(AddActor)` fa **auto-fill** `{...defaultResources, ...actor.resources}` (le risorse fornite sovrascrivono; le mancanti riempite → `hp` garantito anche se il modello lo dimentica) e l'`ActorAdded` porta l'attore MERGIATO (**replay-safe**: `applyEvent`/`rebuild` NON prendono il ruleset; il vocabolario è la lente, non lo stato). **Tool con `z.enum` dal vocabolario** (`master-tools.ts`): `buildTools(vocab)` parametrizza i 3 schemi vocab-dipendenti; `enumOrString(set): z.ZodType<string>` (set non vuoto → `z.enum`, JSON `{enum:[...]}` mostrato al modello → non può EMETTERE un id fuori-vocabolario; set vuoto → `z.string()` fallback; **cast-free**, niente `any` al confine). **`spawn_npc` resta a record APERTI** (NO enum): verificato empiricamente che `z.record(z.enum)` rende il JSON con TUTTE le chiavi `required` (ingannevole) → le chiavi le valida il motore. `masterToolDefs(phase, vocab)`/`resolveToolCall(name, args, vocab)` consumano il vocab (single source, niente divergenza JSON↔validazione). **`renderL1` (`memory`) espone attributi/abilità per-attore** (`risorse:`/`attr:`/`abil:` labeled, solo se presenti) → il modello copia id reali. **Seme dev `devRuleset`** (foglio fantasy stand-in) in `@loomn/host`, iniettato in `createCampaignService` + `app/desktop` main; il Piano 11 cambia solo la sorgente.

**Strategia anti-ripple (lezione SP4, applicata):** il ripple della firma di `decide` (42 call-site solo in `commands.test.ts`) è stato **confinato nel Task 3** (threading behavior-preserving) dando a ogni file di test un vocabolario largo con `defaultResources` VUOTO → i Task 4-6 (validazioni + auto-fill) sono diventati puramente additivi, suite verde a ogni task. **Verifica empirica del feedback di review:** accolto l'hardening su rami reali (test di rifiuto skill di Attack, Task 5; messaggio `requireMember` gender-neutral "fuori vocabolario" — i `kind` sono femminili; test e2e combat-ready via `devRuleset` reale nel servizio, final review); declinati con motivazione i gold-plating (`RulesetInput` named per un tipo inline a 2 campi; rami irraggiungibili `(nessuno)`; `events[0]?.type` ottico).

**Deferral dichiarati (nessun debito silenzioso):** validazione degli identificatori DENTRO gli effetti degli oggetti (`defenseModifier.defense ∈ vocab.defenses`) → appartiene alla traccia **Inventario** (oggi gli oggetti non vengono dall'AI, `spawn_npc` mappa `items:[]`); vocabolario delle zone in `start_encounter`; vocabolario di condizioni/status (Piano 11); `Ruleset` comportamentale oltre `dcForDifficulty` (formule di difesa, hook); richiedere a un check di nominare ≥1 stat; il sistema di moduli completo (Piano 11).

**[⚠️ SUPERATO — F3/G5 FATTO, vedi §0-terdecies]** Prossimo *era* F3/G5; ora è fatto e mergiato (`0acbff0`).

### 0-terdecies. Aggiornamento 2026-06-17: F3/G5 (coercizione strutturale write-path Reflection) fatto → prossima = segmentazione `reflect` (item 6, ULTIMO del backlog pre-Piano 10)

**F3/G5 — coercizione strutturale sul write-path della Reflection ✅ FATTO e mergiato in main** (ff a `0acbff0`; HEAD ora `0acbff0`; spec `specs/2026-06-17-f3g5-coercizione-write-path-design.md`, piano `plans/2026-06-17-loomn-f3g5-coercizione-write-path.md`). **427 test verdi** (422 → 427: +5 in `host/reflection-ports.test.ts`), typecheck pulito (6 progetti). Processo: brainstorming (scope A + arch ii decisi con l'utente) → spec (commit su main `ef74422` + emendamento `703875c`) → writing-plans (commit su main `b9ce5a3`) → branch `feat/f3g5-coercizione-write-path` → subagent-driven (2 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus = Ready to merge: Yes**, zero Critical/Important) → merge ff → branch cancellato.

**Diagnosi (il valore del brainstorming):** i 3 livelli dello `StructuredOutputPort` (7b: function-call → json_schema → repair) gestiscono *robustezza sintattica* + *fallback di capacità del provider*, NON la *coercizione di tipo*. Il fallimento empiricamente-vivo dopo F4 era **G5a strutturale**: il modello debole stringifica `facts` (`"[{...}]"`) → `Expected array, received string` al livello function-call → degrado morbido a repair. È **lo stesso vizio di G6** (`participants` stringificato), ma `llmArray` viveva **solo sul tool-path**. **G5b** (qualità semantica, il vecchio blob nei campi) era **già smussato da F4** → fuori scope dichiarato.

**Decisione di design (con l'utente):** **scope (A) solo strutturale** (scartati: B prompt-tuning semantico non-verificabile-in-CI; C modello-più-forte che contraddice l'acceptance "estrazione robusta su modello debole") + **architettura (ii)** helper coercivi condivisi. I due giudizi presi con l'utente: **(a)** coercio i pattern *osservati* (array via `llmArray`, numeri via `coerceNumericString`) ma NON `functional: z.boolean()` (booleani-come-stringhe mai osservati → niente `llmBool`, YAGNI); **(b)** acceptance via **unit-test** (forme malformate), ri-validazione spike LM Studio **opzionale/fuori-banda** (non gira in CI).

**Cosa è stato fatto (ai + host; `shared`/engine/memory/`structured-output`/`master-turn` INTATTI):** **Task 1** — estratti `coerceNumericString`/`llmNumber`/`llmArray`/`llmInt` da `master-tools.ts` in un nuovo `packages/ai/src/coercion.ts` condiviso (behaviour-preserving byte-per-byte; `enumOrString` resta in `master-tools.ts` — è vincolo-vocabolario, non rescue), re-export dal barrel `index.ts`; i 52 test di `master-tools.test.ts` (inclusa la guardia di trasparenza G6) sono la rete del refactor. **Task 2** — `reflection-ports.ts` (`@loomn/host`) importa `llmArray`/`coerceNumericString` da `@loomn/ai`; `factsResultSchema.facts` → `llmArray(z.array(...))`; `importance` (in `extractedFactSchema` **e** `sceneDraftSchema`) coerciuto via un `importanceSchema` condiviso (`z.preprocess(coerceNumericString, z.number().int().min(1).max(10))`, DRY); `functional` invariato. +5 test (3 positivi coercizione, 2 strict via `strategies:['function-call']` per pinnare il gate dello schema).

**Decisione tecnica notevole (cast forzati dal contratto 7b):** `z.preprocess` produce un `ZodEffects` con input statico `unknown`, NON assegnabile a `StructuredOutputRequest<T>.schema: z.ZodType<T>` (input=output=T) di `structured-output.ts` — **fuori scope, non si tocca**. Quindi **2 cast `as z.ZodType<...>` sono load-bearing** (`importanceSchema` foglia, `factsResultSchema` per via di `llmArray` al top), documentati e runtime-safe (Zod valida). Un **3° cast su `sceneDraftSchema` era ridondante** (verificato empiricamente in code-review: `importanceSchema` è già pulito → l'oggetto resta input=output) → **rimosso** (`0acbff0`), con commento sull'asimmetria. La trasparenza dello schema JSON a `zodToJsonSchema` è **ereditata** dalla guardia G6 di `master-tools.test.ts` (il write-path riusa l'helper identico) → niente test host ridondante né nuova dep `zod-to-json-schema` su host.

**Verifica empirica del feedback di review (working-style §5.2):** accolto (verificato empiricamente, allineato all'analisi indipendente dell'orchestratore) il finding del cast ridondante su `sceneDraftSchema` → rimosso; declinato il Minor cosmetico della final review (commento `llmNumber` più snello nell'estrazione = un miglioramento, non un difetto).

**Deferral dichiarati (nessun debito silenzioso):** G5b qualità semantica dell'estrazione (blob/terne mal-comprese) → follow-up F4-adiacente (few-shot o modello più capace, **NON** coercizione di tipo); `llmBool`/coercizione booleana → additivo se mai osservato; ri-validazione empirica con LM Studio → fuori-banda opzionale; forzare/riordinare il constrained-decoding per la Reflection → inutile (la coercizione fa riuscire il function-call).

**[⚠️ SUPERATO — item 6 FATTO, vedi §0-quaterdecies]** Prossimo *era* = segmentazione `reflect` per scena (item 6 di §0-quinquies, ULTIMO del backlog pre-Piano 10). Oggi `reflect(scope)` riflette l'intero stream come una scena → una seconda `reflect` collide sugli id deterministici (`f-<from>-<to>-<i>`, `s-scene-<from>-<to>` in `reflection.ts`) → `UNIQUE constraint failed`. ORA HA IL SUBSTRATO: gli eventi `PhaseChanged` di SP4 danno i confini di scena naturali da cui segmentare/tracciare i range già riflessi. *Acceptance:* riflessioni ripetute su scene successive non collidono. È **engine/memory, medio**. **NON strettamente design-first** (è un bug di correttezza e i confini di scena sono già dati da SP4) → si può partire da `writing-plans`, MA valuta un brainstorming breve se emergono scelte di design sui range già-riflessi da **persistere** (oggi nulla traccia cosa è stato riflesso). Dopo l'item 6 → **backlog pre-Piano 10 CHIUSO** → **Piano 10 (UI, design-first)**. Pianificata anche la **feature core Inventario & Equipaggiamento** (§8): all'apertura, DESIGN-FIRST → brainstorming.

### 0-quaterdecies. Aggiornamento 2026-06-17: item 6 (segmentazione `reflect` per scena) fatto → BACKLOG PRE-PIANO 10 CHIUSO → prossimo = Piano 10 (UI)

**Item 6 — segmentazione `reflect` per scena ✅ FATTO e mergiato in main** (ff a `2607342`; HEAD ora `2607342`; spec `specs/2026-06-17-item6-segmentazione-reflect-design.md`, piano `plans/2026-06-17-loomn-item6-segmentazione-reflect.md`). **444 test verdi** (427 → 444: +5 `segmentScenes`, +3 cursor, +1 atomicità `runReflection`, +6 `runScenesReflection`, +2 wiring host), typecheck pulito (6 progetti). **Conclude il backlog pre-Piano 10 (§0-quinquies): G6/F2/SP1-4/G3-G4/F3-G5/item 6 tutti fatti e mergiati.** Processo: brainstorming breve (una forcella di design reale decisa con l'utente) → spec (commit su main `2fb58df`) → writing-plans (commit su main `ba8476f`) → branch `feat/item6-segmentazione-reflect` → subagent-driven (5 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus = Ready to merge: Yes**, zero Critical/Important) → merge ff → branch cancellato.

**Il bug:** `reflect(scope)` rifletteva l'**intero stream** come una scena → coniava id deterministici **globali** (`f-<from>-<to>-<i>` PK di `canon_facts`, `s-scene-<from>-<to>` PK di `summaries`) su `min..max` seq → una seconda `reflect` su un range sovrapposto → `UNIQUE constraint failed` (riprodotto nello spike).

**Decisione comportamentale (con l'utente):** `reflect` diventa **incrementale + segmentato per scena + flush della scena aperta**. Un **watermark** traccia fino a che `seq` lo stream è stato riflesso; `reflect` processa solo `seq > watermark`, segmenta ai confini `PhaseChanged` (substrato SP4 §0-undecies 4b), riflette ogni scena (range per-scena → id unici), avanza il watermark. La coda dopo l'ultimo `PhaseChanged` (scena aperta) viene riflessa (flush) → regge gli stream **senza** `PhaseChanged` (sessione mono-fase / scenario spike).

**Decisione di persistenza (con l'utente, "la più professionale / senza debiti / meno pigra"):** **cursor esplicito** = tabella-proiezione `reflection_cursor` (riga singleton). Scartati: **(B)** derivare il watermark da `summaries` (`max(eventSeqTo)`) — debito latente: il futuro **rollup L2** (scena→sessione→arco) compatterebbe i riassunti di scena → il watermark regredirebbe → ri-collisione; **(C)** id-unici-per-costruzione + skip idempotente — si auto-confuta sulla scena aperta (cresce a ogni chiamata → mai "già fatta" → fatti/summary orfani). Cursor **singleton (uno per stream), NON per-scope**: gli id sono globali → un cursor per-scope si resetterebbe cambiando lo `scope` tra sessioni → collisione; lo `scope` resta l'etichetta dei summary. **Gli id NON si toccano** (il watermark garantisce range mai ri-coperti).

**Layering (zero debito, dichiarato):** il watermark è una **proiezione `memory`, NON un evento di dominio** (la Reflection è write-path async; ES solo in Campaign/World); **niente entità `Scene`** (i `PhaseChanged` sono la sorgente unica dei confini → niente duplicazione). Blast radius confinato a **`memory` + `host`**: `@loomn/shared` (ipc/commandSchema/domain-schema), `engine`, `ai`, `app/desktop` **intatti**; `ReflectOutcome` invariato (`{factCount, summarized}`) → **contratto IPC non toccato** (verificato dalla final review).

**Cosa è stato fatto (memory + host):** **memory** — `scene-segmentation.ts` (`segmentScenes` PURA: spezza ai `PhaseChanged`, il marker chiude la scena, coda aperta flush, vuoto→`[]`, range contigui non sovrapposti); `reflection-cursor.ts` (`ReflectionCursor {get/set}` + `createReflectionCursor`, gemello di `canon-ledger`/`summary-store`, riga singleton id=0) + tabella `reflection_cursor` (`schema.ts`) + **migrazione `0004` scritta a mano** (CREATE + seed riga a 0, journal 5ª entry `when` congelato `1750000000004`); **reorder behaviour-preserving di `runReflection`** (entrambe le `await` LLM PRIMA delle scritture → scena **atomica** contro il fallimento LLM, output identico, 6 test originali invariati); `runScenesReflection` (orchestratore: legge cursor → `seq>cursor` → `segmentScenes` → `runReflection` per scena → **avanza il cursor DOPO ogni scena = crash-safe**: un fallimento a metà lascia le scene committate + il cursor lì, il retry riprende senza ri-riflettere né collidere). **host** — `MemorySystem.cursor` montato sulla connessione condivisa; `reflectionDepsFor` → `ScenesReflectionDeps` (+`cursor`); `campaign-service.reflect` usa `runScenesReflection` e aggrega (`factCount` somma, `summarized` some). **Bonus:** L2 ottiene **un riassunto per scena** (granularità corretta) invece di uno per l'intero stream.

**Verifica empirica del guard (working-style §5.2):** il test di non-collisione del servizio è stato **rafforzato** dopo un'auto-segnalazione onesta dell'implementer: la prima versione (eventi aggiunti TRA le due `reflect`) passava anche sul vecchio codice (il range cambiava) → non era un vero guard. Ristretto a "seconda `reflect` sullo stesso stream senza nuovi eventi" e **verificato RED sul vecchio codice** (`UNIQUE constraint failed: canon_facts.id`), GREEN sul nuovo. Finding di review accolti (commenti stantii in `reflection-ports.ts`/`campaign-service.ts` allineati a `ScenesReflectionDeps`/`runScenesReflection`/comportamento segmentato); declinato il finding cosmetico (assertion `factCount` ridondante sul guard) e il commento sul guard `noUncheckedIndexedAccess` (idioma standard del progetto).

**Deferral dichiarati (nessun debito silenzioso):** transazione DB intra-scena oltre il reorder LLM (il caso realistico — fallimento LLM — è già coperto; un fallimento sincrono fra scritture è catastrofico, non un retry); filtro semantico delle scene meccaniche (territorio F3/G5); rollup L2 (il cursor esplicito è scelto **proprio** per non accoppiarvisi); `sceneCount` nell'esito/IPC (additivo, quando la UI lo userà).

**Prossimo = Piano 10 — UI** (il **backlog pre-Piano 10 è CHIUSO**). ⚠️ **NON partire da `writing-plans`:** prima studio/design — `superpowers:brainstorming` + `frontend-design`, **decidendo l'approccio UI con l'utente**; aggancio `ReadModel={version,state}` + i canali IPC del 9c-ii (Pinia sul read side); dadi 3D `@3d-dice/dice-box` a risultati deterministici; chat / scheda PG / journal / gestione provider. Pianificata anche la **feature core Inventario & Equipaggiamento** (§8, design-first all'apertura). Follow-up minori ancora aperti (§7-quinquies): seed RNG per-campagna persistito; delta read-model.

### 0-quindecies. Aggiornamento 2026-06-17: STUDIO PIANO 10 (UI) CHIUSO → spec committato → prossimo = scrivere il Piano 0 (IPC/CQRS completeness)

**Studio design-first del Piano 10 ✅ CHIUSO** con l'utente (`superpowers:brainstorming` + `frontend-design`). **Spec di design = AUTORITÀ**, committato su main (`5530aaf`): `docs/superpowers/specs/2026-06-17-piano10-ui-design.md`. Prototipo dark navigabile committato (approvato come direzione visiva): `docs/superpowers/prototypes/piano10/play-shell.html` (server `server.cjs` :5178, `.claude/launch.json` name `loomn-proto`; d20 Three.js UMD **r0.149** via jsDelivr — r150+ NON ha UMD min; preview/screenshot via Claude Preview MCP). Memoria dedicata: `loomn-piano10-design.md`.

**⚠️ NON ripartire da `brainstorming`/`frontend-design`: lo studio è CHIUSO.** Lo spec è l'autorità. La **prossima azione è `superpowers:writing-plans` per il Piano 0**.

**Decisioni bloccate (sintesi; dettagli nello spec §2):** (1) layout C-frame/B-panels **adattivo alla fase** (`GameState.phase`); (2) design language **"strumento notturno"** (dark graphite, pannelli arrotondati/distanziati ovunque, accento brass per-fase, serif **Newsreader** narrazione + grotesk **Archivo** UI + mono **JetBrains Mono** numeri/dadi, **Fraunces** display, dadi WebGL; **bespoke token-driven** a variabili CSS, NO AI-slop); (3) **Reka UI headless + design system bespoke, NO Tailwind**, NO librerie opinate; (4) IA **ibrida** (Gioco home + route Diario/Scheda/Compagnia/Impostazioni; campaign-picker **deferito**; first-run provider); (5) inventario nella **Scheda**, **data-driven dal `Ruleset`**, render del modello piatto `Item{equipped}` per ora; (6) **copertura TOTALE** (UI su TUTTO il backend esistente); (7) pannelli Gioco **riposizionabili/ridimensionabili** (`grid-layout-plus`, preset per fase, layout **persistito nelle UI settings**); (8) **`commandSchema` esteso all'unione `Command` completa**; (9) verifica = TDD logico (**Vitest + Vue Test Utils**) + component selettivi + **self-test `LOOMN_SELFTEST` esteso** + screenshot (NO Playwright); (10) **streaming del turno DEFERITO** (fast-follow).

**Audit di binding (verificato sul codice, spec §7):** `commandSchema` (shared) copre solo **5 Command** (AddActor/StartEncounter/EndTurn/NextRound/Attack); i Command SP1-4 (RequestCheck/ApplyEffect/StartQuest/AdvanceQuest/EnterPhase/EndEncounter) **esistono in engine ma NON sull'IPC**; `CampaignService` ritorna gli `events` (coi `RollResult`) ma l'handler IPC li **scarta** (`{narration,version}`); canon ledger e summaries L2 **non** attraversano l'IPC; equip/movimento **non** sono Command/Event.

**⚠️ IL READ-LAYER (per il futuro) È GIÀ INDIRIZZATO = PIANO 0.** La UI ha bisogno di read oggi non esposti sull'IPC + del write esteso → **Piano 0 — IPC/CQRS completeness**, backend **testabile** (host + shared + handler IPC sottili), flusso §4, **prerequisito** dei piani UI:
- **write:** `commandSchema` = unione `Command` completa (additivo, cast-free, drift-guard).
- **read:** `events` (coi tiri `RollResult`) nei risultati di `dispatch`/`run-turn` (validati `domainEventSchema`) + **canali read** per storia narrazione (`NarrationRecorded`), canon ledger (`ledger.active()/all()`), riassunti L2 (`summaries.list()`), query on-demand/paginate (non gonfiano il push).
- **NIENTE streaming** (deferito); **delta read-model** (§13) deferito.

**Decomposizione (spec §10), ordine `0 → 10a → 10g → 10f → 10b → 10c → 10d → 10e`:** **0** IPC completeness · **10a** Fondamenta UI (design system + Reka + Pinia read-side ← `read-model-push` + router shell + frame adattivo + grid-layout-plus + persistenza layout) · **10f** Impostazioni/provider + first-run + creazione PG + controlli GM · **10b** Gioco (turno+narrazione+dadi, spike `@3d-dice/dice-box`) · **10c** Combattimento (cockpit) · **10d** Scheda+inventario (piatto, data-driven) · **10e** Diario+Compagnia.

**Prossima azione concreta del prossimo agente:** verifica setup (`git status` pulito su `main` HEAD `5530aaf`+, `pnpm test` **444** verdi, `pnpm -r typecheck` pulito; se SQLite fallisce con ABI 146→137 → `pnpm -r rebuild better-sqlite3`) → leggi lo spec `specs/2026-06-17-piano10-ui-design.md` → **`superpowers:writing-plans` per il Piano 0** → flusso §4 (commit doc su main → branch → subagent-driven → finishing-a-development-branch → aggiorna HANDOFF/memoria). I sotto-piani UI (10a-10e) seguono lo stesso flusso; il Piano 10 è grande → split confermato (stile 7/8/9).

**Deferiti espliciti (spec §11):** streaming turno; multi-campagna (picker); motore Inventario profondo; equip/movimento come Command/Event; relazioni strutturate; delta read-model (§13).

### 0-sexdecies. Aggiornamento 2026-06-18: Piano 0 (IPC/CQRS completeness) FATTO → prossimo = Piano 10a (Fondamenta UI)

**Piano 0 — IPC/CQRS completeness ✅ FATTO e mergiato in main** (ff a `614a6bb`; HEAD ora `614a6bb`; piano `plans/2026-06-17-loomn-piano0-ipc-cqrs-completeness.md`, committato su main a `ad7222b`). **476 test verdi** (444 → 476), typecheck pulito (6 progetti). **Gate "esegui l'app" SUPERATO** (2 fasi `VERDICT: PASS`, coi nuovi check IPC inclusi). Processo §4: writing-plans → commit doc su main → branch `feat/piano0-ipc-cqrs-completeness` → subagent-driven (5 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus 'Ready to merge: Yes'** zero Critical/Important) → gate app (`rebuild:electron` → 2 lanci electron → `rebuild:node`) → merge ff → branch cancellato.

**Decisione di design (con l'utente): paginazione cursor-by-seq** per i canali read (`{before?,limit?}` → `{entries,hasMore}`, newest-first, stabile sotto append); canon/L2 interrogati per filtro (sono limitati).

**Cosa è stato fatto (additivo, cast-free; `@loomn/shared` resta FOGLIA):**
- **WRITE** — `commandSchema` (`shared/domain-schema.ts`) esteso da 5 a TUTTE e 11 le varianti `Command`: aggiunti RequestCheck/ApplyEffect/StartQuest (con `.transform()` che OMETTE gli opzionali assenti, cast-free sotto `exactOptionalPropertyTypes`, pattern di `attackCommandSchema`) + AdvanceQuest/EnterPhase/EndEncounter (inline); nuovo `softPhaseSchema`. Drift guard cast-free wire→motore in `packages/host/src/command-schema.test.ts` (`const c: Command = commandSchema.parse(...)` per i 6 nuovi, verificato dal typecheck host — `shared` non può importare engine).
- **READ (events)** — `dispatchResultSchema`/`runTurnResultSchema` (`shared/ipc.ts`) hanno ora `events: z.array(domainEventSchema)` nell'arm ok (coi `RollResult` dentro AttackResolved/CheckResolved/ResourceEffectApplied); l'handler IPC del 9c-ii li scartava, ora li espone (`out.events`).
- **READ (canali on-demand)** — 3 nuovi canali invoke/handle (`narrationHistory`/`canon`/`summaries`) con schemi richiesta/esito + 3 metodi `LoomnBridge`. Sul servizio (`host/campaign-service.ts`): `getNarrationHistory(query?)` (legge lo stream, filtra `NarrationRecorded`, paginazione cursor-by-seq newest-first con `hasMore`), `getCanon(query?)` (ledger `active`/`all` + filtro + `includeRetracted`), `getSummaries(filter?)` (`summaries.list`). **Read SINCRONI, FUORI dalla coda FIFO** (la coda serializza solo le mutazioni; i read sono su stato SQLite committato → vista coerente anche durante un turno async). I DTO canon/summary (shared) rispecchiano i tipi `@loomn/memory`; l'assegnabilità `CanonFact[]`/`Summary[]`→DTO è imposta a compile-time dall'handler del main (è il drift guard read).
- **Wiring Electron** (`app/desktop`): handler IPC sottili per i 3 canali + `events` nei result dispatch/run-turn; bridge preload; **self-test `LOOMN_SELFTEST` esteso** (fase 1: events nel dispatch + i 3 canali read vuoti a inizio).

**Fuori ambito (deferito, dichiarato):** streaming del turno; delta read-model (§13); equip/movimento come Command/Event; slot inventario profondi; relazioni strutturate. Nessun nuovo Command/Event/tabella; nessuna modifica a `package.json`/`tsconfig`/`vitest.config`/`electron.vite.config`.

**Verifica empirica del feedback di review (working-style §5.2):** accolti come hardening reale — test di rifiuto `StartQuest senza title` (consistenza coi sibling), `dice` non-vuoto nel rifiuto `ApplyEffect` (chiarezza del segnale), vincoli `.int().nonnegative()` su `createdAt`/`eventSeqFrom`/`eventSeqTo` di `summarySchema` (consistenza con `canonFactSchema`), test dei bound dei cursori (`before:0`, `level` invalido), rinomina `window`→`page` in `getNarrationHistory` (no shadowing del globale). Declinati con motivazione (cosmetici): `!hasMore` vs `=== false` (entrambi gli stili già coesistono nel self-test), read channel solo in fase 1 del self-test (by-design, stateless al DB pulito). **Plan gap risolto dall'implementer:** sotto `exactOptionalPropertyTypes` i `parsed.data` Zod (`T | undefined`) non sono assegnabili ai param exact-optional del servizio → adattati negli handler con lo spread condizionale (pattern di `toLanguageProviderConfig`; spec-review ha verificato field-by-field che nessun filtro viene perso, incl. `object` aliasato per la parola riservata JS).

**Nota operativa (lezione del gate):** non eseguire un subagent che lancia `pnpm test` (→ ABI Node) mentre un `rebuild:electron` (→ ABI Electron) è in corso: condividono il binario nativo `better-sqlite3` nello store pnpm → vanno **serializzati**. Il gate Piano 0 ha avuto una corsa innocua tra il `rebuild:node` della final review e il `rebuild:electron` di background; risolto ri-eseguendo `rebuild:electron` serializzato prima dei lanci electron, poi `rebuild:node` + `pnpm test` (476 verdi) a chiusura.

**⚠️ Lacuna backend scoperta per la UI data-driven (prerequisito di 10f/10d, NON di 10a): il vocabolario del `Ruleset` non attraversa l'IPC.** Il `devRuleset` (attributi/abilità/risorse/difese + `defaultResources`) è iniettato in `createCampaignService` nel main e **non è esposto** (il read-model push è `{version, state: GameState}` — il vocabolario è la *lente*, non lo stato, decisione G3/G4: `applyEvent`/`rebuild` non prendono il ruleset; `get-status` porta solo i flag diagnostici). Il renderer importa solo `@loomn/shared` (non engine) → **non può conoscere gli id legali**. Serve a: **10f** (creazione PG + controlli GM: popolare i form/dropdown coi valori legali di `RequestCheck`/`ApplyEffect`/`Attack` — altrimenti l'utente invia un id che il motore rifiuta), **10d** (Scheda+inventario "data-driven dal Ruleset", decisione 5 dello spec). Companion minore: gli enum di comando statici (`DIFFICULTIES`/`SOFT_PHASES`/`QUEST_OUTCOMES`/`restore|drain`) servono ai form GM e non sono esportati da `@loomn/shared`. **Soluzione (stile Piano 0, additiva, testabile su ABI Node, `shared` resta foglia):** canale read `get-ruleset` con DTO `{vocabulary:{attributes,skills,resources,defenses,defaultResources}, difficulties, softPhases, questOutcomes, directions}` (da `deps.ruleset.vocabulary` + gli enum engine) → metodo sincrono su `CampaignService` (come i read del Piano 0) + handler IPC sottile + bridge + self-test. **NON blocca 10a** (dipende solo dal Piano 0). **Deciso con l'utente: è una FASE DEDICATA del Piano 10 — `10g` — da eseguire PRIMA di 10f** (ordine `10a → 10g → 10f → 10b → 10c → 10d → 10e`; resequenziabile anche prima di 10a perché non dipende da 10a). Vedi lo spec del Piano 10 §10 (riga 10g) e §8. **Altre piccole lacune/rischi scoperti (oltre a 10g):** **(A, 10f)** `get-status` non espone `baseUrl`/`model` correnti → il form Impostazioni non li pre-compila; estenderlo con `provider?:{baseUrl,model}` (mai la chiave) o un `get-provider` (config app, NON in `get-ruleset`). **(F, dentro 10g)** esporre anche `commandPhaseRules` (da `isCommandLegalInPhase`) per disabilitare i comandi GM illegali nella fase corrente. **(C, spike 10b / font in 10a)** `@3d-dice/dice-box` (wasm+worker) e i font letterari vanno bundlati offline → validare bundling asset + CSP/`sandbox` nello spike (eventuale passo orchestratore su CSP/`electron.vite.config`). **(D, 10a)** mantenere scriptabile `LOOMN_SELFTEST` quando il renderer diventa la vera app Vue (il gate deve restare `VERDICT: PASS`). **By-design/deferiti (NON lacune):** GM Attack `defenseBase` grezzo (band-model deferito), movimento/equip display-only, streaming (latenza turno tutto-in-una-volta), tiri storici non nel Diario, reset/nuova campagna, snapshot-non-delta (§13), stat "effettive" ricalcolate lato UI. **Verificato che NON è una lacuna:** l'ordine di turno del combat è derivabile dal read-model (`encounter.participants` pre-ordinato per iniziativa, `turnIndex` diretto). Resto del backend: **completo** per la UI pianificata (read-model+`phase`, `run-turn`, `events`/`RollResult`, narrazione/canon/L2, `commandSchema` completo, provider/status, `reflect`).

**Prossimo = Piano 10a — Fondamenta UI** (ordine sotto-piani `10a → 10g → 10f → 10b → 10c → 10d → 10e`; il vocabolario sopra è prerequisito di 10f, non di 10a): design system bespoke "strumento notturno" (token CSS) + Reka UI headless + Pinia read-side ← `read-model-push` + Vue Router shell (rail/topbar/route) + frame adattivo alla fase + contenitore `grid-layout-plus` + persistenza layout nelle UI settings (**decidere il meccanismo**: localStorage del renderer vs nuovo store UI-settings nel main — concern di 10a). Ogni piano = flusso §4 (writing-plans → commit doc su main → branch → subagent-driven → finishing-a-development-branch → aggiorna HANDOFF/memoria). Autorità di design: `specs/2026-06-17-piano10-ui-design.md` (§10 decomposizione) e [[loomn-piano10-design]]. Follow-up minori ancora aperti (§7-quinquies): seed RNG per-campagna persistito; delta read-model (§13).

### 0-septdecies. Aggiornamento 2026-06-18: Piano 10a (Fondamenta UI) FATTO → prossimo = Piano 10g (vocabolario su IPC)

**Piano 10a — Fondamenta UI ✅ FATTO e mergiato in main** (ff a `071e71c`; HEAD ora `071e71c`; piano `plans/2026-06-18-loomn-piano10a-fondamenta-ui.md`, committato su main a `b7fc19c`). **515 test verdi** (476 packages + **39 renderer**, nuovo progetto Vitest jsdom), typecheck pulito (6 progetti). **Gate "esegui l'app" SUPERATO** (2 fasi `VERDICT: PASS` su Electron, ora coi check store-Pinia-riflette-il-push + router-naviga). Processo §4: writing-plans → commit doc su main → branch `feat/piano10a-fondamenta-ui` → subagent-driven (Step 0 orchestratore + 6 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus 'Ready to merge: Yes'** zero Critical/Important) → gate app → merge ff → branch cancellato.

**Decisione di design (con l'utente): persistenza layout = localStorage del renderer dietro un port `LayoutPersistence`** (non lo store UI-settings nel main). Razionale: il layout è preferenza di vista pura (non dominio/event store) → lo possiede il renderer; zero nuova superficie IPC; isolato dietro un'interfaccia → migrabile lato main (multi-finestra/backup) senza toccare i call site. Stesso pattern "isolato-e-migrabile" di `dcForDifficulty`.

**Cosa è stato fatto (renderer-only in `app/desktop`; `@loomn/shared`/engine/host INTATTI; nessun nuovo Command/Event/IPC):**
- **Step 0 (orchestratore, non subagent):** deps UI in `app/desktop/package.json` (**pinia 3, vue-router 4** [NON 5: la 5 richiede Vite 7/8 + `@pinia/colada`, electron-vite 4 usa Vite 5.4 → incompatibile], **reka-ui 2, grid-layout-plus 1, @fontsource-variable** {fraunces/newsreader/archivo/jetbrains-mono}; devDep vitest/@vue/test-utils/jsdom); **`vitest.workspace.ts`** (root) compone il progetto packages (node, `./vitest.config.ts`) + il nuovo `app/desktop/vitest.config.ts` (jsdom + plugin-vue) → `pnpm test` esegue ENTRAMBI (**⚠️ dalla ROOT**: da `app/desktop` gira solo il progetto renderer); CSP del renderer estesa (`style-src 'self' 'unsafe-inline'` per gli stili iniettati a runtime da Vue/Reka, `font-src 'self' data:` per i woff2 bundlati offline, `connect-src ws://localhost` per l'HMR dev; `script-src 'self'` INVARIATO).
- **Store Pinia read-side** (`stores/read-model.ts`): `useReadModelStore` tiene `{version, state}` dal `read-model-push`; **`applyPush` è l'unica scrittura** (CQRS, il renderer non muta lo stato); selettori `loaded`/`phase`/`actors`/`pcs`/`npcs`/`quests`/`encounter`/`inCombat`; tipi di vista derivati da `@loomn/shared` `ReadModelPush` (**NIENTE import `@loomn/engine`**); `state` privato (non esposto, incapsulamento da review).
- **Layout** (`layout/presets.ts` + `layout/persistence.ts`): `presetFor(phase)` (preset per fase — combat=cockpit con scontro, non-combat condivisi), port `LayoutPersistence` + adapter localStorage (storage iniettabile, `isLayout` rifiuta JSON corrotto/array vuoto/coord non finite), `resolveLayout` (override persistito o preset). Composable `composables/use-game-layout.ts` (risolve per fase, ri-risolve al cambio fase, persiste su `@layout-updated`).
- **Design system** (`styles/tokens.css` + `base.css` + `index.ts`; `components/Loomn*.vue`): token graphite + accento brass per-fase via `[data-phase]` (combat→clay, dialogue→oro, downtime→slate; bordi via `color-mix(var(--accent))` → fase-safe); font offline; `LoomnPanel`/`LoomnButton` + wrapper Reka headless `LoomnDialog`; token `--well`.
- **Shell** (`router/index.ts` + `views/*.vue` + `App.vue`): Vue Router **hash history** (per `file://`, iniettabile per i test), 5 route (Gioco/Diario/Scheda/Compagnia/Impostazioni; Compagnia legge il roster dallo store, le altre skeleton); `App.vue` = rail (`exact-active-class` per non avere due link attivi) + topbar + `<RouterView>` + `data-phase` legato a `store.phase` (frame adattivo).
- **Gioco** (`views/GameView.vue`): `grid-layout-plus` con binding **controlled** `:layout` one-way + `@layout-updated` (verificato dalla sorgente della libreria che `v-model` è inerte a col-num fisso; il composable è l'unico write-path); pannelli fondazionali (narrazione/scheda|scontro/dadi, contenuto profondo nei sotto-piani).
- **Bootstrap** (`renderer.ts`): createApp + pinia + router + stili, store sottoscritto a `onReadModelPush`; **self-test `LOOMN_SELFTEST` evoluto** sulla vera app Vue (asserisce store-riflette-push + router-naviga, oltre ai check IPC del Piano 0).

**Note operative (lezioni):** **`grid-layout-plus@1.1.1` NON ha `dist/style.css`** (CSS injected-by-JS via l'import del componente) → l'import esplicito del CSS è stato rimosso (la CSP `style-src 'unsafe-inline'` copre l'iniezione). **`pnpm test` va lanciato dalla ROOT** (la workspace compone i due progetti; da `app/desktop` gira solo il renderer). Verifica visiva fatta con un **harness throwaway** (Vite standalone + Claude Preview MCP, NON committato): exploration brass (narrazione/scheda/dadi), combat clay (scontro al posto della scheda) → re-theme + swap confermati, zero errori console, DOM full-viewport corretto.

**Verifica empirica del feedback di review (working-style §5.2):** accolti come hardening reali — incapsulamento di `state`; `isLayout` strict (array vuoto/non-finiti); `exact-active-class`; bordi accento fase-safe (`color-mix`); emit record-style Vue 3.3+; binding griglia controlled (dopo aver **letto la sorgente di grid-layout-plus** e refutato la premessa del reviewer del "doppio-write su drag" — `update:layout` scatta solo su col-num/breakpoint, fissi qui); token `--well` per l'ultimo hex duplicato. Declinati con motivazione (gold-plating): token font-size inesistente, `v-model:open` su DialogRoot (YAGNI), tokenizzazione di hex bespoke senza token esatto.

**Prossimo = Piano 10g — Vocabolario di gioco su IPC** (canale read `get-ruleset`, stile Piano 0): espone il vocabolario del `Ruleset` (attributi/abilità/risorse/difese + `defaultResources`) + gli enum di comando statici (`DIFFICULTIES`/`SOFT_PHASES`/`QUEST_OUTCOMES`/`restore|drain`) + `commandPhaseRules` (da `isCommandLegalInPhase`) per i pannelli **data-driven** (creazione PG, controlli GM in 10f, Scheda in 10d). Metodo sincrono su `CampaignService` + handler IPC sottile + bridge + export enum da `@loomn/shared` + self-test; backend testabile su ABI Node, `shared` resta foglia. **Da fare PRIMA di 10f** (ordine `10a → 10g → 10f → 10b → 10c → 10d → 10e`). Vedi spec Piano 10 §10 (riga 10g)/§8 e §0-sexdecies. Follow-up minori ancora aperti (§7-quinquies): seed RNG per-campagna persistito; delta read-model (§13).

### 0-octodecies. Aggiornamento 2026-06-18: Piano 10g (Vocabolario di gioco su IPC) FATTO → prossimo = Piano 10f

**Piano 10g — Vocabolario di gioco su IPC (`get-ruleset`) ✅ FATTO e mergiato in main** (ff a `fa6bc17`; HEAD ora `fa6bc17`; piano `plans/2026-06-18-loomn-piano10g-ruleset-ipc.md`, committato su main a `9680ae2`). **533 test verdi** (515 → 533, +18 packages), typecheck pulito (6 progetti). **Gate "esegui l'app" SUPERATO** (2 fasi Electron `VERDICT: PASS`, coi 4 nuovi check `get-ruleset` inclusi in fase 1, su `devRuleset` reale). Processo §4: writing-plans → commit doc su main → branch `feat/piano10g-ruleset-ipc` → subagent-driven (5 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus 'Ready to merge: Yes'** zero Critical/Important) → gate app → merge ff → **`git push origin main`** (origin a `fa6bc17`) → branch cancellato.

**Cosa è stato fatto (additivo, cast-free, stile Piano 0; `@loomn/shared` resta FOGLIA):** nuovo canale read IPC `get-ruleset` (invoke/handle, **senza payload** come `get-status`) che espone il DTO `{vocabulary:{attributes,skills,resources,defenses,defaultResources}, difficulties, softPhases, questOutcomes, directions, commandPhaseRules:{combatOnly,nonCombatOnly}}`. Il `Ruleset` (`devRuleset`, iniettato in `createCampaignService` nel main) **non attraversava l'IPC** (è la *lente*, non lo stato — il read-model push è `{version,state}`): ora i pannelli data-driven di 10f/10d conoscono gli id legali.
- **ENGINE** (`commands.ts`, commit `0cafefe`): nuovi const `RESOURCE_DIRECTIONS` (`['restore','drain']`, ora referenziato da `ApplyEffect.direction` — type-level, behaviour-preserving) e `COMMAND_TYPES` (lista runtime degli 11 tipi di Command) + tipi `ResourceDirection`/`CommandType` + **guard di esaustivita bidirezionale a compile-time** (`Exclude<...> extends never ? true : never`, silenziato con `void`) che tiene `COMMAND_TYPES` allineato all'unione `Command`. Completano gli enum statici di comando (DIFFICULTIES/SOFT_PHASES/QUEST_OUTCOMES esistevano già).
- **SHARED** (`domain-schema.ts`, `bcac3f8`): esportati i 4 enum come const (`DIFFICULTIES`/`SOFT_PHASES`/`QUEST_OUTCOMES`/`RESOURCE_DIRECTIONS`, **copie proprie** — shared è foglia) per i form GM del renderer; gli schemi privati (`difficultySchema`/`softPhaseSchema`/`questOutcomeSchema`/`direction`) ora ne derivano (single-source nel pacchetto; **verificato empiricamente** che `z.enum` di Zod 3.25 accetta tuple `readonly` → infer identico). **`ipc.ts`** (`64b8e4d`): canale `getRuleset`, `rulesetResultSchema`/`RulesetResult` (union ok/errore; enum-array come `z.array(z.string())`), `getRuleset()` su `LoomnBridge`.
- **HOST** (`campaign-service.ts`, `cc54cfd`+`3f84623`): `getRuleset(): RulesetView` **sincrono, FUORI dalla coda FIFO** (come i read del Piano 0; non legge mai `state`): proietta `deps.ruleset.vocabulary` (Set→array, `defaultResources` deep-copy) + gli enum del motore + `commandPhaseRules` **derivato** da `COMMAND_TYPES.filter(t => isCommandLegalInPhase('combat',t) && !isCommandLegalInPhase('exploration',t))` (e mirror) — **single-source: nessuna lista di comandi hardcoded in host** (un comando combat-only aggiunto al motore compare automaticamente). Drift guard runtime `shared==engine` per i 4 enum in `command-schema.test.ts`.
- **DESKTOP** (`main/index.ts`/`preload`/`renderer.ts`, `fa6bc17`): handler IPC sottile (`{ok:true, ...service.getRuleset()}` = drift guard read host→DTO via `vue-tsc`); bridge preload; self-test `LOOMN_SELFTEST` fase 1 esteso (verifica attributi/risorse/difficolta/regole di fase end-to-end in Electron reale).

**Decisione di design dichiarata:** gli array di enum nel DTO sono `string[]` (NON literal-union tipizzati) — il DTO è trasporto di valori, i tipi precisi vivono nei const esportati da `@loomn/shared`; `commandPhaseRules` è `string[]` (sottoinsiemi derivati, membership-only `.includes`). Alternativa "DTO tipizzato" considerata e **scartata** (accoppiamento non necessario tra vista host e 5 tipi enum del motore).

**Lezione di processo (verifica empirica del feedback, working-style §5.2):** in Task 4 l'implementer aveva risolto un conflitto di ordinamento (mio bug nel piano: il test asseriva l'ordine del set-literal del motore, il `COMMAND_TYPES.filter` produce l'ordine di dichiarazione) **duplicando** in host le liste hardcoded `combatOrder`/`nonCombatOrder` — reintroducendo proprio il debito che il design evitava (un nuovo comando combat-only sarebbe stato silenziosamente omesso dal DTO). **Scartato dall'orchestratore**: fix nella direzione opposta (derivazione `COMMAND_TYPES.filter` single-source + test order-insensitive `[...].sort()`, perché il renderer usa membership). Commit di refactor `3f84623`. Lezione: l'ordine nel DTO è cosmetico (membership), non pinnarlo a una lista duplicata.

**Fuori ambito (deferito, dichiarato):** config provider (`baseUrl`/`model`) NON in `get-ruleset` (è config app → estensione `get-status` o `get-provider`, in 10f); master-tools (`@loomn/ai`) `z.enum(['restore','drain'])` inline NON riconciliato a `RESOURCE_DIRECTIONS` (superficie di validazione separata, YAGNI); tassonomia slot inventario nel Ruleset (feature Inventario §8); nessun nuovo Command/Event/tabella/migrazione; nessuna modifica a `package.json`/`tsconfig`/`vitest.config`/`vitest.workspace`/`electron.vite.config`.

**Prossimo = Piano 10f — Impostazioni/provider + first-run + creazione PG + controlli GM** (form data-driven dal vocabolario+regole di 10g). ⚠️ **HA decisioni di design aperte** → interpella l'utente (brainstorming/AskUserQuestion) PRIMA del piano: **(A)** read-back della config provider corrente (baseUrl/model, **MAI** la chiave) via `get-status` esteso con `provider?:{baseUrl,model}` vs un canale `get-provider` dedicato (lacuna §8/§0-sexdecies punto A); **(B)** UX del first-run (se `providerConfigured===false` guida a Impostazioni prima del primo turno). Lega a: `set-provider`/`get-status` (esteso), `commandSchema` completo (Piano 0), `get-ruleset` (10g, `dispatch(AddActor)` per creazione PG, controlli GM coi comandi disabilitati via `commandPhaseRules`). Flusso §4 (writing-plans → commit doc su main → branch → subagent-driven → finishing-a-development-branch merge ff → **`git push origin main`** → aggiorna HANDOFF/memoria).

### 0-novodecies. Aggiornamento 2026-06-18: Piano 10f (Impostazioni/provider + first-run + creazione PG + controlli GM) FATTO → prossimo = Piano 10b

**Piano 10f ✅ FATTO e mergiato in main** (ff a `bf532f4`; HEAD ora `bf532f4`; spec `specs/2026-06-18-piano10f-impostazioni-firstrun-gm-design.md` committato a `4d5c2d3`, piano `plans/2026-06-18-loomn-piano10f-impostazioni-firstrun-gm.md` a `9110b11`). **570 test verdi** (533 → 570: +3 packages, +34 renderer = 497 packages + 73 renderer), typecheck pulito (6 progetti). **Gate "esegui l app" SUPERATO** (2 fasi Electron `VERDICT: PASS`, coi nuovi check 10f su `devRuleset` reale + `safeStorage` reale). Processo §4: **brainstorming + AskUserQuestion** (decisioni A/B/C con l utente) → writing-plans → commit doc su main → branch `feat/piano10f-impostazioni-firstrun-gm` → subagent-driven (**7 task TDD**, implementer sonnet + spec-review + code-quality-review per task; **final review opus 'Ready to merge: Yes'** zero Critical/zero Important) → gate Electron → merge ff → **`git push origin main`** (origin a `bf532f4`) → branch cancellato.

**Decisioni di design (con l utente; tutte = raccomandazione dell orchestratore, vincolo "professionale, senza debiti"):** **(A)** read-back config provider = **estensione di `get-status`** con `provider?:{baseUrl,model,hasApiKey}` (NON un canale `get-provider` dedicato): un solo canale/round-trip, il provider-status vive gia li (`providerConfigured`); la chiave **non attraversa mai l IPC** (solo `hasApiKey:boolean`). **(B)** first-run = **soft gate guidato**: al boot, se `providerConfigured===false`, `runFirstRun` naviga **una volta** a Impostazioni + banner globale dismissibile con CTA; non e un hard gate (creazione PG/Regia/navigazione restano usabili); il segnale **`canRunTurn`** (= providerConfigured) lo consumera 10b per gate-are il turno. **(C)** IA = **Regia overlay** (slide-over, trigger globale in topbar) + **creazione PG in Compagnia** (rail resta a 5 voci). **Sotto-decisione:** UX chiave API **tri-stato** (anti-footgun): `apiKey` undefined (campo vuoto) = **mantieni** la chiave esistente, `''` = **rimuovi**, stringa = **sostituisci** → ri-salvare per cambiare solo il model NON cancella la chiave.

**Cosa e stato fatto (additivo, `@loomn/shared` resta FOGLIA; renderer importa solo `@loomn/shared`; nessun nuovo Command/Event/tabella):**
- **SHARED** (`ipc.ts`): `statusResultSchema` += `provider?:{baseUrl,model,hasApiKey}` (mai la chiave).
- **DESKTOP main**: `provider-config.ts` (nuovo, **puro, no import electron**) = `resolveStoredKey` tri-stato (cifratura iniettata → testabile su jsdom). `settings.ts`: `saveProviderConfig` tri-stato (legge il ciphertext prior, lo mantiene/rimuove/sostituisce), `loadProviderMeta` (read-back **SENZA decifrare** la chiave; `hasApiKey` = ciphertext presente), `ProviderMeta = NonNullable<StatusResult['provider']>` (read-drift guard); `loadProviderConfig` INVARIATO. `index.ts`: handler `get-status` usa `loadProviderMeta` (**non decifra mai** la chiave in un risultato IPC); `set-provider` dopo il save **rilegge la config effettiva** (`loadProviderConfig()`) e riconfigura l holder → la chiave mantenuta entra nel provider. **Self-test esteso** (read-back, preservazione chiave tri-stato, comando GM EnterPhase; versione fase-2 portata 1→2 per l evento EnterPhase aggiunto).
- **RENDERER**: store `useProviderStatusStore` (get-status reattivo, `canRunTurn`) + `useRulesetStore` (get-ruleset fetch-once); helper **puri** `lib/provider-form.ts` (`buildProviderPayload` tri-stato), `lib/actor-form.ts` (`buildActorId` slug-unico/`buildActor`, tipo `ActorInput = Extract<DispatchCommand,{type:'AddActor'}>['actor']`), `lib/gm-commands.ts` (`isGmCommandEnabled` da `commandPhaseRules`, niente classificazione hardcoded); `views/SettingsView.vue` (form provider, read-back prefill, chiave tri-stato, `<legend>` a11y); `components/FirstRunBanner.vue` + `composables/use-first-run.ts` (soft gate, redirect one-shot); `views/CompanyView.vue` (creazione PG data-driven dal vocabolario → `dispatch(AddActor)`); `components/GmConsole.vue` (slide-over Regia, i 6 Command non-combat RequestCheck/ApplyEffect/StartQuest/AdvanceQuest/EnterPhase/EndEncounter, disabilitati per fase via `<fieldset :disabled>`); `App.vue` (banner first-run + trigger Regia in topbar).

**Decisioni correttive dell orchestratore (bug latenti nel piano, corretti nei prompt — il file di piano li conserva nella forma originale):** le viste-form usano **`<div>` non `<form>`** (LoomnButton renderizza un `<button>` con `type=submit` di default → dentro un `<form>` darebbe doppio-fire submit+@click e il Cancel submitterebbe); **GmConsole senza `<Teleport>`** (overlay `position:fixed` inline — nessun antenato della topbar crea un containing-block, quindi copre il viewport, ed e testabile con VTU senza il gotcha Teleport→body).

**Verifica EMPIRICA del feedback di review (working-style §5.2 — dichiarato cosa accolto/scartato):** accolti come hardening reali — **I2** (`ProviderMeta` agganciato a `NonNullable<StatusResult['provider']>` = read-drift guard alla definizione); fallback `EMPTY_PHASE_RULES` const + stub `beforeEach` + doc retry negli store; `<legend>` a11y + commento re-idratazione post-save in Impostazioni; `runFirstRun` store **richiesto** (tolto default fuorviante) + test sparizione reattiva banner + hit-target dismiss; `openCreator` (form PG pulito su riapri); Regia (**cast tipizzati `as Extract<DispatchCommand,...>['campo']` al posto di `as never`**, guardia `count`/`sides`, feedback pulito su apri, `aria-modal`, spacing via gap). **Scartati con motivazione (gold-plating/scope):** I1 (duplicazione `existsSync+readStored` — il fix toccava `loadProviderConfig`, task-scoped invariato), test component ridondanti gia coperti dai test puri, dibattito `role=status` vs `alert`.

**LEZIONE DI PROCESSO (verifica empirica del report, non solo del codice):** in Task 7 l implementer ha **mis-segnalato** 4 unhandled rejection in `App.test.ts` come "rumore pre-esistente identico prima/dopo". **Falso:** erano NUOVE — Task 7 ha aggiunto `<GmConsole>` alla topbar di `App.vue`, il cui `onMounted` chiama `getRuleset`, e `App.test.ts` non stubava `window.loomn`. Scoperto **ri-eseguendo io stesso `App.test.ts`** (mai fidarsi del report). Fix: stub `getRuleset` in `App.test.ts` (`bf532f4`). Lezione: anche il "rumore" dei test va verificato empiricamente, non accettato sulla parola.

**Fuori ambito (deferito, dichiarato):** input del turno/chat narrazione → **10b** (10f fornisce solo il segnale `canRunTurn`); controlli GM di combattimento (StartEncounter/Attack/EndTurn/NextRound) → **10c**; inventario profondo/equip/movimento → display-only/feature deferite (creazione PG parte con `items:[]`); streaming, multi-campagna, delta read-model (§13) → deferiti. Nessuna modifica a `package.json`/`tsconfig`/`vitest.config`/`vitest.workspace`/`electron.vite.config`.

**Prossimo = Piano 10b — Gioco** (chat/narrazione: storia + `run-turn`, input azione coi gate `canRunTurn` di 10f; **dadi 3D `@3d-dice/dice-box`** validati con uno **spike** (CSP/wasm/worker/font offline, eventuale passo orchestratore su CSP/`electron.vite.config`) + chip esito dai `RollResult` negli `events`). Ordine sotto-piani `10a✅ → 10g✅ → 10f✅ → 10b → 10c → 10d → 10e`. Flusso §4 (writing-plans → commit doc su main → branch → subagent-driven → finishing-a-development-branch merge ff → `git push origin main` → aggiorna HANDOFF/memoria). Autorita di design: `specs/2026-06-17-piano10-ui-design.md` (§10 riga 10b, §6 dadi) e [[loomn-piano10-design]].

### 0-vicies. Aggiornamento 2026-06-18: Piano 10b (Gioco: chat/narrazione + dadi 3D) FATTO → prossimo = Piano 10c

**Piano 10b ✅ FATTO e mergiato in main** (ff a `1138a97`; HEAD ora `1138a97`; piano `plans/2026-06-18-loomn-piano10b-gioco.md` committato su main a `8d0bd6d`). **609 test verdi** (570 → 609: +39 renderer = 497 packages + 112 renderer), typecheck pulito (6 progetti). **Gate "esegui l app" SUPERATO** (2 fasi Electron `VERDICT: PASS`, coi nuovi check 10b "Gioco montato coi pannelli" + "narration history coerente dopo il dispatch"). Processo §4: **spike dadi/CSP throwaway PRIMA del piano** (decisione tecnica con l utente via AskUserQuestion) → writing-plans → commit doc su main → branch `feat/piano10b-gioco` → subagent-driven (Step 0 orchestratore + 8 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus 'Ready to merge: Yes'** zero Critical/zero Important) → gate Electron → merge ff → **`git push origin main`** (origin a `1138a97`) → branch cancellato → spike throwaway eliminato.

**DECISIONE TECNICA dallo spike (con l utente, deviazione dallo spec autorita): libreria dadi = `@3d-dice/dice-box-threejs` NON `@3d-dice/dice-box`.** Lo spike (sandbox Vite throwaway + CSP di produzione riprodotta fedelmente in Chrome via Claude Preview) ha **invalidato** la scelta dello spec (`@3d-dice/dice-box` Babylon/Ammo): (1) sotto la CSP attuale (`script-src 'self'`) **non si inizializza** — spawna la fisica in un Worker `blob:` → `worker-src` lo blocca; richiederebbe **due rilassamenti CSP** (`worker-src 'self' blob:` + `script-src 'wasm-unsafe-eval'`); (2) **non sa forzare le facce** (fisica caotica → facce casuali, viola "il codice e l arbitro"/spec §6/§11.3). Il fork **`@3d-dice/dice-box-threejs`** invece: ✅ gira sotto la CSP di **produzione ESATTA, zero rilassamenti** (niente worker/wasm/eval/blob — usa Three.js + fisica JS); ✅ **forza le facce** deterministicamente con notazione `NdS@v1,v2,...` (verificato: `1d20+2d6+1d8@18,3,5,7` → esattamente quelle facce, ripetibile); ✅ output `{sets:[{rolls:[{value}]}]}` mappa diretto da `RollResult.dice`. ⚠️ sides non-standard (d7) → la lib lancia → **token numerico di fallback** (gia richiesto da spec §6). Lo spec §6 prevedeva esplicitamente il fallback Three.js "se lo spike fallisce" — questa e quella strada. **Nessun passo orchestratore su CSP/`electron.vite.config`** (la scelta threejs lo ha reso non necessario): gli asset (texture, 1.7M `.webp`) sono **vendorati offline** in `app/desktop/src/renderer/public/dice-box/textures` (serviti da Vite `publicDir` in dev + copiati in `out/renderer` in build; suoni `sounds:false`).

**Cosa e stato fatto (renderer-only sul backend gia pronto; `@loomn/shared`/engine/host INTATTI; nessun nuovo Command/Event/IPC):**
- **Step 0 (orchestratore):** dep `@3d-dice/dice-box-threejs@0.0.12` (+ transitivo `three@0.143.0`) in `app/desktop/package.json`; texture vendorate; shim di tipi `dice-box-threejs.d.ts` (la lib non spedisce `.d.ts`). NESSUNA modifica a CSP/`electron.vite.config`.
- **Mappa pura** (`lib/dice.ts`): `toDicePlan(dice) -> {notation, tokens}` — raggruppa i dadi standard (d4/6/8/10/12/20/100) per sides in ordine di prima apparizione → notazione `NdS+MdT@valori` a facce forzate; sides non-standard → token numerici. Tipi (`DomainEventView`/`CheckView`/`DieView`/`Outcome`) derivati da `@loomn/shared` `DispatchResult` (renderer non importa engine).
- **Estrazione tiri** (`lib/turn-events.ts`): `extractRolls(events) -> RolledDice[]` dai 3 eventi coi tiri (`AttackResolved.check`/`CheckResolved.result`/`ResourceEffectApplied.roll`), con readout (modifier/total + dc/margin/outcome per le prove) e tag fonte.
- **Store read-side** (`stores/narration.ts`): storia cronologica (cursor-by-seq via `getNarrationHistory`, "carica piu vecchie" con `before`=seq minima, append ottimistico del turno, pending/error). **`stores/dice.ts`**: coda dei tiri dell ultimo turno + `nonce` di ri-trigger.
- **Orchestratore** (`composables/use-run-turn.ts`): `submit(action)` → `run-turn` → su ok appende la narrazione + accoda `extractRolls(events)`; **svuota il readout dadi a inizio turno** (readout = turno corrente); pending/error; gate input via `canRunTurn` di 10f.
- **Componenti** (`components/NarrativePanel.vue`): log storia (serif Newsreader) + input azione **gated da `canRunTurn`+pending** (`<div>` non `<form>` per il gotcha LoomnButton di 10f) + "il Master sta scrivendo..." (streaming DEFERITO). **`components/DiceCanvas.vue`**: 3D `dice-box-threejs`, **init LAZY (primo tiro, mai onMounted)** così non parte WebGL nel gate a finestra nascosta, degrada in silenzio su errore (readout resta autorevole), **serializza i roll del turno** (la lib azzera+rimpiazza → roll concorrenti si sovrascriverebbero). **`components/DicePanel.vue`**: DiceCanvas + readout autorevole del MOTORE (chip `+N`, total, outcome vs CD, token non-standard, tag) — NON il risultato della lib.
- **Wiring** (`views/GameView.vue`): NarrativePanel sul pannello `narrative`, DicePanel su `dice` (scheda/scontro restano placeholder per 10d/10c); self-test `LOOMN_SELFTEST` esteso.

**Verifica (spec §9):** TDD su mappa/store/selector/orchestratore + component test selettivi (VTU jsdom; **i dadi 3D NON in unit test** — jsdom non ha WebGL); **dadi 3D validati dallo SPIKE** (determinismo facce forzate/pool misti/fallback non-standard sotto CSP); gate Electron 2 fasi PASS. **Prova visiva:** lo spike ha screenshottato i dadi 3D che renderizzano sotto la CSP reale; il gate prova il boot dell app coi pannelli. *(Una prova visiva in-app completa — narrazione+dadi pieni — e deferita a quando un provider LLM reale e configurato: `run-turn` senza LLM non produce narrazione/tiri; l utente ha LM Studio installato.)*

**Verifica EMPIRICA del feedback di review (working-style §5.2 — dichiarato accolto/scartato):** accolti come hardening reali — anchor `DieView` documentato + spread leggibile + test del ramo `tag` (T1); `tag`/`margin` asseriti + fallback `skill` + delta negativo (T2); `liveCounter` per-istanza nella factory + test cursore multi-voce/no-op-senza-seq (T3); test del `catch` su reject + `dice` vuoti su errore + commento no-guard (T5); **token CSS corretti** `--text`/`--f-read`/`--bad`/`--f-mono` (il piano usava nomi inesistenti `--text-1`/`--font-serif`/`--danger`/`--font-mono` — drift scoperto dal reviewer, verificato contro `styles/tokens.css`) + test `loadOlder`/errore/draft-clear (T6); **bug CRITICO della concorrenza dadi** (roll concorrenti del turno attacco+effetto si sovrascrivono → serializzati con await-loop) + retry init su errore transitorio + commento singleton (T7); **dead-code `dice.clear()`** wirato a inizio-turno con test (final review). **Scartati con motivazione:** rilassamento CSP per la Babylon (risolto scegliendo threejs); restore-draft-on-failure (UX accettabile, azioni brevi, errore mostrato); `->` vs `→` (convenzione ASCII del progetto); `:key=idx` su liste sostituite in blocco.

**LEZIONE DI PROCESSO (ripetuta da 10f, gestita):** il wiring di GameView (rotta `/`) monta NarrativePanel il cui `onMounted` chiama `getNarrationHistory` → senza stub `App.test.ts` dava 4 unhandled rejection NUOVE. L implementer del Task 8 le ha identificate e stubate correttamente (un 4° file fuori scope dichiarato, legittimo). **Verificato ri-eseguendo io stesso la suite** (mai fidarsi del "0 unhandled rejection" sulla parola — la suite e pulita).

**Fuori ambito (deferito, dichiarato):** streaming token-by-token (spec §11); **cockpit combattimento + zone display-only → 10c**; scheda+inventario → 10d; diario+compagnia → 10e. **Seam dispatch→dadi CHIUSO (follow-up post-10b `6423011`, 612 test):** composable `composables/use-dispatch.ts` (dispatch del Command + `dice.enqueue(extractRolls(events))`) cablato in `GmConsole.vue` → i comandi manuali della **Regia** (RequestCheck/ApplyEffect/Attack) ora **animano il pannello dadi senza un turno AI** (modo deterministico per vedere i dadi: Compagnia→crea PG→Regia→Richiedi prova→Esegui→pannello Dadi). 10c lo riusa per il feedback combat (Attack/EndTurn nel cockpit). **Polish deferito:** colore semantico dell outcome nel readout (oggi `--accent` come il chip modifier); prova visiva in-app con LLM reale (run-turn narrativo). Nessuna modifica a `tsconfig`, `vitest.config`, `vitest.workspace`, `electron.vite.config`, CSP.

**Prossimo = Piano 10c — Combattimento** (cockpit scontro: iniziativa/round/turni; **zone display-only** — label/raggruppamento da `encounter.participants[].zone`, NO mappa spaziale ne azione muovi → DEFERITE post-10, vedi §8; feedback attacco/check/effetto dai `RollResult` negli events di dispatch — **il seam dispatch→dadi e gia cablato** via `composables/use-dispatch.ts` (`6423011`), 10c lo riusa per Attack/EndTurn del cockpit; re-theme combat). Ordine `10a✅ → 10g✅ → 10f✅ → 10b✅ → 10c → 10d → 10e`. Dipende da 10a+10b. Flusso §4. Autorita: `specs/2026-06-17-piano10-ui-design.md` (§10 riga 10c, §11 zone deferite). **[⚠️ SUPERATO — 10c FATTO, vedi §0-unvicies.]**

### 0-tervicies. Aggiornamento 2026-06-19: Piano 10e (Diario + Compagnia: memoria narrativa L2 + canon + roster display-only) FATTO → PIANO 10 (UI) COMPLETO

**Piano 10e ✅ FATTO e mergiato in main** (ff a `0e91cc2`; HEAD ora `0e91cc2`; piano `plans/2026-06-19-loomn-piano10e-diario-compagnia.md` committato su main a `bbce6c5`; pushato a `origin/main` `893f53b..0e91cc2`). **691 test verdi** (664 → 691: +27 renderer = 500 packages + 191 renderer), typecheck pulito (6 progetti). **Gate "esegui l app" SUPERATO** (2 fasi Electron `VERDICT: PASS` su `LOOMN_USERDATA` temp, coi 3 nuovi check 10e: router naviga al Diario [JournalView montato], router naviga alla Compagnia [CompanyView montato], la Compagnia vede il roster dal read-model; **fase 2 = versione 7 ANCORA persistita** — Diario e Compagnia sono read-only, NON aggiungono eventi; `reflect` NON invocato nel self-test perche LLM-backed e nel gate non c e un LLM reale). Processo §4: writing-plans → commit doc su main → branch `feat/piano10e-diario-compagnia` → subagent-driven (5 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus "Ready to merge: Yes"** zero Critical/zero Important) → gate Electron → merge ff → `git push origin main` → branch cancellato. **NESSUN spike necessario, NESSUNA decisione di prodotto aperta** (spec autorita §10 riga 10e; relazioni strutturate deferite §11).

**Cosa e stato fatto (renderer-only; `@loomn/shared`/engine/host INTATTI; NESSUN nuovo Command/Event/IPC/dipendenza/CSP/config; `tokens.css`/`GameView`/`router` intatti):**
- **Mappa pura Diario** (`lib/journal-view.ts`): `toCanonLine`/`sortCanonBySalience` (canon ordinato per salienza, ritirati in coda) + `groupSummaries` (riassunti L2 raggruppati per livello campaign→arc→session→scene, recency per `eventSeqTo` dentro il gruppo, salta i vuoti) + `levelLabel` + `reflectMessage` (esito Reflection in stringa, singolare/plurale). **Cuore testabile #1** (11 test).
- **Store condiviso** (`stores/journal.ts`): read-side della memoria narrativa persistente = riassunti L2 (`getSummaries`) + canon (`getCanon`) + trigger `reflect`; `load()` in parallelo (Promise.all; un errore per-canale conserva l elenco gia caricato dell altro), `runReflect(scope)` (ricarica SOLO su successo, `reflectMessage` per l esito). NON fetch-once. Consumato da Diario (tutto) e Compagnia (canon filtrato). **Cuore testabile #2** (5 test).
- **Diario** (`views/JournalView.vue`, da placeholder a reale): compone cronologia (RIUSA lo store `narration` di 10b, cursor-by-seq + "Carica piu vecchie") + riassunti raggruppati + fatti canon + affordance "Rifletti" (input scope default "sessione" → `journal.runReflect`); read-only sul canon/L2, il solo write e `reflect`. **App.test.ts** aggiornato (stub `getSummaries`/`getCanon`: la route `/diario` ora monta la vista reale → `journal.load()` onMounted). (5 test)
- **Mappa pura Compagnia** (`lib/company-view.ts`): `toCompanyCard` (carta compatta attore: livello/risorse/conteggi) + `canonForActor` (fatti canon che coinvolgono l attore = relazioni DISPLAY-ONLY, match per nome case-insensitive o per id su subject/object; nota slug-collision). **Cuore testabile #3** (4 test).
- **Compagnia** (`views/CompanyView.vue`, arricchita): roster PG/PNG dal read-model (`store.pcs`/`.npcs`) in carte raggruppate + relazioni canon per-attore (display-only via `canonForActor`→`toCanonLine`); **la creazione PG di 10f resta INTATTA** (byte-per-byte). `CompanyView.test.ts` aggiornato (stub `getSummaries`/`getCanon` + 2 test roster, creator test preservato). (3 test)
- **Self-test** (`renderer.ts`): la fase 1 naviga a `/diario` e `/compagnia` (montano le viste reali via route, leggono i canali read del Piano 0); read-only → versione 7 invariata (fase 2). `reflect` NON invocato (LLM-backed).

**Verifica (spec §9):** TDD su mappe/store (i cuori testabili: composizione fonti Diario + selettori roster); component test (VTU jsdom); self-test esteso; gate Electron 2 fasi PASS. **Decisioni di review (verifica empirica, working-style §5.2):** accolti — rimozione `.slice()` ridondante + copertura ramo attivo di `toCanonLine` (T1); test conservazione elenco su canale fallito (T2, ramo contrattuale reale documentato); parentesi su `loadOlder()` + titolo sezione Riflessione (T3, coerenza con NarrativePanel); copertura ramo object-by-id + nota slug-collision (T4, ramo promesso dal titolo del test). **Scartati con motivo (premesse verificate / fuori ambito):** surfacing degli errori store nel Diario (coerente col pattern preesistente di NarrativePanel 10b — decisione cross-cutting fuori ambito 10e, la final review lo conferma non-blocker); `Readonly<ActorView>` (coerente col gemello `sheet-view.ts`); try/catch attorno ai canali IPC tipizzati (risolvono sempre a `{ok:false}`, non lanciano per contratto — il `finally` resetta comunque i flag).

**Fuori ambito (deferito, dichiarato):** **relazioni strutturate** (entita relazione PG↔PNG↔luogo / link quest↔PNG — solo fatti canon display-only per ora, spec §11); editare/ritirare canon dalla UI (il ledger e prodotto da Reflection/eventi, nessun Command); **motore Inventario & Equipaggiamento** e **movimento/topologia di zona** (feature core deferite post-10, §8); streaming token-by-token; multi-campagna; delta read-model. Nessuna modifica a `tokens.css`/CSP/config/dipendenze.

**Prossimo: PIANO 10 (UI) COMPLETO** (`10a→10g→10f→10b→10c→10d→10e` tutti fatti e mergiati). Restano le **feature DEFERITE post-Piano-10, DESIGN-FIRST all apertura**: (a) **motore Inventario & Equipaggiamento** (slot profondi/contenitori/equip-come-azione/catalogo — §8) e (b) **movimento/topologia di zona** (sorella dell Inventario — §8/spec §11), entrambe tracce engine stile SP1-4 co-progettate con la UI; oppure **Piano 11 — moduli a tema**. ⚠️ Per le feature deferite parti da `superpowers:brainstorming` (+ `frontend-design` per i pannelli). Flusso §4. Autorita: `specs/2026-06-17-piano10-ui-design.md`.

---

### 0-duovicies. Aggiornamento 2026-06-18: Piano 10d (Scheda + inventario display-only, data-driven dal Ruleset) FATTO → prossimo = Piano 10e

**Piano 10d ✅ FATTO e mergiato in main** (ff a `83cafea`; HEAD ora `83cafea`; piano `plans/2026-06-18-loomn-piano10d-scheda-inventario.md` committato su main a `da5858e`; pushato a `origin/main` `1522ec1..83cafea`). **664 test verdi** (639 → 664: +25 renderer = 500 packages + 164 renderer), typecheck pulito (6 progetti). **Gate "esegui l app" SUPERATO** (2 fasi Electron `VERDICT: PASS` su `LOOMN_USERDATA` temp, coi 3 nuovi check 10d: router naviga alla Scheda/SheetPanel montato, la Scheda vede l attore dal read-model, router torna al Gioco; **fase 2 = versione 7 ANCORA persistita** — la Scheda e read-only, NON aggiunge eventi). Processo §4: writing-plans → commit doc su main → branch `feat/piano10d-scheda-inventario` → subagent-driven (4 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus "Ready to merge: Yes"** zero Critical/zero Important) → gate Electron → merge ff → `git push origin main` → branch cancellato. **NESSUN spike necessario, NESSUNA decisione di prodotto aperta** (spec autorita §2 dec.5).

**Cosa e stato fatto (renderer-only; `@loomn/shared`/engine/host INTATTI; NESSUN nuovo Command/Event/IPC/dipendenza/CSP/config; `tokens.css` intatto):**
- **Mappa pura** (`lib/sheet-view.ts`): `toSheetView(actor, vocab|null) -> SheetView` (identita/livello/xp + `orderedEntries` attributi/abilita ordinati dal vocabolario [extra in coda alfabetiche, niente dato nascosto] + `resourceBars` con pct clampata in [0,1] [max<=0 → 0] + condizioni formattate [checkModifier con/senza appliesTo, resourcePerTurn, durate turns/scenes/permanent] + oggetti formattati [contributeDice→"NdS", checkModifier, defenseModifier] + flag equipped). `resolveSelectedActor(actors, selectedId)` = preferisci selezione → primo PG → primo attore → null. Switch esaustivi `never`; `noUncheckedIndexedAccess` con `?? 0`/`?? {current:0,max:0}`/`?? null`. NON importa engine (tipi da `@loomn/shared`/read-model). **Il cuore testabile.**
- **Componente** (`components/SheetPanel.vue`): legge `useReadModelStore`+`useRulesetStore` (`ruleset.load()` onMounted come EncounterPanel/CompanyView) + la lib pura; selettore attore (`<select aria-label="attore">` coi NOMI, `v-if length>1`, `:value="actor?.id"` controlled → riflette l attore RISOLTO, anche su selezione stantia); identita/attributi(grid)/abilita(grid)/risorse(barre con %)/condizioni/inventario DISPLAY-ONLY (nome+badge "equipaggiato"+effetti). READ-ONLY: nessun dispatch/use-dispatch/Command/`<form>`. Stato vuoto "Nessun personaggio nel roster". RIUSATO in DUE posti (DRY, spec §5).
- **Route** (`views/SheetView.vue`): da placeholder a `<main class="route-view"><SheetPanel/></main>`.
- **Wiring** (`views/GameView.vue`): `<SheetPanel v-else-if="item.i==='sheet'">` (il pannello scheda non e piu placeholder; `titles` rimosso, tutti i 4 pannelli cablati). `GameView.test.ts` stubba `SheetPanel` (la sua `getRuleset` onMounted darebbe unhandled-rejection nei test che non stubbano `getRuleset` — lezione del mandato). Self-test `LOOMN_SELFTEST` esteso: naviga a `/scheda` (monta SheetPanel via route reale dentro Electron) + check route+attore, torna a `/`. Versione persistita INVARIATA (7): la Scheda non muta lo stato.

**Verifica (spec §9):** TDD su mappa/selettore (i cuori testabili: `sheet-view.test.ts` 16 test, `SheetPanel.test.ts` 8, `SheetView.test.ts` 1); component test (VTU jsdom); self-test esteso; gate Electron 2 fasi PASS. **Decisioni di review (verifica empirica, working-style §5.2):** accolti — test clamp inferiore di `resourceBars` (current negativo, ramo reale dell invariante `Math.max(0,...)`); test ramo a singolo attore del selettore (`v-if length>1`); **fix in-task: il selettore mostra il NOME attore** (l implementer aveva ripiegato sull id per far passare un assert `not.toContain('Eroe')` che collideva con la lista di option — root-cause: ho rivisto l assert su valori risorsa body-only, e tenuto i nomi per la UX, no AI-slop). **Scartati con motivo (premesse verificate FALSE/gold-plating):** refactor `<select>` a `v-model` (avrebbe ROTTO l invariante "il dropdown riflette l attore risolto": con `v-model` su un ref di default '' la option mostrata diverge dalla scheda risolta — il controlled `:value="actor?.id"` e corretto); guardia `?d?` su `formatDice` con dice vuoto (l engine non emette mai un `contributeDice` senza dadi — edge YAGNI); inline di `kindLabel` (computed nominato e leggibile, churn inutile).

**Fuori ambito (deferito, dichiarato):** **motore Inventario & Equipaggiamento** (slot tipizzati profondi, contenitori annidati, equip/unequip/sposta/lascia/consuma come Command/Event, catalogo/economia — feature core post-Piano-10, spec §11/§8); **movimento/topologia di zona** (sorella dell Inventario, deferita); modifica della scheda dalla UI (non esiste un Command per mutare un attore esistente — la scheda e read-only); diario/compagnia → 10e; streaming token-by-token (spec §11). Nessuna modifica a `tokens.css`/CSP/config/dipendenze.

**Prossimo = Piano 10e — Diario + Compagnia** (narrativa L2 + canon [canali read del Piano 0: `getSummaries`/`getCanon`] + trigger `reflect`, roster PG/PNG relazionale). Ordine `10a✅ → 10g✅ → 10f✅ → 10b✅ → 10c✅ → 10d✅ → 10e`. Dipende da 0+10a. Flusso §4. Autorita: `specs/2026-06-17-piano10-ui-design.md` (§10 riga 10e). **10e e l ULTIMO sotto-piano del Piano 10**; dopo → feature deferite (motore Inventario, movimento-zona) design-first, o Piano 11 (moduli a tema).

---

### 0-unvicies. Aggiornamento 2026-06-18: Piano 10c (Combattimento: cockpit scontro + zone display-only) FATTO → prossimo = Piano 10d

**Piano 10c ✅ FATTO e mergiato in main** (ff a `f0e931c`; HEAD ora `f0e931c`; piano `plans/2026-06-18-loomn-piano10c-combattimento.md` committato su main a `89ad8c7`; pushato a `origin/main`). **639 test verdi** (616 → 639: +23 renderer = 500 packages + 139 renderer), typecheck pulito (6 progetti). **Gate "esegui l app" SUPERATO** (2 fasi Electron `VERDICT: PASS` su `LOOMN_USERDATA` temp, coi 5 nuovi check 10c: StartEncounter avvia/entra-in-combat, EndTurn avanza, EndEncounter chiude/torna-fuori-combat; fase 2 = **versione 7 PERSISTITA dopo il riavvio** [lo slice combat e durevole su disco], goblin sopravvissuto). Processo §4: writing-plans → commit doc su main → branch `feat/piano10c-combattimento` → subagent-driven (5 task TDD, implementer sonnet + spec-review + code-quality-review per task; **final review opus "Ready to merge: Yes"** zero Critical/zero Important) → gate Electron → merge ff → `git push origin main` → branch cancellato. **NESSUN spike necessario** (i dadi 3D di 10b gia validati; il seam dispatch→dadi gia cablato). **NESSUNA decisione di prodotto aperta** (spec autorita).

**Cosa e stato fatto (renderer-only; `@loomn/shared`/engine/host INTATTI; NESSUN nuovo Command/Event/IPC/dipendenza/CSP/config):**
- **Mappa pura** (`lib/encounter-view.ts`): `toEncounterView(encounter, actors) -> CockpitView {round, turnIndex, order: ParticipantRow[], zones: ZoneGroup[], current}`. Ordine di turno = `encounter.participants` GIA PRE-ORDINATO dal motore (NON ri-ordina), `isCurrent = i===turnIndex`; join con gli attori per nome/risorse; `isDowned` = condizione `morente` (mirror del literal engine `ActorDowned`); zone raggruppate per stringa `participants[].zone` in ordine di prima apparizione (display-only). `DOWNED_CONDITION_KEY='morente'`.
- **Builder puri** (`lib/combat-commands.ts`): `buildAttack(params)` (omette attribute/skill assenti/vuoti, cast-free), `endTurn()`/`nextRound()`/`endEncounter()` (literal a tipi stretti `Extract<DispatchCommand,...>`), `buildStartEncounter(id, rows)` (filtra i soli `include`, null se nessuno). **Ritornano oggetti PLAIN** (mai i proxy reactive: clone IPC sicura, lezione 10b).
- **Cockpit** (`components/EncounterPanel.vue`): nel pannello `encounter` del preset combat (10a lo mostra al posto della scheda in `[data-phase='combat']`). Display round/turno/iniziativa (turno corrente evidenziato con `var(--accent)` → clay) + risorse + stato "a terra" + zone (display-only); controlli IN-combat via il composable `use-dispatch` (`Fine turno`/`Round successivo`/`Termina scontro` + affordance `Attacco` con attaccante=partecipante di turno, bersaglio/difesa/risorsa dal vocabolario 10g) → i `RollResult` dell attacco animano i dadi 3D di 10b. Round mostrato SOLO nell header `:meta` del pannello. Stato vuoto se nessuno scontro.
- **Regia** (`lib/gm-commands.ts` + `components/GmConsole.vue`): **swap EndEncounter → StartEncounter** in `GM_COMMANDS` (la Regia ospita l ENTRATA in combat, `nonCombatOnly`, visibile fuori combat; l USCITA `EndEncounter` ora vive nel cockpit, `combatOnly`, visibile in combat — simmetria col modello di fase, una sola casa per comando). `StartEncounter` ha un builder di partecipanti (include/iniziativa/zona per attore; `watch(store.actors)` ricostruisce le righe preservando le scelte; `encounterId = scontro-${store.version}`; `:disabled` su "nessuno incluso").
- **Wiring** (`views/GameView.vue`): `<EncounterPanel v-else-if="item.i==='encounter'">` (il pannello scontro non e piu placeholder; `sheet` resta placeholder per 10d). Self-test `LOOMN_SELFTEST` esteso con uno slice combat via IPC reale (StartEncounter da exploration → EndTurn → EndEncounter; versione persistita aggiornata 2→7).

**Verifica (spec §9):** TDD su mappa/builder/cockpit/Regia (l ordine di turno e il raggruppamento per zona sono i cuori testabili, coperti da `encounter-view.test.ts`); component test (VTU jsdom); self-test esteso; gate Electron 2 fasi PASS. **Decisioni di review (verifica empirica, working-style §5.2):** accolti — assert `actedThisRound` (T1), tipi stretti turn-command + nota `damageModifiers` YAGNI + test attribute asimmetrico (T2), rimozione del round duplicato in-body (T3, lo stub di test ora rende `meta`), disabilita "Avvia scontro" senza partecipanti + test preservazione selezioni (T4). **Scartati con motivo:** I-1/I-2 della review di T3 (premesse FALSE verificate: lo stub rende `{{meta}}` quindi un binding rimosso farebbe FALLIRE il test; `defenseBase: z.number()` accetta qualunque numero, l engine non rifiuta) e i minor stilistici/a11y (gold-plating per un tool GM).

**Fuori ambito (deferito, dichiarato):** **movimento/topologia di zona** (traccia engine stile SP1-4 post-Piano-10, co-progettata con l Inventario — spec §11, §8); scheda/inventario → 10d; diario/compagnia → 10e; streaming token-by-token (spec §11); `damageModifiers`/attribute/skill avanzati nell affordance Attacco del cockpit (YAGNI; la Regia copre i casi manuali). Nessuna modifica a `tokens.css`/CSP/config/dipendenze.

**Prossimo = Piano 10d — Scheda + inventario** (identita/attributi/risorse/condizioni/progressione + shell equip/inventario **data-driven** dal modello piatto, Ruleset-aware via `get-ruleset` di 10g; riempie il pannello `sheet` del preset non-combat). Ordine `10a✅ → 10g✅ → 10f✅ → 10b✅ → 10c✅ → 10d → 10e`. Dipende da 10a+10g. Flusso §4. Autorita: `specs/2026-06-17-piano10-ui-design.md` (§10 riga 10d).

---

## 1. Il progetto

**Loomn** = app desktop (**Electron + Vue 3**) per **campagne interattive di qualsiasi genere** con un **Master AI**, girabile sia **in locale (LM Studio)** sia via **API cloud** (client OpenAI-compatibile + adattatori Anthropic/Gemini).

**Principio guida (sacro):** *il codice è l'arbitro, l'AI è il narratore.* Regole/tiri/HP deterministici in codice; l'AI **propone Command tipizzati** e **narra** gli esiti reali, non decide mai i numeri né muta lo stato direttamente.

**Architettura:** esagonale + DDD; **Event Sourcing solo nel contesto Campaign/World**; memoria a strati L1/L1.5(canon ledger)/L2/L3(RAG, Fase 2); monorepo pnpm. Vedi lo **spec completo** (autorità): `docs/superpowers/specs/2026-06-15-simulatore-campagne-ai-design.md`.

Decisioni di prodotto fissate: single + multiplayer con Master AI; motore **generico universale** + moduli a tema (no sistemi su licenza); tiro = **espressione di dadi componibile** + **gradi di successo**; pannello **dadi 3D deterministico**; combattimento **a zone**; authoring moduli via **editor visuale** (AI-authoring **escluso**). Nome **Loomn** (verificato, niente conflitti nel settore).

---

## 2. Documenti chiave (in-repo)

- **Spec (autorità del design):** `docs/superpowers/specs/2026-06-15-simulatore-campagne-ai-design.md`
- **Piani (tutti in `docs/superpowers/plans/`):**
  - `2026-06-15-loomn-fase1-piano1-fondamenta-engine.md` ✅ fatto
  - `2026-06-15-loomn-fase1-piano2-modello-dominio.md` ✅ fatto
  - `2026-06-15-loomn-fase1-piano3-inventario-progressione.md` ✅ fatto
  - `2026-06-15-loomn-fase1-piano4-combattimento-zone.md` ✅ fatto
  - `2026-06-15-loomn-fase1-piano5-event-sourcing.md` ✅ fatto
  - `2026-06-15-loomn-fase1-piano6-persistenza.md` ✅ fatto (pacchetti `shared` + `memory`)
  - `2026-06-15-loomn-fase1-piano7a-provider-layer.md` ✅ fatto (`@loomn/ai`)
  - `2026-06-15-loomn-fase1-piano7b-structured-output.md` ✅ fatto
  - `2026-06-15-loomn-fase1-piano7c-ai-master-pipeline.md` ✅ fatto
  - `2026-06-16-loomn-fase1-piano8a-canon-ledger.md` ✅ fatto (Canon Ledger L1.5 in `memory`)
  - `2026-06-16-loomn-fase1-piano8b-reflection-l2.md` ✅ fatto (Reflection + L2 `summaries` + Salienza in `memory`)
  - `2026-06-16-loomn-fase1-piano8c-context-assembler.md` ✅ fatto (Context Assembler con budget di token; `@loomn/memory` + punto di iniezione in `@loomn/ai`)
  - `2026-06-16-loomn-fase1-piano9a-shell-electron.md` ✅ fatto (Shell Electron `app/desktop`: electron-vite+Vue, contratto IPC in `shared`; vedi §7-bis)
  - `2026-06-16-loomn-fase1-piano9b-wiring-memoria-ai.md` ✅ fatto (Wiring memoria+AI, pacchetto `@loomn/host`; vedi §7-ter)
  - `2026-06-16-loomn-fase1-piano9ci-application-layer.md` ✅ fatto (Application layer in `@loomn/host`: `createCampaignService`/`createLanguageProvider` + `commandSchema` in `shared`; vedi §7-quater)
  - `2026-06-16-loomn-fase1-piano9cii-integrazione-electron.md` ✅ fatto (Integrazione Electron: IPC reale + DB su userData + safeStorage; **conclude il Piano 9**; vedi §7-quinquies). Ultimo sotto-piano del Piano 9.
  - `2026-06-16-loomn-f4-storia-in-canone.md` ✅ fatto (traccia engine/AI — F4: la storia entra in canone via evento `NarrationRecorded`; vedi §0-quater). Spec dedicato: `specs/2026-06-16-f4-storia-in-canone-design.md`. (Prima, sempre sulla traccia: **G1** schemi tool coercivi, fatto direttamente con TDD+final review, senza piano dedicato — vedi §0-ter.)
- **Tracce di rafforzamento (trasversali, NON feature — da affrontare in sessioni separate):** `2026-06-16-rafforzamento-ci-validazione.md` (A: gate self-test scriptabile + CI; B: slice giocabile per validare memoria+AI con un LLM vero; C: rito di review proporzionato). Brief autosufficienti.
- **Memoria:** `C:\Users\zagor\.claude\projects\C--Users-zagor-Desktop-tabl\memory\` (`loomn-project.md`, `loomn-working-style.md`, indice in `MEMORY.md`).

Ogni piano ha in fondo una **roadmap aggiornata** e una sezione **self-review**.

---

## 3. Stato dell'engine (`packages/engine`) — cosa esiste già

Monorepo **pnpm workspaces** (`pnpm-workspace.yaml` globba `packages/*` e `app/*`). Esistono `packages/engine` (dettagliato qui sotto), più `packages/shared` e `packages/memory` (Piano 6 + 8a + 8b + 8c + 9b + 9c-i + 9c-ii — vedi §3-bis) e `packages/ai` (Piani 7a/7b/7c + 8c — vedi §3-ter) e **`packages/host`** (Piano 9b, pacchetto di composizione `ai`+`memory` — vedi §7-ter), più l'app **`app/desktop`** (Piani 9a + 9c-ii — vedi §7-bis/§7-quinquies). TS strict (`tsconfig.base.json`): `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitOverride`, `verbatimModuleSyntax`. Test: Vitest (config root `vitest.config.ts`, include `packages/**/*.test.ts` — **`app/desktop` NON è incluso**: il suo gate è build + `vue-tsc` + esecuzione app/self-test; `packages/host` è incluso automaticamente). **266 test verdi totali** = **221** (baseline a fine 9a) **+ 14 del 9b** (`memory` 3 [`event-store-on`] + `host` 11 [memory-system 4, reflection-ports 5, wiring 2]) **+ 23 del 9c-i** (`shared` 9 [`command-schema`] + `host` 14 [provider 4, campaign-service 10]) **+ 8 del 9c-ii** (`shared/ipc.test.ts` 6→14; il resto del 9c-ii vive in `app/desktop`, fuori dall'include Vitest).

`packages/engine/src/index.ts` ri-esporta **15 moduli**, in quest'ordine. Export pubblici per modulo:

| Modulo | Export principali |
|---|---|
| `random.ts` | `RandomSource` (porta), `createSeededRandom(seed)` — mulberry32 |
| `dice.ts` | `RollMode`('check'\|'effect'), `DieGroup{count,sides,tag?}`, `Modifier{value,source}`, `RollExpr{dice,modifiers,mode}`, `DieResult{sides,value,tag?}`, `RollResult{dice,modifierTotal,total,mode}`, `rollExpression(expr,rng)` |
| `check.ts` | `Outcome`('critical'\|'success'\|'success_at_cost'\|'failure'\|'disaster'), `CheckResult` (RollResult + `dc,margin,outcome`), `outcomeFromMargin(margin)`, `resolveCheck(expr,dc,rng)` |
| `actor.ts` | `ActorKind`('pc'\|'npc'), `ResourcePool{current,max}`, `ConditionEffect`(checkModifier\|resourcePerTurn), `Duration`(turns\|scenes\|permanent), `Condition{key,source,effects,duration}`, `ItemEffect`(contributeDice\|checkModifier\|defenseModifier), `Item{id,name,equipped,effects}`, `Progression{xp,level}`, `Actor{id,name,kind,attributes,skills,resources,conditions,items,progression}`, `getAttribute`, `getSkill` |
| `resource.ts` | `adjustResource(actor,resource,delta)` (clamp [0,max], throw se risorsa ignota), `isDepleted(actor,resource)` |
| `condition.ts` | `addCondition`, `checkModifierFrom(conditions,target?)`, `tickConditions(actor)` |
| `actor-check.ts` | `CheckRequest{actor,attribute?,skill?,baseDice?,situationalModifiers?,includeEquipped?,dc}`, `buildCheckExpr(req)`, `actorCheck(req,rng)` |
| `item.ts` | `addItem`, `removeItem`, `setEquipped`, `equippedItems`, `collectItemDice(items,mode)`, `collectItemCheckModifier(items,target?)`, `defenseValue(actor,defense,base)` |
| `progression.ts` | `awardXp(actor,amount)`, `levelFor(xp,thresholds)`, `applyProgression(actor,thresholds)`, `advanceMilestone(actor)` |
| `zone.ts` | `ZoneMap`(Record<string,string[]>), `RangeBand`('engaged'\|'near'\|'far'), `areAdjacent(map,a,b)`, `zoneDistance(map,from,to)` (BFS), `rangeBand(distance)` |
| `encounter.ts` | `Participant{actorId,zone,initiative,actedThisRound}`, `Encounter{id,participants,round,turnIndex}`, `ParticipantInput`, `createEncounter(id,participants)`, `rangeBetween(enc,map,a,b)`, `currentParticipant(enc)`, `endTurn(enc)`, `roundComplete(enc)`, `nextRound(enc)`, `moveParticipant(enc,map,actorId,toZone)` |
| `combat.ts` | `AttackInput`, `AttackResult{check,hit,damage,target,downed}`, `performAttack(input,rng)` |
| `events.ts` | `DomainEvent` (7 varianti: ActorAdded, EncounterStarted, TurnEnded, RoundAdvanced, AttackResolved, DamageApplied, ActorDowned), `GameState{version,actors,encounter}`, `initialState`, `applyEvent(state,event)` (reducer puro, version++ ogni evento), `replay(events)` |
| `commands.ts` | `Command` (5 varianti: AddActor, StartEncounter, EndTurn, NextRound, Attack), `decide(state,command,rng)` |
| `event-store.ts` | `StoredEvent{seq,event}`, `ConcurrencyError`(class, `readonly expected/actual`), `EventStore`(porta: `version()`/`append(events,expectedVersion)`/`load()`), `createInMemoryEventStore()`, `Snapshot{state,version}`, `takeSnapshot(state)`, `rebuild(stored,snapshot?)` |

**Grafo dipendenze (aciclico):** `random` ← `dice` ← `check`; `actor` → `dice`(type); `resource`/`condition`/`actor-check`/`item`/`progression` → `actor`(+ dice/check); `zone` (foglia) ← `encounter`; `combat` → actor/dice/check/actor-check/resource/condition/item; `events` → actor/check/resource/condition/encounter; `commands` → events + encounter/combat/dice; `event-store` → events. **Nessun ciclo. Niente importa `events`/`commands`/`event-store`/`encounter`/`combat` (a parte il barrel).**

**La proprietà ES centrale (NON romperla):** `decide` consuma l'RNG e registra i **fatti risolti** negli eventi; `applyEvent` **rigioca senza RNG** → replay deterministico. `decide(Attack)` esegue `performAttack` ma usa solo i fatti (`check/hit/damage/downed`), **scartando** il `target` mutato; lo stato cambia solo via `applyEvent`.

### 3-bis. Pacchetti `shared` e `memory` (Piano 6 + 8a + 8b + 8c + 9b + 9c-i) — cosa esiste già

Aggiunti dal Piano 6 (+ Canon Ledger dal Piano 8a, + Reflection/L2/Salienza dal Piano 8b, + Context Assembler dal Piano 8c), mergiati in `main`. Grafo dipendenze: `memory → engine`, `memory → shared`; **`shared` è foglia** (dipende solo da `zod`, NON importa engine). **`memory` NON dipende da `ai`** (regola di dipendenza; la Reflection di 8b usa **porte iniettate** `FactExtractor`/`Summarizer`/`Clock`; le impl LLM-backed vivono nel pacchetto di composizione `@loomn/host`, Piano 9b — `ai` e `memory` non si importano a vicenda).

| Pacchetto | Export / contenuto |
|---|---|
| `@loomn/shared` (`packages/shared`) | `domain-schema.ts` → `domainEventSchema`, `gameStateSchema` (Zod). **Unica fonte di validazione** ai confini. Cast-free: `.transform()` sui 4 campi opzionali (`DieGroup.tag`, `DieResult.tag`, `ConditionEffect.appliesTo`, `ItemEffect.appliesTo`) → `z.infer` assegnabile 1:1 ai tipi engine sotto `exactOptionalPropertyTypes`. **`commandSchema` (9c-i)** = validatore cast-free dell'unione `Command` del motore al confine IPC non fidato (`z.union`, non `discriminatedUnion`, per via del `.transform()` di Attack; riusa `actorSchema`; vedi §7-quater). Dep: `zod`. |
| `@loomn/memory` (`packages/memory`) | `createSqliteEventStore(dbPath): SqliteEventStore` (implementa la porta `EventStore` del Piano 5 + `saveSnapshot`/`latestSnapshot`/`close`); **`createSqliteEventStoreOn(db): SqliteEventStoreOn` (9b — event store sul handle Drizzle CONDIVISO, `= Omit<SqliteEventStore,'close'>`, NON possiede la connessione; `createSqliteEventStore(dbPath)` ora delega `{ ...createSqliteEventStoreOn(db), close }`, refactor a comportamento invariato — lo usa `createMemorySystem` in `@loomn/host`);** `openDatabase(dbPath): OpenDb`. **`createCanonLedger(db): CanonLedger` (8a — L1.5):** `record`/`active`/`all`/`retract`/`supersede` su `canon_facts` (id/subject/predicate/object/eventSeq/status); prende un **handle Drizzle gia aperto** (l app condividera UNA connessione fra event store e ledger); `status` validato Zod in lettura; `supersede` = anti-contraddizione per predicati funzionali (ritira-e-rimpiazza in transazione; funge anche da primo inserimento). **8b — Reflection/L2/Salienza:** `createSummaryStore(db): SummaryStore` (L2 `summaries`, gerarchia scena→sessione→arco→campagna; `record`/`list(filter?)`, `level` validato Zod); `scoreSalience({importance,recurrence}): number` (PURA, importanza×ricorrenza, output [0.1,1]); porta **`Clock`** (`now():number`, tempo iniettato — mai `Date.now`); colonna **`salience`** aggiunta a `canon_facts` (default 0, backward-compat); **`runReflection(deps, input)`** (pipeline **asincrona** fuori dal turno: porte iniettate `FactExtractor`/`Summarizer`/`Clock` → fatti su L1.5 con `supersede`/`record`, riassunto scena su L2, salienza; id **deterministici** per range; ricorrenza come snapshot per-soggetto; testato coi doppi). **8c — Context Assembler (percorso di lettura, spec §6.2):** `createContextAssembler(deps{ledger,summaries,clock}, config{tokenBudget, estimateTokens?, recencyDecayPerHour?}): (state: GameState) => string` (in `context-assembler.ts`) — funzione **deterministica** (rilegge gli store a ogni chiamata) che assembla **L1** (GameState reso in prosa) + **L1.5** (`ledger.active()` filtrato ai *soggetti in scena* = {id,name} degli attori presenti) **sempre inclusi, mai tagliati** + **L2** (`summaries.list()` rankata per **salienza × recency**, decadimento `decay^(ore)` sul `createdAt` via porta **`Clock`**, default 0.995) **tagliata dal basso** (`break` al primo overflow) entro `tokenBudget`; token via porta `estimateTokens` iniettabile (default euristica char/4); esporta anche `recencyWeight`/`defaultEstimateTokens`. **`memory` continua a NON importare `ai`** (il punto di iniezione vive in `ai`, vedi §3-ter). `schema.ts` (tabelle Drizzle `events`/`snapshots`/**`canonFacts`** (+`salience`)/**`summaries`**), `migrations/` (`0000_init` + `0001_canon_ledger` + `0002_summaries` + `0003_canon_salience`, **scritte a mano**, journal 4 entries con `when` congelato, applicate via `migrate()`). Deps: `@loomn/engine`, `@loomn/shared`, `better-sqlite3@^12.10.1` (la 11.x non ha prebuilt per Node 24 sotto pnpm), `drizzle-orm@^0.38.4`, `zod`. |

**Punti chiave (NON romperli):** la porta `EventStore` resta **sincrona** (better-sqlite3 sincrono). `append` usa una **transazione** con check `MAX(seq) === expectedVersion` → `ConcurrencyError` (riusata da engine); validazione Zod **solo in lettura** (`load`/`latestSnapshot`), non in scrittura (gli eventi vengono da `decide`, già tipati). Due **drift guard** a compile-time in `sqlite-event-store.ts` tengono gli schemi Zod allineati ai tipi engine. Una **suite di conformità condivisa** (`event-store-contract.ts`, `runEventStoreContract`) gira verde su in-memory **e** SQLite (contract test, spec §9). **`drizzle-kit` RIMANDATO (verificato empiricamente in 8a):** su questa baseline (journal scritto a mano, senza `0000_snapshot.json`) `drizzle-kit generate` non ha uno snapshot con cui diffare → ricrea **tutte** le tabelle nella nuova migrazione (il `migrate()` fallirebbe) e usa un `when` non-deterministico. Le migrazioni restano **scritte a mano** (deterministiche, `when` congelato, coerenti); drizzle-kit si introdurrà quando il churn dello schema lo giustificherà, con la ricostruzione una tantum della baseline. La `8a` aggiunge `canon_facts` (`0001`); la `8b` aggiunge `summaries` (`0002`) e la colonna `salience` su `canon_facts` (`0003` = `ALTER TABLE ADD ... DEFAULT 0 NOT NULL`, additiva/backward-compat) — tutte a mano, journal a 4 entries con `when` congelato.

### 3-ter. Pacchetto `ai` (Piani 7a + 7b + 7c + punto di iniezione 8c) — cosa esiste già

Aggiunto dai Piani 7a/7b/7c, mergiato in `main`. Dipende da `zod` + `zod-to-json-schema@~3.23.5` + `jsonrepair` (runtime) + **`@loomn/engine@workspace:*` (aggiunta in 7c → `ai` NON è più foglia)** e `@types/node` (dev). Rispetta la regola `ai → engine → shared`; **NON** dipende da `@loomn/shared` (in 7c non serviva — gli `Command`/`Event` vengono già tipati dall'engine; YAGNI). L'engine **non** importa `ai` (nessun ciclo, verificato). Tutto il codice è stato verificato empiricamente in sandbox prima della stesura dei piani.

| Modulo (`packages/ai/src/`) | Export / contenuto |
|---|---|
| `language-model.ts` | Porta `LanguageModel` (`readonly id`; `stream(req): AsyncIterable<LlmStreamEvent>`); tipi `LlmRole`/`LlmMessage`/`LlmToolDef`/`LlmResponseFormat`/`LlmRequest`/`LlmFinishReason`/`LlmStreamEvent`(text\|tool-call\|finish)/`LlmToolCall`/`LlmResponse`; `collectResponse(stream)` → `{text,toolCalls,finishReason}`. |
| `transport.ts` | `HttpRequest`/`HttpResponse` (`body()`/`text()` **mutuamente esclusivi**, single-use)/`HttpTransport`; `createFetchTransport(fetchImpl=fetch)` (avvolge `fetch`; l'adapter non lo chiama mai direttamente). |
| `tracing.ts` | `TraceEvent` (`request`\|`response`\|`validation-failure`\|`retry`\|`error`; **senza timestamp** → chiamanti puri; `strategy` è `string` per evitare cicli), `TracingPort`, `noopTracer`, `createRecordingTracer`. |
| `openai-adapter.ts` | `createOpenAiCompatibleModel(config)` (LM Studio + cloud), `LanguageModelError`, `OpenAiCompatibleConfig`. Internamente: `buildBody` (spread condizionali per `exactOptionalPropertyTypes`), `parseSse` (robusto ai confini di chunk: buffer + `\n\n` + `TextDecoder({stream:true})`), `streamChatCompletion` (chunk validato Zod con `safeParse` + skip difensivo anche su `data:` non-JSON; accumulo tool-call per `index` in Map; emissione ordinata; `[DONE]`). |
| `json-repair.ts` (7b, interno) | `parseJson`, `extractJsonCandidate` (strip fence + slice primo`{`..ultimo`}`, **object-rooted** per design), `repairJson`. `jsonrepair` è **lenient** (coerce testo nudo a stringa) → la validazione Zod è il vero filtro. **Non** nel barrel. |
| `structured-output.ts` (7b) | `createStructuredOutput(model, {tracer?, strategies?})`, `StructuredOutputPort`, `StructuredOutputError` (con `lastText`), tipi `StructuredOutputRequest/Result/Options/Strategy`. 3 livelli in ordine (function-call → json_schema → repair+retry), Zod come gate, cascata su qualunque fallimento; `strategies` per saltare livelli. Costruito sopra `LanguageModel` (testato con fake model). |
| `language-model-contract.ts` | `runLanguageModelContract(label, makeModel)`: suite di conformità condivisa (spec §9), come `event-store-contract.ts`. **Non** nel barrel (utility di test). Oggi gira sull'adapter OpenAI; in Fase 2 la riuseranno Anthropic/Gemini. |
| `master-tools.ts` (7c) | Contratto LLM↔engine del Master. *(Snapshot 7c sotto; il registro e poi cresciuto con SP1-4/G3-G4 [`request_check`/`apply_effect`/`start_quest`/`advance_quest`/`enter_phase`/`end_encounter`, schemi vocab-dipendenti via `enumOrString`] e in **F3-remediation `next_round` e stato RIMOSSO** [il motore auto-avanza il round, I‑01]; **M‑04**: registro memoizzato per identita del `Vocabulary` via `WeakMap`. Oggi 9 strumenti combat / 7 non-combat.)* 7c: 5 strumenti che mappavano 1:1 ai `Command` esistenti: `spawn_npc→AddActor`, `attack→Attack`, `start_encounter→StartEncounter`, `end_turn→EndTurn`, `next_round→NextRound`. Registro omogeneo type-safe (`makeEntry<A>` cattura il tipo concreto dello schema Zod → `ToolEntry` type-erased, niente cast non sicuri). `masterToolDefs()` → `LlmToolDef[]` (schema JSON **inline** via `zodToJsonSchema` openApi3/`$refStrategy:'none'`); `resolveToolCall(name, rawArgs)` → `ToolResolution` (parse via `parseJson` + Zod `safeParse` + map al `Command`, o errore). |
| `master-turn.ts` (7c) | `runMasterTurn(request)`: **turno agentico singolo** (spec §5.4). `assembleContextStub(state)` (stub L1, il vero Context Assembler è il Piano 8c) + `buildMasterMessages` + il ciclo: `model.stream` con `tools`+`toolChoice:'auto'` → `collectResponse` → per ogni tool-call `resolveToolCall` → `decide(state,cmd,rng)` (RNG **iniettato**, `request.rng`) → `applyEvent` → **reiniezione degli Event reali come messaggio `role:'user'`** (provider-agnostico: l'adapter 7a NON fa round-trip dei `tool_calls`; un `role:'tool'` verrebbe rifiutato da un provider reale). Termina su testo libero (= narrazione) o `maxIterations` (default 6). Rami falliti (Zod o `decide` che lancia) tracciati (`validation-failure`/`error`) e **senza eventi**. Ritorna `{state, events, narration, invocations, transcript}`. **8c — punto di iniezione:** `MasterTurnRequest` ha ora `assembleContext?: AssembleContext` (`AssembleContext = (state: GameState) => string`, esportato), usato come `request.assembleContext ?? assembleContextStub`. Il vero Context Assembler (in `@loomn/memory`, §3-bis) e **iniettato dall app**; `ai` resta senza dipendenza da `memory` (compatibilita solo strutturale via il tipo del port). `assembleContextStub` resta come default. |

**Punti chiave (NON romperli):** la porta è **async/streaming** (a differenza dell'`EventStore` sincrono); l'adapter **nasconde** la frammentazione delle tool-call (emette tool-call intere); il transport è **iniettato** (nessuna chiamata di rete reale nei test — fake con SSE predefinito); il `TracingPort` non porta tempo nei suoi eventi (purezza dei chiamanti); il barrel `index.ts` esporta language-model/tracing/transport/openai-adapter/structured-output/master-tools/master-turn (NON il contract né `json-repair`, interni). **7c — il codice è l'arbitro:** nel turno, `decide` consuma l'RNG seedato e produce gli **Event reali**; gli argomenti dei tool sono validati con Zod (`resolveToolCall`) prima di diventare `Command`; comandi invalidi → rifiutati senza eventi. Strumenti rimandati (`request_check`/`apply_effect`/`advance_quest`) e **FSM di fase** (spec §5.5) sono **fuori ambito** di 7c (servono nuovi `Command`/`Event` engine o il contesto quest) — *NB: tutti implementati dopo, SP1-4/G3-G4, §0-octies→§0-duodecies.* Follow-up minore noto **✅ RISOLTO in F3-remediation (I‑04, `a413ab8`):** al raggiungimento di `maxIterations` o a turno muto, `runMasterTurn` emette ora un `TraceEvent` `kind:'error'` diagnostico + una narrazione di fallback deterministica non-vuota (→ `campaign-service` persiste un `NarrationRecorded` di fallback).

---

## 4. Il processo di sviluppo (replicalo identico)

Workflow superpowers, una skill per fase:

1. **Scrivi il piano** — skill `superpowers:writing-plans`. Decomponi in ~4 task bite-sized TDD; ogni task elenca i file esatti, codice completo (no placeholder), comandi con output atteso, e un commit. Includi: riferimenti allo spec, "fuori ambito" esplicito, **disciplina di scope** in ogni task, self-review, roadmap, execution handoff. **Conteggi test attesi** per task (cumulativi). Salva in `docs/superpowers/plans/AAAA-MM-GG-...md`.
2. **Verifica il piano**: grep anti-bug-apostrofo (vedi §5), poi committa il doc su `main` (commit `docs: ...` con la riga Co-Authored-By).
3. **Chiedi all'utente** come eseguire (di solito "subagent-driven").
4. **Esegui subagent-driven** — skill `superpowers:subagent-driven-development`:
   - Crea un **branch dedicato** `feat/fase1-pianoN-...` (MAI implementare su main).
   - Per **ogni task**, in sequenza:
     - **Implementer** (`Agent`): incolla il **testo completo** del task + contesto + **istruzioni di disciplina di scope**. Non far leggere il file di piano al subagent.
     - **Spec review** (`Agent`): "non fidarti del report, verifica leggendo i file e rieseguendo test/typecheck". Deve dire ✅/❌.
     - **Code-quality review** (`Agent`) — *solo dopo* spec ✅. Per i task senza logica (scaffold/solo tipi) la salto e lo dico.
     - **Hardening selettivo** (vedi §5): accogli SOLO rami reali; uno o due test/doc extra via un piccolo Agent "test-only".
   - **Final review** dell'intero branch (`Agent`), con BASE=punto di branch e HEAD=ultimo commit.
   - **finishing-a-development-branch** — skill `superpowers:finishing-a-development-branch`: presenta le 4 opzioni; l'utente sceglie sempre **"Merge in main (locale)"** → `git checkout main && git merge <branch>` (fast-forward) → `pnpm test` (verifica) → `git branch -d <branch>`.
   - **Aggiorna la memoria** (`loomn-project.md`): segna il piano fatto, conteggio test, prossimo passo.

**Scelta del modello:** **la decide l'orchestratore che esegue**, per ruolo, seguendo la skill `subagent-driven-development` (il modello meno potente che regge il compito; il più capace dove serve giudizio/architettura, es. la final review). **Non vincolata a priori — la sceglie l'agente che continua il lavoro** in base a complessità del task e contesto; nessun modello è prescritto per ruolo.

**Comunicazione:** tra un task e l'altro l'utente NON va interpellato; procedi fino al merge. Tieni l'utente aggiornato con una tabellina di stato dei task.

---

## 5. House rules / lezioni dure (rispettale alla lettera)

1. **Disciplina di scope (CRITICO).** Ogni subagent modifica SOLO i file elencati. **MAI** toccare `package.json`, `tsconfig*.json`, `vitest.config.ts`; **MAI** creare un `tsconfig.json` di root o aggiungere `composite`/project references. Verifica `git status --short` prima di ogni commit. *(Incidente reale nel Piano 2: un subagent annullò il fix del typecheck e aggiunse composite/root tsconfig fuori dal commit — andò annullato a mano. Da allora la regola è in ogni prompt di task.)*
2. **typecheck root = `pnpm -r typecheck`** (in `package.json`). **NON** reintrodurre `tsc -b --noEmit` (era rotto: manca un tsconfig root con references). Il typecheck di pacchetto è `tsc --noEmit`.
3. **Verifica EMPIRICAMENTE il feedback dei reviewer prima di applicarlo.** *(Nel Piano 1 un reviewer segnalò un falso "CRITICAL": sosteneva che mulberry32 finisse con `| 0`; il canonico finisce con `^ t`. Smentito con `node -e`. C'è un golden-test che fissa la sequenza canonica.)* Tieni i reviewer all'evidenza (i loro prompt lo richiedono già).
4. **Bug apostrofo nelle stringhe dei test.** Le descrizioni `it('...')`/`describe('...')` in **apici singoli** NON devono contenere apostrofi (`l'`, `un'`, `dell'`, `c'è`). Spezzano la stringa JS. Scrivi senza apostrofo (`l attore`, `c è`); `è/é` vanno bene (sono lettere). **Grep di verifica del piano:** `(it|describe)\('[^']*'[A-Za-zàèéìòù]` → deve dare *no matches*.
5. **Filosofia di hardening (niente over-engineering).** Accogli test/doc extra SOLO per rami **algoritmici/contrattuali reali** (terminazione BFS su cicli, percorsi di `throw`/error-contract, confini del set "colpito", errori tipati, golden values del PRNG). **Scarta** il gold-plating: delegati sottili già coperti a valle, edge YAGNI. Dichiara cosa accetti e cosa scarti.
6. **TS strict.** `exactOptionalPropertyTypes` → niente `campo: undefined` esplicito: usa **spread condizionali** `...(x !== undefined ? { campo: x } : {})`. `noUncheckedIndexedAccess` → l'accesso a `Record`/array è `T | undefined`: usa `?? default` o guardie. Switch su union → **esaustivi** con `default: { const _exhaustive: never = x; ... }`.
7. **Purezza dell'engine.** NIENTE `Math.random`/`Date.now`/stato globale. RNG iniettato (`RandomSource`, seedato mulberry32). Funzioni pure `(stato,…) → nuovo stato`. L'**unico** pezzo con stato mutabile è l'`EventStore` (adapter).
8. **Git:** **remote `origin` configurato (2026-06-18)** = `https://github.com/devfrx/loomn.git` (default `main`; `main` traccia `origin/main`; credenziali cache in Git Credential Manager per l'utente `devfrx`; anche `spike/slice-llm` è su origin). I merge restano **fast-forward locali** (workflow `finishing-a-development-branch`); **dopo il merge di un piano in `main`, fai `git push origin main`** per mantenere il remote allineato. I doc di piano e i fix che faccio *io direttamente* hanno la riga `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`; i commit dei subagent usano messaggi conventional semplici. Branch per il lavoro, `main` per i doc.

---

## 6. Ambiente / comandi

- **OS:** Windows 11. Shell: PowerShell; **Bash disponibile** (usa Bash per git/pnpm, sono POSIX). I warning `LF will be replaced by CRLF` sono cosmetici.
- **Toolchain:** Node v24.9.0, pnpm 9.12.0, corepack 0.34.0 (già installati, su PATH).
- **Verifica (dalla root):** `pnpm test` (Vitest, atteso **266 verdi**), `pnpm typecheck` (→ `pnpm -r typecheck` → `tsc --noEmit` su engine/shared/memory/ai/**host** + `vue-tsc --noEmit` su `app/desktop`), `pnpm -C packages/<pkg> typecheck` per il singolo pacchetto. **NB (attrito noto del monorepo):** il comando `pnpm -C packages/<pkg> exec vitest run <path>` NON risolve la config Vitest di root → per girare un singolo file usa la **root**: `pnpm exec vitest run packages/<pkg>/src/<file>.test.ts`. Il typecheck di pacchetto (`pnpm -C packages/<pkg> typecheck`) invece va.
- **Script ABI nativa (dal 9c-ii, root `package.json`):** `pnpm rebuild:electron` (ricompila better-sqlite3 per ABI Electron — serve solo per eseguire l'app) e `pnpm rebuild:node` (= `pnpm -r rebuild better-sqlite3`, ripristina l'ABI Node). **Se `pnpm test` fallisce con `NODE_MODULE_VERSION 146 ... requires 137`**, la nativa e rimasta su ABI Electron da un gate → lancia `pnpm rebuild:node` (vedi §7-quinquies; fallback: `prebuild-install` nella dir store di better-sqlite3).
- I subagent creano file con lo strumento Write (NON `New-Item -Force`, che tronca).

---

## 7. Piano 7 (7a/7b/7c) e Piano 8 (8a/8b/8c) — COMPLETI (Piani 9a/9b/9c-i fatti; il prossimo passo è in §7-quater/§9 → Piano 9c-ii)

> **Piano 8a — Canon Ledger (L1.5) ✅ FATTO e mergiato** — vedi `docs/superpowers/plans/2026-06-16-loomn-fase1-piano8a-canon-ledger.md` e §3-bis. `canon_facts` (proiezione SQLite) + `createCanonLedger` (record/active/all/retract/supersede). **Piano 8b — Reflection + L2 + Salienza ✅ FATTO e mergiato** — vedi `docs/superpowers/plans/2026-06-16-loomn-fase1-piano8b-reflection-l2.md` e §3-bis: tabella `summaries` (L2) + `createSummaryStore`; `scoreSalience` (importanza×ricorrenza, output [0.1,1]) + porta `Clock`; colonna `salience` su `canon_facts` (migrazione 0003 ALTER); `runReflection` (porte `FactExtractor`/`Summarizer`/`Clock` **iniettate**, asincrona/fuori dal turno, testato coi doppi). Migrazioni ancora **scritte a mano** (drizzle-kit rimandato, §3-bis). **Piano 8c — Context Assembler ✅ FATTO e mergiato** — vedi `docs/superpowers/plans/2026-06-16-loomn-fase1-piano8c-context-assembler.md` e §3-bis/§3-ter: allocatore di lettura (spec §6.2) in `@loomn/memory` (`createContextAssembler`) + punto di iniezione `AssembleContext` in `@loomn/ai` (`runMasterTurn`); L1+L1.5 mai tagliati, L2 rankata per salienza×recency e tagliata dal basso entro un budget di token. Il **Piano 8 è splittato in 8a (fatto) / 8b (fatto) / 8c (fatto)** → **Piano 8 completo**. Il **Piano 9 è splittato in 9a / 9b / 9c**, e il **9c in 9c-i (fatto) / 9c-ii**: 9a (Shell Electron), 9b (Wiring memoria+AI, `@loomn/host`) e 9c-i (Application layer `createCampaignService`/`createLanguageProvider` + `commandSchema`) sono fatti e mergiati (vedi §7-bis, §7-ter, §7-quater); il prossimo è **Piano 9c-ii** (Integrazione Electron: IPC reale + persistenza nell'app, vedi §0/§7-quater/§9). Il follow-up PK di 8a (`record`/`supersede` lanciano su `id` duplicato) è **gestito da 8b** con id deterministici per range (precondizione: una Reflection per range).

Il **Piano 7** (spec §5.4/§7) era splittato in tre sotto-piani, **tutti fatti e mergiati**:

- **7a — Provider Layer ✅ FATTO e mergiato** — vedi `docs/superpowers/plans/2026-06-15-loomn-fase1-piano7a-provider-layer.md` e la §3-ter. Pacchetto `@loomn/ai`: porta `LanguageModel` (async/streaming), adapter OpenAI-compatibile su `HttpTransport` iniettabile, `TracingPort`, suite `runLanguageModelContract`.
- **7b — `StructuredOutputPort` + 3 livelli di fallback ✅ FATTO e mergiato** — vedi `docs/superpowers/plans/2026-06-15-loomn-fase1-piano7b-structured-output.md` e la §3-ter. `createStructuredOutput(model, {tracer?, strategies?})`: dato uno schema Zod, ritorna un oggetto **validato** con 3 livelli in ordine — (1) function-call nativo → (2) constrained decoding `response_format: json_schema` → (3) parse+repair+1 retry — con **Zod come gate** a ogni livello e cascata su qualunque fallimento; `StructuredOutputError` se tutti falliscono. `json-repair.ts` (interno), `TraceEvent` esteso. Deps `zod-to-json-schema@~3.23.5` + `jsonrepair`.
- **7c — AI Master pipeline + tool schemas ✅ FATTO e mergiato** — vedi `docs/superpowers/plans/2026-06-15-loomn-fase1-piano7c-ai-master-pipeline.md` e la §3-ter. `master-tools.ts` (schemi Zod dei 5 strumenti + `masterToolDefs`/`resolveToolCall`, mappa 1:1 ai `Command` esistenti) + `master-turn.ts` (`runMasterTurn`, turno agentico singolo: contesto stub → prompt → `LanguageModel.stream` → tool-call → Zod → `Command` → `decide` con RNG seedato → reiniezione Event reali come `role:'user'` → narra). `ai` ha acquisito **`@loomn/engine`** (NON `shared`: non serviva). **Fuori ambito (rimandati):** strumenti `request_check`/`apply_effect`/`advance_quest` (servono nuovi `Command`/`Event` engine o il contesto quest) e la **FSM di fase** (spec §5.5).

> **Decisioni di confine del Piano 7 (prese in 7a/7b/7c):** `LanguageModel` async/streaming; lo stream emette tool-call **intere** (frammentazione nascosta), `collectResponse` aggrega; transport **iniettato** (nessuna rete reale nei test); validazione Zod ovunque; lo `StructuredOutputPort` è **object-rooted**; schemi degli strumenti in `@loomn/ai`. 7c: turno agentico **singolo** (`toolChoice:'auto'`, niente "risolvi→narra"), reiniezione eventi come messaggio utente (l'adapter 7a non fa round-trip dei `tool_calls`), validazione tool-call con Zod inline (non lo `StructuredOutputPort`, che resta per i casi a singolo oggetto forzato es. Reflection del Piano 8).
>
> **Metodo replicato in 7a/7b/7c (replicalo nei prossimi):** decisioni aperte risolte verso lo spec + **verifica empirica in sandbox** di ogni scelta tecnica prima della stesura (i piani contenevano solo codice già eseguito verde — per 7c provati cross-package import+typecheck, `zodToJsonSchema` inline, l'attrito `.default()`/`exactOptionalPropertyTypes`, il ciclo agentico con determinismo/replay). La sandbox è esterna al repo e va rimossa a fine lavoro.

---

## 7-bis. Piano 9a — Shell Electron ✅ FATTO, e cosa è già verificato per 9b/9c

> **Piano 9a — Shell Electron ✅ FATTO e mergiato** (`cbac493`) — piano `docs/superpowers/plans/2026-06-16-loomn-fase1-piano9a-shell-electron.md`.

**Cosa esiste ora (`app/desktop`, pacchetto `@loomn/desktop`):** electron-vite 4 + Vue 3 + TS strict. `electron.vite.config.ts` (main ESM; **preload forzato a CJS** `format:'cjs'`/`entryFileNames:'[name].cjs'` per `sandbox:true`; `externalizeDepsPlugin({exclude:['@loomn/shared']})` per bundlare il TS di shared; `renderer` con `@vitejs/plugin-vue`). **Un solo `tsconfig.json`** (no project references/composite — house rule §5.1), typecheck via **`vue-tsc --noEmit`** (lo script `typecheck` del pacchetto → incluso in `pnpm -r typecheck`). `src/main/index.ts` (BrowserWindow con `contextIsolation:true`+`sandbox:true`+`nodeIntegration:false`; handler IPC `ping` con validazione Zod; push read-side su `did-finish-load`; log `[MAIN]` diagnostici), `src/preload/index.ts` (`contextBridge.exposeInMainWorld('loomn', bridge)`, CJS), `src/renderer/{index.html (CSP), env.d.ts (window.loomn:LoomnBridge), src/App.vue, src/renderer.ts}`. **Contratto IPC in `@loomn/shared/src/ipc.ts`** (unica fonte, spec §4): `IPC_CHANNELS` + schemi Zod + tipi + `interface LoomnBridge` (`ping`; `onReadModelPush` ritorna un **unsubscribe**). `shared` resta foglia. **Niente DB/AI in 9a.** `app/desktop/package.json` + deps = passo orchestratore (NON subagent).

**GIA VERIFICATO EMPIRICAMENTE in sandbox esterna (NON rifarlo — usalo per scrivere 9b/9c):**
- Electron **42.4.0** → Node **ABI 146**, Chrome 148. `better-sqlite3` (repo: ABI 137 per Node/Vitest) **va ricompilato** per l'ABI Electron (`@electron/rebuild`/`electron-rebuild -f -w better-sqlite3`) → carica sotto Electron. `safeStorage.isEncryptionAvailable()` = `true`.
- **Conflitto ABI (nodo di 9c):** la copia nativa di `better-sqlite3` è **condivisa nello store pnpm** (un solo binario per versione). I test (ABI Node) e l'app (ABI Electron) non possono usarla simultaneamente → tenere il DB **fuori da 9a/9b** (test verdi su ABI Node); in 9c la verifica "app apre il DB" si fa **eseguendo l'app** (ricompilata per Electron), i test automatici restano in `@loomn/host`/`memory` su ABI Node. pnpm tiene copie native **per-versione** (verificato: 11.10.0 e 12.10.1 coesistono).
- **Wiring su connessione condivisa (per 9b → FATTO, §7-ter):** prototipato `eventStoreOn(db)` = corpo dell'event store parametrizzato su un handle Drizzle già aperto → `@loomn/memory` esteso con `createSqliteEventStoreOn(db)` (era: `createSqliteEventStore(dbPath)` apriva la SUA connessione; ora delega). `openDatabase(':memory:')` → ledger+summary+assembler+event-store sulla **stessa** connessione: letture coerenti, concorrenza ottimistica ok. Realizzato in `createMemorySystem`.
- **Composizione `ai`+`memory` (per 9b → FATTO, §7-ter):** verificato che **né `ai` né `memory` possono importarsi a vicenda** (`@loomn/ai` non risolve da dentro `memory`). Il pacchetto `packages/host` (`@loomn/host`) dipende da entrambi e compone le impl LLM-backed di `FactExtractor`/`Summarizer` (su `createStructuredOutput` di 7b) e pilota `runReflection`; resta coperto dall'attuale `vitest.config.ts` (`packages/**`) **senza toccarlo**.
- **Bundling pacchetti TS + migrazioni (per 9c):** electron-vite bundla i pacchetti workspace TS nel main (esbuild); `new URL('../migrations', import.meta.url)` di `memory/db.ts` **sopravvive al bundle** e risolve a `out/migrations` → un plugin Vite inline `closeBundle` che copia `packages/memory/migrations` in `out/migrations` fa passare `migrate()`. `__dirname` disponibile nella config electron-vite.

## 7-ter. Piano 9b — Wiring memoria+AI ✅ FATTO (pacchetto `@loomn/host`)

> **Piano 9b — Wiring memoria+AI ✅ FATTO e mergiato** (`cd2db3d`) — piano `docs/superpowers/plans/2026-06-16-loomn-fase1-piano9b-wiring-memoria-ai.md`. È il pacchetto che i **Piani 9c-i/9c-ii** compongono (9c-i nell'application layer `@loomn/host`; 9c-ii dentro `app/desktop`).

**Cosa esiste ora (`packages/host`, pacchetto `@loomn/host`):** il **pacchetto di composizione** — l'**unico** posto dove `ai` e `memory` si incontrano (regola di dipendenza: né l'uno né l'altro li importa). Deps: `@loomn/engine`, `@loomn/memory`, `@loomn/ai`, `zod`. **NON** dipende da `@loomn/shared` né da `better-sqlite3`/`drizzle-orm` (YAGNI: nessun file di host li importa direttamente — la persistenza passa tutta per `@loomn/memory`; aggiungere una dep è passo dell'orchestratore quando un import reale lo richiede). Un solo `tsconfig.json` (no project references), typecheck via `tsc --noEmit` (incluso in `pnpm -r typecheck`); coperto da `vitest.config.ts` (`packages/**`) senza modifiche.

Export del barrel `packages/host/src/index.ts`:
- **`createMemorySystem(dbPath, config?): MemorySystem`** (`memory-system.ts`) — apre **UNA** connessione (`openDatabase` una volta) e monta sullo **stesso** handle: `createSqliteEventStoreOn(db)` + `createCanonLedger(db)` + `createSummaryStore(db)` + `createContextAssembler({ledger,summaries,clock}, …)`. Ritorna `{ eventStore, ledger, summaries, clock, assembleContext, close }` (`close` = chiusura della connessione condivisa). `MemorySystemConfig`: `clock?` (default `systemClock`), `tokenBudget?` (default **2000**), `recencyDecayPerHour?`, `estimateTokens?` (i due opzionali passati al `createContextAssembler` via **spread condizionali**, mai `field: undefined`, per `exactOptionalPropertyTypes`).
- **`systemClock: Clock`** (`clock.ts`) — `{ now: () => Date.now() }`. L'**unico** punto sanzionato in cui si legge `Date.now`: host è un adapter di composizione; engine e pacchetti puri restano puri (house rule §5.7). Iniettabile/sovrascrivibile nei test (clock fisso).
- **`createLlmFactExtractor(port): FactExtractor`** e **`createLlmSummarizer(port): Summarizer`** (`reflection-ports.ts`) — impl **LLM-backed** delle porte di SCRITTURA (spec §6.1) costruite sopra lo `StructuredOutputPort` di 7b: pilotano `port.generate({messages, schema, schemaName})` con schemi Zod (`extract_facts` → `{facts:[{subject,predicate,object,functional,importance 1..10}]}`; `summarize_scene` → `{text, importance 1..10}`). `renderEventsForReflection(events)` (una riga per evento, deterministico) costruisce il prompt. **`reflectionDepsFor(system, port): ReflectionDeps`** monta `ledger`/`summaries`/`clock` del MemorySystem + extractor/summarizer LLM → pronto per `runReflection` (è lo specchio write-path di `assembleContext`).
- L'assembler reale (`system.assembleContext`) si **inietta** in `runMasterTurn` (`MasterTurnRequest.assembleContext`) al posto di `assembleContextStub` (8c aveva già aperto il punto di iniezione; 9b fornisce l'impl reale).

**Verificato (test su ABI Node, fake, niente rete/DB nativo):** `createSqliteEventStoreOn` interopera col Canon Ledger sullo stesso handle; `createMemorySystem` condivide UNA connessione fra i 4 store; il refactor di `createSqliteEventStore(dbPath)` è a comportamento invariato (18 test event store verdi). Test wiring **end-to-end**: (1) l'assembler reale iniettato in `runMasterTurn` fa arrivare al modello L1+L1.5 (provato via il fatto L1.5 `impugna`, che lo stub non produrrebbe mai → asserzione non-vacua); (2) giro **Reflection write → Context Assembler read** sullo stesso DB (`runReflection` con porte LLM-backed scrive L1.5/L2, poi `assembleContext` li recupera). Le porte LLM-backed testate con fake `LanguageModel` + `createStructuredOutput` reale (esercita il livello function-call) e con fake `StructuredOutputPort`.

**Per il 9c (cosa resta):** `@loomn/host` è **agnostico dall'ABI** (i test girano su ABI Node). Il 9c apre il DB reale via `createMemorySystem` **dentro Electron** → lì serve il nativo ricompilato per ABI Electron + la copia migrazioni in `out/migrations` (groundwork in §7-bis). Il `LanguageModel` reale (adapter OpenAI-compat di 7a) e lo `StructuredOutputPort` (7b) si costruiscono nell'app e si passano a `createLlmFactExtractor`/`createLlmSummarizer`/`reflectionDepsFor`; le chiavi provider via `safeStorage`.

## 7-quater. Piano 9c-i — Application layer della campagna ✅ FATTO (`@loomn/host` + `@loomn/shared`)

> **Piano 9c-i ✅ FATTO e mergiato** (`c0f8808`) — piano `docs/superpowers/plans/2026-06-16-loomn-fase1-piano9ci-application-layer.md`. È la metà testabile su ABI Node del 9c; la metà Electron (IPC reale, DB su userData dentro Electron, safeStorage, nativa per ABI Electron, plugin migrazioni) è il **9c-ii** (✅ fatto — vedi §7-quinquies). Lo split del 9c (deciso con l'utente) ha confinato il pericoloso flip ABI al solo gate finale del 9c-ii.

**Cosa esiste ora:**
- **`@loomn/shared` — `commandSchema`** (in `domain-schema.ts`): validatore Zod dell'unione `Command` del motore al **confine IPC non fidato** (renderer→main, spec §4). Riusa `actorSchema` + nuovi `modifierSchema`/`participantInputSchema`. **`z.union`** (NON `discriminatedUnion`): la variante `Attack` usa `.transform()` per i campi opzionali **cast-free** (`exactOptionalPropertyTypes`), e `discriminatedUnion` rifiuta i `ZodEffects` del `.transform()`. `shared` resta foglia; l'assegnabilità a `Command` del motore è provata a compile-time da un test in `host` (`const wire: Command = commandSchema.parse(...)`) — è il drift guard wire↔motore.
- **`@loomn/host` — `createCampaignService(deps): CampaignService`** (`campaign-service.ts`): l'**application layer** (write side + proiezione read side, spec §5.1/§5.2). `deps = { memory: MemorySystem, model: LanguageModel, structured: StructuredOutputPort, rng: RandomSource }`. Espone: `getReadModel(): ReadModel` (`{version, state: GameState}`, snapshot — delta rimandato §13; `state` è un **riferimento puntuale, da trattare READ-ONLY** → in 9c-ii fai **structured-clone** al push IPC); `dispatch(command): Promise<DispatchOutcome>` (`decide`→`append` con concorrenza ottimistica→`applyEvent`; rigetta sui Command invalidi, nessun Event); `runTurn(playerAction): Promise<TurnOutcome>` (`runMasterTurn` con `memory.assembleContext` reale iniettato; persiste gli Event reali contro `startVersion`); `reflect(scope): Promise<ReflectOutcome>` (`runReflection(reflectionDepsFor(memory, structured), …)`). Proiezione **in-memory** ricostruita all'avvio (`rebuild(eventStore.load())`). **Coda FIFO** (catena di Promise) che serializza dispatch/runTurn/reflect → niente `ConcurrencyError` spuri quando il turno async e un dispatch concorrono (spec §5.6); verificata empiricamente (la coda non si blocca dopo un rigetto, il chiamante riceve comunque il reject).
- **`@loomn/host` — `createLanguageProvider(config): { model, structured }`** (`provider.ts`): provider AI reale = adapter OpenAI-compat (7a) + `StructuredOutputPort` (7b). `config = { baseUrl, model, apiKey?, transport?, tracer? }`, **transport iniettabile** (default `createFetchTransport()`) → testato senza rete con un fake SSE. **NON** tocca `safeStorage`/IO: riceve la chiave **già in chiaro** (la decifra il main in 9c-ii). `structured` costruito sullo **stesso** `model`.
- `@loomn/host` ha acquisito **`@loomn/shared` come devDependency** (solo il test di `campaign-service` importa `commandSchema`; la produzione no).

**Verificato (ABI Node, fake, niente rete/DB nativo):** 258 test verdi (totale **a fine 9c-i**; il totale attuale e 266 dopo il 9c-ii); il servizio gira su SQLite reale (`createMemorySystem(':memory:')`) con fake solo al seam model/port; `runMasterTurn`/`runReflection` reali. La final review (opus) ha verificato empiricamente: coda FIFO (ordine, no-stall su reject, reject propagato al chiamante, niente race sulla versione), `z.union` first-match-wins safe, assegnabilità cast-free wire→motore.

## 7-quinquies. Piano 9c-ii — Integrazione Electron ✅ FATTO (conclude il Piano 9)

> **Piano 9c-ii ✅ FATTO e mergiato** (merge ff a `644dfaf`) — piano `docs/superpowers/plans/2026-06-16-loomn-fase1-piano9cii-integrazione-electron.md`. **Con questo il Piano 9 e COMPLETO.**

**Cosa esiste ora:**
- **`@loomn/shared` — `ipc.ts` riscritto al contratto reale** (foglia: importa solo `zod`+`./domain-schema`): `IPC_CHANNELS` = `dispatch`/`run-turn`/`set-provider`/`reflect`/`get-status` (invoke/handle) + `read-model-push` (send/on); schemi Zod degli esiti come union `{ok:true,…}|{ok:false,error}` (il main non propaga throw grezzi al renderer); `dispatchRequestSchema = commandSchema`; `readModelPushSchema = {version, state: gameStateSchema}`; `LoomnBridge` riscritto; **`ping` rimosso**. (`ipc.test.ts` 14 test.)
- **`app/desktop` — main reale** (`src/main/index.ts`): `createMemorySystem(join(app.getPath('userData'),'loomn.db'))` **dentro Electron** + `createCampaignService` (RNG `createSeededRandom(1)` di dev) dietro **handler IPC sottili** (`safeParse` del payload non fidato → esito tipizzato; `commandSchema` per `dispatch`); `pushReadModel` spinge `{version, structuredClone(state)}` su `did-finish-load` e dopo ogni mutazione; `memory.close()` su **`will-quit`** (NON `window-all-closed`: evita use-after-close su `activate` macOS). **`provider-holder.ts`**: `model`/`structured` STABILI che delegano a un provider corrente swappabile → `set-provider` riconfigura a runtime **senza ricostruire il service e senza modificare `@loomn/host`** (non configurato → errore chiaro → handler `{ok:false}`). **`settings.ts`**: chiave cifrata con `safeStorage` in `userData/settings.json` (mai in chiaro; throw se safeStorage non disponibile; load resiliente `safeParse`), ricostruita all'avvio. Preload bridge reale; renderer diagnostico + **self-test `LOOMN_SELFTEST`** (guidato dal renderer: fase 1 `?selftest=1`, fase 2 `?selftest=2`; logga `VERDICT: PASS|FAIL …`, il main lo cattura via `console-message` ed esce `app.exit(0|1)`).
- **Setup orchestratore (manifesti/config — NON subagent):** deps di `app/desktop` (`@loomn/host`/`@loomn/engine`/**`@loomn/ai`**/`better-sqlite3`/`drizzle-orm`, devDep `@electron/rebuild`); `electron.vite.config.ts` (`exclude` = TUTTI i workspace TS = bundle; plugin `closeBundle` che copia `packages/memory/migrations`→`out/migrations`); script root `rebuild:electron`/`rebuild:node`.

**Gate "esegui l'app" SUPERATO (verifica by-running-the-app, ABI Electron):** build (ABI Node, nativa esternalizzata) → `pnpm rebuild:electron` (better-sqlite3 → ABI Electron 146) → 2 lanci `electron .` con `LOOMN_USERDATA` temp: **fase 1 `VERDICT: PASS`** (DB fresco v0 → dispatch AddActor v1 → set-provider con safeStorage) e **fase 2 `VERDICT: PASS`** (stesso DB: v1 PERSISTITA dopo riavvio = **durabilita su disco** spec §6.3, provider ricostruito da `settings.json` con chiave **decifrata**, attore sopravvissuto) → `pnpm rebuild:node` → 266 verdi ripristinati. Prova end-to-end: nativa carica sotto Electron, migrazioni risolte da `out/migrations`, giro IPC completo, safeStorage round-trip, ES durevole.

**Lezioni operative del gate (VERIFICATE — servono al prossimo che ritocca l'app o rifa il gate):**
- **`rebuild:node` DEVE essere `pnpm -r rebuild better-sqlite3`** (corretto in `644dfaf`). La forma root `pnpm rebuild better-sqlite3` **no-oppa** (better-sqlite3 e dep di `memory`/`desktop`, non della root → non matcha, lascia la nativa su ABI 146). Fallback affidabile se mai no-oppasse: `prebuild-install` nella dir store `node_modules/.pnpm/better-sqlite3@<ver>/node_modules/better-sqlite3` (scarica il prebuilt per l'ABI Node corrente; `node-gyp rebuild` da sorgente invece e fallito qui).
- **`@loomn/ai` va dichiarata dep DIRETTA di `app/desktop`** (pnpm isolato; `@loomn/host` non ri-esporta i port di `@loomn/ai` che `provider-holder.ts` importa).
- **Electron 42 `console-message`**: l'handler riceve un oggetto `details` (`details.message`), non gli arg posizionali. Decisione gate via `startsWith('VERDICT: PASS')` (robusto al contenuto delle label/eccezioni interpolate).
- Verifica dei task *by-app*: `vue-tsc` + `electron-vite build` girano su **ABI Node** (la nativa e esternalizzata, non caricata) → solo i due lanci electron richiedono il flip ABI, confinato al gate.

**Follow-up noti:** ~~`reflect(scope)` riflette l'**intero stream** come una scena → una seconda `reflect` collide sugli id deterministici~~ → **segmentazione per scena ✅ FATTA** (item 6, `2607342`, §0-quaterdecies: cursor esplicito + segmentazione `PhaseChanged`). Seed RNG per-campagna **persistito** rimandato (replay già deterministico). **Delta read-model** (spec §13) rimandato (oggi snapshot completo `{version,state}`).

## 8. Roadmap rimanente (Fase 1)

- **Piano 6 — Persistenza** (SQLite/Drizzle, EventStore SQLite, contract test, pacchetti `shared`+`memory`) ✅ fatto
- **Piano 7a — Provider Layer** (`@loomn/ai`: porta `LanguageModel` async/streaming, adapter OpenAI-compatibile, `HttpTransport` iniettabile, `TracingPort`, contract `runLanguageModelContract`) ✅ fatto
- **Piano 7b — StructuredOutputPort + 3 livelli di fallback** (`createStructuredOutput`: function-call → json_schema → repair+retry; Zod come gate; `json-repair.ts`; `TraceEvent` esteso) ✅ fatto
- **Piano 7c — AI Master pipeline + tool schemas** (`@loomn/ai`: `master-tools.ts` + `master-turn.ts`; `ai` acquisisce `@loomn/engine`) ✅ fatto
- **Piano 8a — Canon Ledger (L1.5)** (`@loomn/memory`: `canon_facts` + `createCanonLedger`; migrazione scritta a mano) ✅ fatto
- **Piano 8b — Reflection + L2 (riassunti) + Salienza** ✅ fatto (tabella `summaries` + `createSummaryStore`; `scoreSalience` + porta `Clock`; colonna `salience` su `canon_facts`; `runReflection` con porte `FactExtractor`/`Summarizer`/`Clock` iniettate; spec §6.1)
- **Piano 8c — Context Assembler** ✅ fatto (budget di token §6.2; legge L1+L1.5+L2, recency a tempo di lettura sul `created_at` via Clock; `createContextAssembler` in `memory` + punto di iniezione `AssembleContext` in `ai`; `assembleContextStub` resta il default a livello di porta — il 9b fornisce l'impl reale `createMemorySystem().assembleContext` e il 9c la inietta nell'app)
- **Piano 9 — Shell Electron, splittato in 9a/9b/9c (e il 9c in 9c-i/9c-ii): ✅ COMPLETO**
  - **9a — Shell Electron** ✅ fatto (`app/desktop`: electron-vite+Vue, sicurezza contextIsolation/sandbox/nodeIntegration:false, contratto IPC tipizzato in `@loomn/shared`, giro IPC main↔renderer + push read-side; **no DB/AI**). Vedi §7-bis.
  - **9b — Wiring memoria+AI (core testabile)** ✅ fatto — pacchetto **`@loomn/host`** (deps engine/memory/ai + zod): `createSqliteEventStoreOn(db)` (event store su handle, aggiunto a `@loomn/memory`), `createMemorySystem(dbPath)` (UNA connessione: EventStore+CanonLedger+SummaryStore+ContextAssembler), `systemClock` (porta **`Clock`** reale), impl **LLM-backed** di `FactExtractor`/`Summarizer` (su `StructuredOutputPort` di 7b — composizione `ai`+`memory` **solo qui**), assembler reale iniettabile in `runMasterTurn`. Unit-testato a porte iniettate (ABI Node). Vedi §7-ter.
  - **9c-i — Application layer della campagna (core testabile)** ✅ fatto — in `@loomn/host`: `createCampaignService` (write side `dispatch`=`decide`→persisti→proietta; proiezione in-memory; `runTurn` con assembler reale iniettato; `reflect`; tutto serializzato FIFO) + `createLanguageProvider` (model+structured, transport iniettabile); in `@loomn/shared`: `commandSchema` cast-free al confine IPC. Tutto ABI Node (`createMemorySystem(':memory:')` + fake model/port), **258 test**. Vedi §7-quater.
  - **9c-ii — Integrazione Electron (IPC reale + persistenza nell'app)** ✅ fatto — `app/desktop` apre il DB via `createMemorySystem(userData/loomn.db)` dentro Electron (nativa ricompilata per ABI Electron + plugin `closeBundle`→`out/migrations`), monta `createCampaignService`+`createLanguageProvider` dietro handler IPC sottili (`dispatch`/`run-turn`/`set-provider`/`reflect`/`get-status` Zod-validati; push read-side `{version,state}`), `safeStorage` per le chiavi, provider swappabile (`provider-holder`), `ipc.ts` riscritto (`ping` rimosso). Gate "esegui l'app" SUPERATO (self-test `LOOMN_SELFTEST` 2 fasi). **Conclude il Piano 9.** Vedi §7-quinquies (incluse le lezioni operative del gate: `rebuild:node` con `-r`, dep `@loomn/ai`, `console-message`/`details`).
- **Traccia engine separata** (decisa in 8c, fuori dalla numerazione memoria) ✅ **FATTA** = SP1-4 (`request_check`/`apply_effect`/quest+`advance_quest` + **FSM di fase** §5.5) + **G3/G4** (vocabolario iniettato via `Ruleset`). Vedi §0-octies→§0-duodecies.
- **Backlog pre-Piano 10** (findings dello spike + traccia engine) ✅ **CHIUSO** = G6 + F2 + SP1-4 + G3/G4 + F3/G5 + **item 6** (segmentazione `reflect` per scena: cursor esplicito `reflection_cursor` + `segmentScenes` + `runScenesReflection`, `2607342`, **444 test**). Vedi §0-quinquies (lista) e §0-quaterdecies (item 6). **→ ora si apre il Piano 10 (UI).**
- **Piano 10 — UI Vue** (chat, scheda PG, **pannello dadi 3D** con `@3d-dice/dice-box` a risultati predeterminati, journal, gestione provider) (grande, probabile split). **✅ STUDIO design-first FATTO** (§0-quindecies; spec `specs/2026-06-17-piano10-ui-design.md`, `5530aaf`): decomposizione `Piano 0 → 10a → 10g → 10f → 10b → 10c → 10d → 10e`; **FATTI: Piano 0** (`614a6bb`), **10a** (`071e71c`), **10g** (`fa6bc17`), **10f** (`bf532f4`), **10b** (`1138a97`, §0-vicies — dadi 3D `@3d-dice/dice-box-threejs` a facce forzate, **NON** `@3d-dice/dice-box`: la scelta dello spec e stata invalidata dallo spike, vedi §0-vicies), **10c** (`f0e931c`, §0-unvicies — cockpit scontro iniziativa/round/turni + zone DISPLAY-ONLY, controlli combat via il seam `use-dispatch`, **639 test**; movimento/topologia di zona DEFERITI post-Piano-10), **10d** (`83cafea`, §0-duovicies — Scheda PG read-only data-driven dal vocabolario + inventario DISPLAY-ONLY sul modello piatto; `SheetPanel` riusato in Gioco+route `/scheda`; **664 test**; motore Inventario/equip/movimento DEFERITI post-10). **Prossimo da scrivere: Piano 10e** (Diario + Compagnia, ULTIMO sotto-piano del Piano 10). *(Quanto segue è il contesto storico dello studio.)* Fase di studio dedicata = brainstorming (skill `superpowers:brainstorming`) + ricerca tecnica e di design (skill `frontend-design`; librerie: dadi 3D alimentati coi risultati **deterministici** dell'engine, scelta della component lib Vue, Pinia per le proiezioni read-side, pattern per chat/scheda PG/journal) + eventuali prototipi/spike, **decidendo l'approccio UI insieme all'utente**. Solo dopo lo studio → `writing-plans` per il Piano 10.
- **Piano 11 — Moduli a tema** (formato dati Zod + import/export + 1 modulo curato)
- **Feature core: Inventario & Equipaggiamento (trasversale engine+AI+modulo+UI) — PIANIFICATA, NON ANCORA APERTA.** Decisa con l'utente il 2026-06-17 durante il brainstorming di G3/G4. Oggi l'inventario è **solo substrato del Piano 3** (`item.ts`: `Item{id,name,equipped,effects}`, effetti dichiarativi `contributeDice`/`checkModifier`/`defenseModifier`, integrato nella matematica di prove/combat/difesa) ma con **profondità di loop ZERO**: nessun `Command`/`Event` oggetto (`grep` confermato), `spawn_npc` mappa sempre `items:[]`, nessuno strumento Master, niente economia/contenitori. (Stessa situazione per la **progressione**: `progression.ts` esiste come funzioni pure ma senza Command/Event/tool.) **Visione dell'utente (requisiti catturati):** (1) ciclo di vita **canonico via `Command`/`Event`** (concedi/equipaggia/sposta/lascia/consuma, event-sourced come il combat); (2) **gestibile dal Master** via strumenti AI (come `attack`/`start_encounter`); (3) **slot di equipaggiamento profondi** in UI — armatura, arma/i, vestiti, accessori — schema di slot tipizzato (non il flag `equipped` booleano attuale); (4) **contenitori annidati** (tasche, zaini) con capacità/ingombro/oggetti-dentro-oggetti; (5) **catalogo + economia** come dati di modulo (§11.9). **Taglia:** paragonabile all'intero sistema combat/encounter, probabilmente decomponibile (stile Piani 7/8/9). **Sinergia chiave con G3/G4:** la **tassonomia degli slot è vocabolario-di-modulo** (slot fantasy ≠ sci-fi) → vive nella stessa casa **`Ruleset` iniettata** che G3/G4 introduce; aggiungere `slots`/`itemCatalog` al `Ruleset` **non tocca la firma** `decide(state,cmd,rng,ruleset)` (churn zero). Fare G3/G4 prima costruisce la fondazione di questa feature. **Sequenza raccomandata (da confermare all'apertura):** engine+AI (Command/Event del ciclo di vita + strumenti Master, traccia engine stile SP1-4) **dopo** il backlog pre-UI; **UI** (pannello a slot) **co-progettata con lo studio del Piano 10** (gli slot profondi sono UX-driven → engine-model e pannello vanno disegnati insieme); **catalogo/economia** nel **Piano 11**. ⚠️ DESIGN-FIRST → all'apertura parti da `superpowers:brainstorming` (+ `frontend-design` per il pannello). NON dentro G3/G4 (scope-creep).
- **Feature: Combattimento a zone (topologia + movimento) — DEFERITA post-Piano-10, sorella dell'Inventario.** Decisa con l'utente il **2026-06-18** durante la chiusura del Piano 10g. Oggi l'engine ha la **matematica** delle zone (`zone.ts` = `ZoneMap`/`areAdjacent`/`zoneDistance`/`rangeBand`; `encounter.ts` = `moveParticipant`/`rangeBetween`) ma con **profondità di loop ZERO**: l'`Encounter` porta solo `participants[].zone` (stringa), **nessuna topologia di zona** vive in `GameState`/`Ruleset`, e **nessun `Command`/`Event` di movimento** (né tool Master) — il movimento è "helper engine non cablato" (audit spec §7 → 🔴 feature/defer). **Conseguenza decisa:** il **Piano 10c rende le zone display-only** (iniziativa/round/turno + label/raggruppamento da `participants[].zone` + feedback attacco/check/effetto dai `RollResult`), **senza** mappa spaziale né azione muovi. La feature completa (topologia di zona persistita nell'`Encounter` o nel `Ruleset` + `Command`/`Event` tipo `MoveParticipant`/`ParticipantMoved` validati con la matematica di `zone.ts` + eventuale tool Master) sarà una **traccia engine stile SP1-4 DOPO il Piano 10**, co-progettata con l'Inventario (entrambe feature interattive che la UI co-disegna). ⚠️ DESIGN-FIRST all'apertura. Razionale della deferral: evitare una deviazione engine a metà-UI; 10c resta utile in display-only. Vedi spec Piano 10 §10 (riga 10c)/§11.

Stima: ~3-5 piani per fine Fase 1 (Piano 9 ora concluso); ~15-22 per la visione completa (Fase 2 = Module Editor, RAG/L3, più provider/moduli; Fase 3 = multiplayer). I piani grandi (7, 8, 10) si decompongono in sotto-piani — è una feature, non un'imprecisione. Da qui in poi si entra nell'IO reale (DB, AI, Electron, UI): i piani saranno meno puramente unit-testabili.

---

## 9. Checklist d'avvio per te (prossimo agente)

1. Leggi questo file (in particolare **§7-quinquies**: cosa ha consegnato 9c-ii = l'app Electron reale con IPC/DB/safeStorage + le **lezioni operative del gate**; **§7-quater/§7-ter/§7-bis**: 9c-i/9b/9a) + lo spec + la memoria di progetto. **Il Piano 9 e COMPLETO** (9a+9b+9c-i+9c-ii): l'app desktop apre il DB reale dentro Electron, monta l'application layer dietro IPC, cifra le chiavi con safeStorage; verificato eseguendo l'app (self-test `LOOMN_SELFTEST`).
2. `git status` (atteso pulito, su `main`, HEAD = ultimo commit doc di handoff [vedi `git log`]; **ultimo CODICE = `14e7d21` D‑01a; D‑01a Campaign Seed FATTO + REMEDIATION 28/28 COMPLETA**; `main` traccia `origin/main`, **tutto pushato** [origin/main allineato]) e **`pnpm test` lanciato DALLA ROOT** (atteso **805 verdi** [777 baseline + 28 di D‑01a]; **NUOVO: `pnpm verify` = typecheck+test aggregati, `pnpm gate:selftest` = gate Electron 2 fasi automatizzato [VERDICT: PASS], `pnpm gate:kill-ghost` = kill degli electron.exe fantasma di Loomn sul lock sqlite — F7/M‑11/D‑06; il gate flippa l ABI a Electron e la ripristina a Node in `finally`] = 565 packages + 212 renderer dopo il fix dadi; era 774 (565+209) dopo F6; era 769 (565+204) dopo F5; era 757 (565+192) dopo F4; era 742 dopo F2; era 719 dopo F1; era 691 dopo 10e; era 639 dopo 10c, 616 dopo i fix post-10b, 612 a fine seam dadi, 609 a fine 10b, 570 dopo 10f, 533 dopo 10g, 515 dopo 10a, 476 dopo Piano 0, 266 a fine Piano 9; **⚠️ da `app/desktop` gira solo il progetto renderer** — la workspace `vitest.workspace.ts` compone i due progetti solo dalla root), `pnpm -r typecheck` pulito (6 progetti, incluso `app/desktop` via `vue-tsc`). **NB: STUDIO PIANO 10 (UI) CHIUSO (§0-quindecies), Piano 0 FATTO (`614a6bb`), 10a (`071e71c`), 10g (`fa6bc17`), 10f (`bf532f4`), 10b FATTO (§0-vicies, `1138a97`), 10c FATTO (§0-unvicies, `f0e931c`), 10d FATTO (§0-duovicies, `83cafea`); spec autorità `specs/2026-06-17-piano10-ui-design.md` (`5530aaf`). Remote `origin` collegato (github.com/devfrx/loomn, default `main`, pushato a `83cafea`). Prossima azione: scrivere il Piano 10e — Diario + Compagnia (narrativa L2 + canon via i canali read del Piano 0 `getSummaries`/`getCanon` paginati + trigger `reflect` nel Diario [`/diario`, oggi `JournalView` placeholder]; roster PG/PNG relazionale nella Compagnia [`/compagnia`]; dipende da 0+10a; **ULTIMO sotto-piano del Piano 10**; nessuno spike, nessuna decisione di prodotto aperta — spec §10 riga 10e). Ordine sotto-piani: `10a✅ → 10g✅ → 10f✅ → 10b✅ → 10c✅ → 10d✅ → 10e`.** **Se i test SQLite (memory/host) falliscono con `NODE_MODULE_VERSION 146 ... requires 137`:** la nativa e rimasta su ABI Electron da un gate precedente → `pnpm rebuild:node` (= `pnpm -r rebuild better-sqlite3`, NON la forma root; vedi §7-quinquies). **Per il gate/avvio app: `pnpm rebuild:electron` poi `pnpm --filter @loomn/desktop dev`; tornando ai test `pnpm rebuild:node`. Se un rebuild fallisce con EBUSY/EPERM su `better_sqlite3.node`: un processo Electron/Vite di Loomn lo tiene bloccato (anche un `electron-vite dev` "fantasma") → trova SOLO i processi Loomn con `Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'tabl|loomn' -and $_.Name -match 'electron|node' }`, fermali con `Stop-Process -Id <pid> -Force` (NON tutti gli electron: ci sono VS Code/Slack/Claude), poi rebuild. NB: una sola istanza dell app sullo stesso `loomn.db` (niente single-instance lock dopo il revert `1fadda2`).** **✅ GATE Electron — `pnpm gate:selftest` FUNZIONA (flag CHIUSO in `d92b480`: ora lancia il **bin electron diretto** via `require('electron')` dal workspace app/desktop + `spawnSync` senza shell, invece di `pnpm --filter exec electron`; ri-verificato fase1+fase2 `VERDICT: PASS`, `rebuild:node` ripristina l ABI Node. QUANTO SEGUE è il problema STORICO ormai risolto, tenuto come contesto):** `pnpm --filter @loomn/desktop exec electron .` dà `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "electron" not found` **subito dopo `rebuild:electron`** (la risoluzione del *bin* via `pnpm exec` si rompe post-rebuild; lo stesso comando funziona standalone, e `build`/`rebuild` — *script* — funzionano). **Workaround verificato (D‑01a, entrambe le fasi VERDICT: PASS): lanciare il BINARIO electron diretto.** Sequenza manuale (bash, NON via `pnpm gate:selftest` finché non è fixato): build (ABI Node) `pnpm --filter @loomn/desktop build` → `pnpm rebuild:electron` → `` GATE=$(mktemp -d); WIN_GATE=$(cygpath -m "$GATE") `` → `LOOMN_SELFTEST=1 LOOMN_USERDATA="$WIN_GATE" app/desktop/node_modules/.bin/electron app/desktop` (atteso `[MAIN] VERDICT: PASS`, exit 0) e idem `LOOMN_SELFTEST=2` sullo STESSO `$WIN_GATE` (persistenza) → `pnpm rebuild:node` (ripristina SEMPRE l ABI Node). **Fix raccomandato in `scripts/gate-selftest.mjs`: sostituire `pnpm --filter exec electron .` con il bin diretto** (poi `pnpm gate:selftest` tornerà la via canonica).
3. **STATO (2026-06-23, sessione 3): D‑01b (generazione AI‑da‑brief del seed) FATTO e mergiato (`76850e8`, **817 test**) + flag gate-harness CHIUSO (`d92b480`); HEAD codice `d92b480`, baseline **817**, tutto su origin/main (fino a `4c442e8`); **AZIONE CORRENTE = D‑01c (onboarding UX, design-first) oppure D‑03 (registro multi‑campagna)** — vedi il PRIMO bullet del §0. [STORICO segue:] REMEDIATION 28/28 COMPLETA + D‑01a (Campaign Seed: motore+contesto) FATTO e mergiato** (`14e7d21`, **805 test**, pushato `44085b1..14e7d21`, branch eliminato; dettagli completi nel PRIMO bullet del §0). **➡️ AZIONE CORRENTE: la prossima slice È STATA DECISA DALL UTENTE = D‑01b (generazione AI‑da‑brief del seed).** **D‑01b è DESIGN-FIRST: NON partire da `writing-plans`/codice** — apri con `superpowers:brainstorming` per esplorare l intent (forma del *brief* in input; il prompt di generazione; il flusso *review/edit del seed prima di confermarlo*; gestione errori; il wiring `campaignSeedSchema`/`createStructuredOutput` di `@loomn/ai`), POI spec → piano → subagent-driven (flusso §4). **Aggancio già pronto da D‑01a:** `seedCampaign(seed)` sul `CampaignService` + `seedCampaignCommandSchema` in `@loomn/shared` (l LLM genera un `CampaignSeed` validato → `seedCampaign`). Lo spec D‑01a `specs/2026-06-23-d01a-campaign-seed-design.md` (`4b61499`) §13 (Foresight) abbozza D‑01b. **⚠️ PRIMA di D‑01b è CONSIGLIATO il fix del gate-harness** (flag in §0/§9 punto 2 + task spawnato): `pnpm gate:selftest` è ROTTO (`pnpm exec electron` → `Command "electron" not found` post-`rebuild:electron`) → fix in `scripts/gate-selftest.mjs` (bin electron diretto), così il gate torna utilizzabile per D‑01b. *(Alternativa decisa-come-2a-scelta: D‑03 registro multi‑campagna, ora additivo.)* *(Storico:)* F6 (renderer-UI `d6ba863`, 774 test) chiuse M‑09/I‑07‑UI/M‑15/M‑10/I‑03 + bottone Round successivo + fix tipi import.meta in env.d.ts. *(Storico sotto:)* FASE 1 + FASE 2 + FASE 3 + FASE 4 FATTE e mergiate (F1 `582fb7a` 719 test; F2 `fe14646` 742 test; F3 `a413ab8` 751 test; F4 `cce7432` 757 test, gate Electron 2 fasi PASS; branch eliminati, pushati) → ESEGUIRE LA FASE 5 (renderer-logica — ABI jsdom, NESSUN gate Electron: I-09 `useDispatch` ingoia i reject IPC [→ `{ok:false,error}`], I-08 errori dei canali read mai mostrati [`PanelError` condiviso, decisione 5], M-07 `useDispatch` non azzera i dadi prima dell enqueue, M-08 azione persa su errore in `NarrativePanel`). Scrivi il piano F5 just-in-time con `writing-plans`, poi subagent-driven (flusso §4); poi F7. F4 (IPC/main) ha chiuso a causa radice debt-free I-02 (canale pull `get-read-model` + `.once`→`.on` + pull-on-mount), I-11 (single-instance + avvio robusto try/catch→dialog + unhandledRejection), M-06 (StatusResult union + getStatus try/catch + store/ProviderMeta) e il flag `.finite()` sui read-DTO; **la final-review-opus ha colto un Critical eseguendo il GATE VERO: il flag self-test "7→8" era REALE (I-01/F1 auto-emette RoundAdvanced su EndTurn con 1 partecipante → +1 evento; F1/F2/F3 non eseguono il gate → versione 7 stantia da F1) → corretto 7→8**; piano `plans/2026-06-22-loomn-fix-remediation-f4-ipc-main.md`. Flag residuo: bottone 'Round successivo' renderer (F6). Nuovi flag annotati (NON in F4): guardia monotonia `applyPush` (`read-model.ts`, CHIUSA in F5), `rulesetResultSchema.defaultResources` `z.number()` nudo. F3 (scope `packages/ai`) ha chiuso I-07-tool (barriera dadi `llmInt(min,max?)`+`MAX_DICE_COUNT`/`MAX_DICE_SIDES`), M-04 (registro tool memoizzato per identita Vocabulary, WeakMap), I-04 (turno muto → fallback deterministico + TraceEvent `kind:'error'`) e RIMOSSO il tool `next_round` (flag F1; Command/commandSchema/bottone GM intatti); piano `plans/2026-06-22-loomn-fix-remediation-f3-ai.md`. **Conseguenza I-04 gestita (utente approva):** campaign-service persiste ora un NarrationRecorded di fallback → 2 test host riconciliati (produzione intatta). NESSUN flag cross-fase nuovo da F3.** F1 ha chiuso a causa radice I-01 (motore possiede la FSM round/turno: EndTurn auto-avanza + rifiuta illegali, riusa endTurn/roundComplete), I-06 (clampPool: invariante current in [0,max] anche alla creazione + guard NaN/Infinity), I-07 (assertDieGroup nel motore: interi, count 1..100, sides 2..1000 — blocca il freeze da count allucinato; bound mirror in shared SOLO sul comando), I-12 (single-source DOWNED_CONDITION_KEY/dyingCondition, rimosso blocco morente morto in combat.ts), I-13 (finiteNumber rifiuta Infinity al confine), M-02 (StartEncounter rifiuta participants vuoto). Subagent-driven 6 task TDD + final review opus, che ha colto un BUG LATENTE del piano: i bound dadi `.int().min().max()` su `dieGroupSchema` raggiungevano il READ-path (itemEffectSchema->actorSchema->domainEventSchema/gameStateSchema, ri-parsati a ogni replay/load) -> avrebbe brickato campagne con item dai dadi fuori-bound -> fix debt-free = split `dieGroupSchema` (lettura permissiva, solo finiteNumber) vs `dieGroupCommandSchema` (bound solo su ApplyEffect); l arbitro resta assertDieGroup nel motore al tiro. *(Storico: all epoca il prossimo era la FASE 2, ORA FATTE E MERGIATE (F2 `fe14646` 742; F3 `a413ab8` 751) — ora il prossimo e la FASE 7 (la FASE 4 IPC/main e ora fatta), vedi la frase d apertura di questo punto e §0.)* **Flag cross-fase aperti da F1:** self-test versione 7->8 (F4), tool `next_round` ridondante per l auto-advance (F3), bottone 'Round successivo' ridondante (F6), **+ NUOVO: `ipc.ts` `canonFactSchema.salience` / `summarySchema.importance`+`salience` senza `.finite()` (read-DTO host->renderer, basso rischio, F4).** *(Quanto segue era il contesto della FASE 1, ora fatta.)* L audit e FATTO (report `docs/superpowers/audits/2026-06-19-loomn-audit-findings.md`: 0 Critical / 13 Important / 15 Minor; 28 finding-class). Mandato: fixare TUTTI i 28 finding a CAUSA RADICE, senza debiti, a FASI. **Piano-campagna a 7 fasi:** `docs/superpowers/plans/2026-06-19-loomn-remediation-campaign.md`. **Piano dettagliato F1 (motore: I-01 FSM round, I-06 clamp risorse, I-07 dadi, I-12 morente single-source, I-13 finite, M-02 scontro vuoto) ESEGUITO e mergiato** (`582fb7a`, `docs/superpowers/plans/2026-06-19-loomn-fix-remediation-f1-motore.md`). *(Storico: la FASE 2 e ora FATTA E MERGIATA — `fe14646`, 742 test; e cosi la FASE 3 (`a413ab8`, 751 test); il prossimo e la FASE 7 (la FASE 4 IPC/main e ora fatta), vedi la frase d apertura di questo punto e §0.)* Le fasi restanti F3→F7 seguono lo STESSO flusso §4, una alla volta. **Decisioni bloccate:** I-01 = motore-possiede-la-FSM (auto-avanza + rifiuta illegali); Regia M-15 = dev-gate; snapshot M-03 = cablato; ambito = solo i 28 bug ora (D-01/D-02/D-03 dopo). **Flag cross-fase da F1:** self-test versione 7→8 (F4), tool `next_round` ridondante (F3), bottone 'Round successivo' ridondante (F6). **Vincolo debt-free:** mai restringere lo schema di LETTURA (rifiuterebbe dati storici) — clamp solo nel motore, `.min(1)`/bound-dadi solo sul comando, `.finite()` ovunque (sicuro). *Quanto segue (audit playbook) è ora CONTESTO STORICO — l audit e concluso.* Il Piano 10 (UI) e COMPLETO (Piano 0/10a/10g/10f/10b/10c/10d/**10e** tutti FATTI e mergiati, §0-sexdecies→§0-tervicies; HEAD `0e91cc2`, **691 test**, gate PASS, pushato). **NESSUNA modifica al codice di prodotto nella sessione di audit** (solo il file di report); i fix (Fase D) sono TDD + subagent-driven nella sessione ANCORA successiva, branch dedicato. **DOPO l audit+fix:** feature DEFERITE post-Piano-10 (motore Inventario & Equipaggiamento, movimento/topologia di zona — §8, DESIGN-FIRST da `superpowers:brainstorming`) o **Piano 11 — moduli a tema**. *Quanto segue è CONTESTO STORICO (10e ora FATTO, §0-tervicies).* **10e** consegna: narrativa L2 + canon (canali read del Piano 0 `getSummaries`/`getCanon`, paginati) + trigger `reflect` nel Diario (route `/diario`, oggi `JournalView` placeholder); roster PG/PNG relazionale nella Compagnia (`/compagnia`, oggi creazione PG di 10f); dipende da 0+10a; nessuno spike, nessuna decisione di prodotto aperta (spec §10 riga 10e); **e l ULTIMO sotto-piano del Piano 10**. *Il resto di questo punto e CONTESTO STORICO (10d ora FATTO, §0-duovicies).* 10d ha consegnato: Scheda PG read-only (identita/attributi/abilita/risorse/condizioni/progressione dal read-model, ordine/etichette dal vocabolario via `get-ruleset`) + inventario DISPLAY-ONLY sul modello piatto; `SheetPanel` riusato nel pannello `sheet` del Gioco e nella route `/scheda`; motore Inventario/equip/movimento DEFERITI post-10.* 10c ha consegnato: cockpit scontro (iniziativa/round/turni dal read-model `encounter.participants` pre-ordinato + `turnIndex`), **zone display-only** (label/raggruppamento da `participants[].zone`; NO mappa spaziale ne azione muovi → traccia engine deferita post-10, §8/spec §11), feedback attacco/check/effetto dai `RollResult` (il **seam dispatch→dadi e gia cablato** da un follow-up post-10b: `composables/use-dispatch.ts` accoda `extractRolls(events)` alla coda dadi, usato da `GmConsole`; 10c lo riusa per i controlli combat del cockpit — Attack/EndTurn — riutilizzando `use-dispatch` invece di `window.loomn.dispatch` diretto), re-theme combat. Dipende da 10a+10b. *Il resto di questo punto è CONTESTO STORICO (Piano 10b ora FATTO, §0-vicies).* 10b ha consegnato: chat/narrazione (storia via `getNarrationHistory` + `run-turn`), input **gated da `canRunTurn`**, e i **dadi 3D** che atterrano sulle facce dei `RollResult` (libreria cambiata in `@3d-dice/dice-box-threejs` dopo lo spike — vedi §0-vicies). ⚠️ **Lo spike dadi/CSP è un rischio d'integrazione reale da validare PRIMA o in apertura del piano**: `@3d-dice/dice-box` usa wasm + (tipicamente) un web worker e carica asset a runtime; i font letterari vanno bundlati offline → sotto `sandbox:true` + la CSP del renderer questo può bloccare wasm/worker/font (eventuale passo orchestratore su CSP/`electron.vite.config`, vedi spec Piano 10 §6 e §9 rischi). **NON ci sono decisioni di design di prodotto aperte come in 10f** (lo spec autorità le ha già decise: dadi `@3d-dice/dice-box`, chat-first per esplorazione/dialogo); il lavoro design-first qui è lo spike tecnico. *Il resto di questo punto è CONTESTO STORICO.* Mandato storico (vedi §0-quinquies): completato TUTTO il backlog pre-Piano 10, poi la UI. Traccia engine/AI guidata dai findings dello spike — **G1 e F4 già FATTI** (`d1ec4a5`, `9e143aa`); findings in `docs/superpowers/findings-slice-llm.md` (incl. ri-validazione post-G1). Sequenza raccomandata (§0-quinquies), **G6 ✅** (§0-sexies), **F2 ✅** (§0-septies), **traccia engine SP1-4: SP1 ✅** (`c04d9a3`), **SP2 ✅** (`dbee1a8`, §0-novies), **SP3 ✅** (`8d3f67b`, §0-decies) **SP4 ✅** (`50c6060`, §0-undecies — FSM di fase §5.5; **TRACCIA ENGINE CHIUSA, SP1-4 fatti**) e **G3/G4 ✅** (`06b398b`, §0-duodecies — vocabolario iniettato via `Ruleset` §5.3 + `spawn_npc` combat-ready, **422 test**) e **F3/G5 ✅** (`0acbff0`, §0-terdecies — coercizione strutturale sul write-path della Reflection, `llmArray`/coercizione su `facts`/`importance`, helper estratti in `coercion.ts` condiviso, **427 test**) e **item 6 ✅** (`2607342`, §0-quaterdecies — segmentazione `reflect` per scena: cursor esplicito `reflection_cursor` + `segmentScenes` + `runScenesReflection`, **444 test**): **BACKLOG PRE-PIANO 10 CHIUSO → prossimo = Piano 10 (UI, design-first)**. Pianificata la **feature core Inventario & Equipaggiamento** (§8, trasversale, all'apertura design-first). Procedi in autonomia fino al merge di ciascuno; **brainstorming prima** solo per gli item design-first. Solo a backlog chiuso → **Piano 10 — UI**. *Le candidate originali restano sotto come contesto storico.* Direzioni di **prodotto**: (a) **traccia engine separata** (nuovi `Command`/`Event` per `request_check`/`apply_effect`/`advance_quest` + contesto quest, e la **FSM di fase** spec §5.5 — porta engine puro, TDD classico su ABI Node, decomponibile); (b) **Piano 10 — UI Vue** (chat, scheda PG, **dadi 3D** `@3d-dice/dice-box` a risultati deterministici, journal, gestione provider) — ⚠️ **NON partire da `writing-plans`**: prima fase di studio/design dedicata (skill `superpowers:brainstorming` + `frontend-design`), decidendo l'approccio UI **con l'utente**; aggancio: `ReadModel={version,state}` + i canali IPC del 9c-ii (Pinia sul read side); (c) **Piano 11 — moduli a tema** (formato dati Zod + import/export + 1 modulo curato). Direzioni di **rafforzamento trasversale** (doc `2026-06-16-rafforzamento-ci-validazione.md`, brief pronti): **A** gate self-test scriptabile + CI, **B** slice giocabile per validare memoria+AI con un LLM vero, **C** rito di review proporzionato. **Raccomandazione della revisione di metodo:** se l'utente e indeciso, la **B** ha il ritorno piu alto (puo invalidare assunzioni del design memoria/AI mentre costano poco), seguita dalla **A** (rete anti-regressione). La scelta resta dell'utente.
4. Flusso §4 per ogni sotto-piano UI (`writing-plans` → commit doc su main → branch → subagent-driven → `finishing-a-development-branch` merge ff → **`git push origin main`** → aggiorna memoria/HANDOFF). Per le slice **design-first** (D‑01b e in generale le iniziative D‑0x): `superpowers:brainstorming` PRIMA → spec → `writing-plans` → branch → subagent-driven → finishing. Per le slice già spec'ate: `writing-plans` direttamente. *(Il Piano 10 UI è COMPLETO; la remediation 28/28 e D‑01a sono FATTE — vedi §0.)*
5. Mantieni il rigore (§5): scope discipline nei prompt, verifica empirica del feedback (dichiara cosa accetti/scarti), hardening solo su rami reali, niente over-engineering. **Follow-up ancora aperti** (vedi §7-quinquies): seed RNG per-campagna persistito, delta read-model (spec §13). *(La segmentazione `reflect` per scena — item 6 — è FATTA, §0-quaterdecies.)*
